# CR-26 — the profiler stops being a status field: three catalogued rows get a shape, and their first fragments

**Status: ADJUDICATED 2026-08-19 — ADOPT WITH CHANGES, and the changes are applied.** The ruling is
`docs/2026-08-19-ruling-cr26.md` (six MUSTs, five SHOULDs; every load-bearing claim HELD, including both
byte-identity claims, the fragment arithmetic, and the stall enumeration audited against `bus.rs` with a
no-unenumerated-source sweep that also cleared `tas` at `:1189`). M1-M3 edited the empyrean amendment and
M4-M6 edited this document; S1-S5 are applied or recorded here. The §3 and §4 blocks below were
**re-extracted and re-byte-compared** against the amended branch afterwards, which is the CR-25 D-M1 lesson:
a block labelled verbatim stops being verbatim the moment its source is edited. Where a correction changes
what this document previously asserted, the superseded assertion is named rather than quietly overwritten
(§5's limits anchor, §6 item 9's premise, §10's delta 2, and the two anchor drifts in §11).

**Originally raised 2026-08-19 as DRAFT, unadjudicated.** Raised against `empyrean/contract/protocol.md` §6 —
specifically the three rows at `protocol.md:1311-1313`, catalogued under *status / misc* since this document's
first draft — and lands as amendment **§11.16**. It is an **amendment to rows that already exist**, not an
invention: the three method names survive unchanged, and what is re-specified is everything under them.

It **adds three schema fragments** — the `profiler` family's first — taking the source
`bus-protocol.schema.json` `methods` object from **33 to 36**, counted by parsing the file before and after
rather than by transcription (both numbers reproduced at the end of this document). Besides the three
fragments it touches the schema in exactly two other places: `initialize.limits` gains two keys on CR-23's
precedent, and the top-level `description`'s fragment count is recounted. That is the whole of its schema
movement.

**Where this lives.** The CR is `oracle-next` branch `cr26-profiler`, cut from `m68000-microop-framework` at
`5341c80` (the profiler-recon merge). The contract edits are `empyrean` branch `profiler-amendment`, cut from
`main` at `d72513c`, committed as a DRAFT and **not merged**.

**Sequencing.** Contract-first, per the 2026-08-17 owner ruling CR-21/22/23 cite and the ruling's own
execution order (`docs/2026-08-19-ruling-profiler-recon.md:84-91`): CR-26 is drafted and adjudicated
**before** slice 1. The empyrean text then merges in the same window as the implementation — CR-25's
sequencing precedent rather than anything this ruling states — so `contract/protocol.md` on `main` is never
ahead of the server nor behind it. Nothing under `crates/` is touched by this arc yet, and the recon's own
slice 0 sets exactly that green criterion: *"`empyrean` docs only; oracle-next untouched."*

This CR does **not** redesign anything. The design is `docs/2026-08-19-profiler-recon.md`, adopted as
written by the controller ruling `docs/2026-08-19-ruling-profiler-recon.md`; the demand it answers is
transcribed from the consumer's own source in `docs/2026-08-19-aeon-profiler-demand.md`. Where the recon
left an ambiguity that a contract cannot leave open, this CR resolves it and says so out loud — §6 below
lists all four such places, and each names the alternative it did not take.

Every line anchor below was read in the file it names, in this session, at the commit named beside it.

---

## 1. Why this is being raised

### The rows exist, and nothing has ever checked a reply against them

`empyrean/contract/protocol.md:1311-1313`, verbatim as they stood before this CR:

```
| `emulator/set_profiler`        | `enabled`         | `enabled` |
| `emulator/get_profiler`        | —                 | `enabled`,`framesRecorded`? |
| `emulator/get_profiler_frames` | `frames`?,`top`?  | `frameCount`,`totalCycles`,`budgetPct`,`routines[]`,`interrupts{}` |
```

Three facts follow, and together they are the reason a CR comes before a handler:

1. **The catalogued shape is the legacy C++ server's, warts included.** `budgetPct` is that server's
   hardcoded NTSC `128000` written into the contract (`oracle/linux-port/gui/ControlSocket.cpp:2033`);
   `routines[]` and `interrupts{}` have no field-level specification at all; and *status / misc* is not
   where a measurement instrument belongs.
2. **There is no schema fragment.** The schema's own top-level `description` names `profiler` among *"the
   deferred families … completed as each is implemented, per §8 item 20: a server's suite closes every
   result against its fragment, so a method without one cannot ship a result nobody has checked"*
   (`contract/schema/bus-protocol.schema.json:5`).
3. **`UNCOVERED_METHODS` is pinned empty** in the reference suite
   (`crates/oracle-aether/tests/schema_conformance.rs:142`, `const UNCOVERED_METHODS: &[&str] = &[];`), so
   advertising a profiler method without a fragment turns that test red. The contract-first ordering here is
   enforced by a gate, not requested by a convention.

### The demand, and the cost of not answering it

Aeon's cost-gate lane runs on the old C++ oracle **and only there**, because *"oracle-next has no profiler
instrument at all"* — their words, quoting an answer this repo gave them on 2026-08-18
(`docs/2026-08-19-aeon-profiler-demand.md` §0). The migration gap is exactly these three rows; every other
call their probe makes already exists on this bus (demand doc §0's table). The instrument they are stuck on
has cost them measured time. Their own runner records *"the oracle stop-race intermittently wedges ONE
emulator-backed gate forever"* (`aeon/tools/effects_gates.py:56-66`, at aeon `18af84f3`, read today for this
CR; quoted without the emphasis the demand doc's transcription adds), and a whole self-re-invoking segmented
architecture exists because of it.

### The three structural defects this shape corrects — verified firsthand for this CR

Each was re-read in the reference tree today rather than carried over from the recon.

| # | Defect | Anchor, read today |
|---|---|---|
| 1 | **The interrupt conflation, at its measured site.** The classification tests the **handler's entry PC** against **vector-table** constants — `if (vec == 0x78 \|\| (vec >= 0x70078 && vec <= 0x7FFFF)) vintCycles += dur; else hintCycles += dur;` — and it appears **twice, verbatim duplicated**. On the consumer's own ROM (`VBlank_Handler` at `$2310`, the HBlank trampoline at `$FFB452`) **both** fall into the `else`, so `interrupts.hint` is HBlank **plus** VBlank and `interrupts.vint` is structurally `0`. | `oracle/linux-port/gui/ControlSocket.cpp:1995`, duplicated at `:2016`; the consumer's own statement of the consequence is `aeon/tools/raster_cost_probe.py:4-15`, transcribed in the demand doc §2 |
| 2 | **Truncating division with two fabricated values.** `avgTotal = totalCycles / numFrames` (`:2031`), `if (avgTotal == 0) avgTotal = 1;` (`:2032`), `avgCycles = st.cycles / numFrames` (`:2041`), `avgCalls = st.calls / numFrames` (`:2042`), `if (avgCalls < 1) avgCalls = 1;` (`:2043`), `avgVint`/`avgHint` (`:2070-2071`). The divisor is the **caller's requested window**, not anything each row appeared in, and no error bound is reported anywhere. | `oracle/linux-port/gui/ControlSocket.cpp:2031-2043`, `:2070-2071` |
| 3 | **A stall-blind clock.** `_currentCycle += cyclesExecuted;` advances only by the static per-opcode table, while `additionalTime` — bus waits, VDP/DMA contention — goes into `_currentTime`, a separate real-time double. Every figure that instrument has ever produced is therefore an **ideal-cycle** figure. | `oracle/Devices/M68000/M68000.cpp:1029-1031` |

Defect 3 is the one that outranks the others for this CR, because it is the only place where our answer and
theirs must legitimately differ, and the amendment is written around making that difference **subtractable**
rather than tolerable.

---

## 2. What exists today

**In the contract.** The three rows (`protocol.md:1311-1313`), inside the *status / misc* table
(`:1303-1314`). §11.15 is the last amendment entry (`:2627`), so §11.16 appends at end of file. `profiler`
is already a typed capability key in the handshake (`contract/schema/bus-protocol.schema.json:200`), and
already advertised `false` by the reference server (`crates/oracle-aether/src/engine.rs:834`).

**In the reference server.** Nothing — there is no profiler, and no shadow stack of any kind. The recon's
grep for `step_out|step_over|call_stack|callStack` across `crates/` returns nothing (recon §B.4); re-run for
this CR, it still matches **zero files**. The one
number the design needs that the seams do not already carry is the just-retired instruction's cycle cost,
which is a live binding at `crates/oracle-core/src/system.rs:1035` (`let cycles = self.step_cpu(sink);`) and
is never handed to the sink. The interrupt **cause** needs no core change at all: the fc = 7 acknowledge
read is already on the sink's event stream (`crates/oracle-core/src/bus.rs:1063`).

**On the demand side.** `aeon/tools/raster_cost_probe.py` reads exactly two things out of the profiler:
`get_profiler.frames_recorded` as a sanity field, and `get_profiler_frames.routines[]`, whose rows it walks
for `addr`, `cycles` and `calls` (demand doc §1.2). Those figures are consumed by a merge gate that compares
with **`==`** against constants derived from engine source (`aeon/tools/effects_gates.py:754-756`), which is
why exactness is the entry ticket and not a bonus.

---

## 3. The proposed §6 text (verbatim, as it landed on the branch)

A new `### profiler` group, inserted immediately before `### status / misc`; the three rows are removed from
that table. **This block is byte-identical to `contract/protocol.md:1303-1432` on `profiler-amendment` at
`a4002fb`**, the post-M1-M3 tip: re-extracted from the branch after those edits, never retyped.

```markdown
### profiler
*(The three rows below were catalogued under **status / misc** with a legacy shape and no schema fragment.
**§11.16** (CR-26, 2026-08-19) re-specified them, gave them their first fragments, and moved them here: the
method names are unchanged, and a measurement instrument is not status.)*

| Method | params | result |
|---|---|---|
| `emulator/set_profiler` | `enabled` (bool), `perFrame`? (bool, def false) | `enabled`, `perFrame` |
| `emulator/get_profiler` | — | `enabled`, `perFrame`, `framesRecorded`, `routineCount` |
| `emulator/get_profiler_frames` | `frames`? (≥1, ≤`limits.maxProfilerFrames`), `top`? (≥1, ≤`limits.maxProfilerRoutines`) | `frameCount`, `sampleCycles`, `totalCycles`, `perFrameExact`, `budgetPct` \| `budgetPctOmitted`, `routines` (§2.4 container of `{addr,name?,disp?,cycles,cyclesSelf,stallCycles,calls}`), `interrupts{hint,vint}{cycles,stallCycles,calls}`, `perFrame`? (§2.4 container of `{frame,cycles,stallCycles,hintCycles,vintCycles}`) |

> **What the profiler measures, and what a row means** *(added 2026-08-19, §11.16)*. `set_profiler` arms an
> exact per-invocation CPU-cycle accountant, `get_profiler` reports its state, and `get_profiler_frames`
> returns the accumulated sample. Ten behaviours are normative.
>
> - **Attribution is exact, and a row is keyed by a routine entry address.** Every retired instruction's
>   cycle cost is charged to exactly one routine. `cycles` is **inclusive** of callees — the meaning the
>   legacy rows had in practice, kept so a migrating consumer's arithmetic does not change — and
>   `cyclesSelf` is the same span with callee time subtracted. `calls` is the count of **actual
>   invocations**. A server MUST NOT satisfy these rows by PC sampling: they are compared with `==` by the
>   consumers this surface exists for, and an estimator cannot be equality-gated.
>
> - **`stallCycles` is the part of `cycles` the CPU spent held off the bus rather than executing.** It is a
>   **subset of `cycles` on the same row, keyed identically to it** — same routine key, same inclusive
>   span, same in-server division — so `cycles - stallCycles` is a well-formed subtraction and not two
>   quantities with different aggregation glued together. Exactly three conditions count, enumerated rather
>   than described so that a fourth is a visible amendment and never a silent widening under green gates:
>   **(i)** a 68000 write to the VDP data port held off while the write FIFO is full; **(ii)** a 68000 read
>   of the VDP data port waiting for that FIFO to drain; **(iii)** the whole bus-hold window of a 68k→VDP
>   DMA, billed to the instruction that armed it. A VRAM fill and a VRAM copy contribute **nothing**,
>   because the 68000 keeps running through both. A server whose bus stalls the CPU for a reason not on
>   this list raises an amendment before counting it here: a client subtracting `stallCycles` to recover an
>   ideal-cycle figure is entitled to know exactly what it subtracted.
>
> - **Division happens inside the server, and the sample is delimited by frame boundaries at both ends.**
>   Every row's `cycles`, `cyclesSelf`, `stallCycles` and `calls`, both interrupt buckets, and
>   `totalCycles` are **per-frame figures over the sample** — the exact sample total divided by
>   `frameCount`. `sampleCycles` carries the undivided total for the whole sample. The sample **opens at
>   the first frame boundary after arming and closes at the most recent one**, so every frame it covers is
>   a whole frame; cycles retired before the opening boundary or after the closing one are attributed to no
>   reported frame. **A partial first frame is therefore inexpressible**, which is what retires the
>   hand-applied "ask for one frame fewer than you ran" compensation the legacy surface required of its
>   callers.
>
> - **Truncation is reported, never absorbed, and nothing is fabricated.** The divisions are integer
>   divisions. **`perFrameExact` is `true` if and only if every divided figure in the reply divided without
>   remainder**; when it is `false` each divided figure is floored — one-sided low, bounded by one unit per
>   figure — and a client gating with `==` MUST check it, because `perFrameExact: true` is also what makes
>   `cycles - stallCycles` exact arithmetic rather than the difference of two truncations. A count is never
>   floored **up**: a routine invoked fewer times than the sample has frames reports `calls: 0`, and a
>   server MUST NOT substitute `1` for it, MUST NOT substitute `1` for a zero denominator, and MUST NOT
>   answer an empty sample with an error. An empty sample is `frameCount: 0`, empty lists, every divided
>   figure `0` and `perFrameExact: true` — there is nothing to divide and nothing was lost.
>
> - **`frameCount` is counted, and it is the number to divide by — read it rather than assume it.** It is
>   the number of **whole** frames in the sample: incremented at each boundary observed while armed **after
>   the one that opened the sample**, which is never counted, because the span before it is not a frame the
>   sample saw. *n* boundaries observed therefore delimit **n − 1** counted frames, and an armed instrument
>   that has seen one boundary and no more reports `0`. A server MUST NOT derive the number by subtracting
>   frame indices, which are positions on a clock `reset` restarts (§2.2).
>   `get_profiler.framesRecorded` is the same number seen from the snapshot method, and the two MUST agree
>   when no frames were run between the calls. Whether a client that ran `S` frames sees `S` or `S − 1`
>   depends on where in the frame it armed, and that is precisely why the count is on the wire.
>
> - **`interrupts` is keyed by interrupt CAUSE, never by handler address.** `hint` is the level-4 (HBlank)
>   bucket, `vint` the level-6 (VBlank) one, and a server MUST classify by the interrupt it acknowledged
>   rather than by where the handler happens to live: an address heuristic mis-buckets for any ROM whose
>   vector points somewhere it did not anticipate, and a silently wrong number is worse than a missing one.
>   A bucket **opens when the interrupt is taken** — the acknowledge cycle, not the raise — and **closes at
>   the matching `RTE`**, and its `cycles` is inclusive of everything the handler called. **The matching
>   `RTE` is the one that unwinds that interrupt's own exception frame**: the `RTE` of an exception taken
>   *inside* the handler — a `TRAP`, an address error — closes that exception and **not** the bucket, and an
>   `RTE` that finds no open bucket closes nothing. An interrupt the sample never saw taken, because it was
>   taken before the sample opened, accrues to no bucket at all: a server MUST NOT retroactively open one,
>   since a bucket whose entry was never observed would report a cost the sample cannot account for.
>   `calls` counts the times the interrupt was **taken**: a raised-but-masked HBlank appears nowhere. **Nesting is by depth and
>   never double-counted** — cycles retired while a nested HBlank runs inside a VBlank handler belong to
>   `hint` alone, and the suspended `vint` bucket accrues nothing until the nested one closes. The split is
>   additive to per-routine rows, not a replacement: a handler's entry address still gets its own
>   `routines[]` row.
>
> - **`addr` is canonical; `name` is the bare label and `disp` is a number.** `addr` is a hex string (D9
>   category 1) of the routine's entry address **masked to the 24 address lines the 68000 drives**, so a row
>   key and a listing address compare directly instead of through a client-side mask. `name` is §4's
>   identifying spelling with no `+$hex` suffix, ever, and `disp` is the displacement past it — `0` in the
>   normal case, where the row key is exactly a label. The two are **absent together** when nothing
>   resolves: a server MUST NOT fall back to the address string, because a client cannot tell a symbol
>   spelled like an address from a lookup that failed.
>
> - **`budgetPct` is derived from the machine's own `timingBasis`, or it is not emitted.** It is
>   `totalCycles` as a percentage of one frame's CPU-cycle budget, and that budget MUST be computed from
>   the `timingBasis` this same server advertises (§2.1, D16) — `mclkPerFrame` over the master clocks per
>   CPU cycle — never from a hardcoded constant, which is wrong by ~16% the moment the machine is PAL. Read
>   it as a **sanity check on the accounting** rather than a measure of load: on a machine whose CPU is
>   never halted it sits at ~100%, and a materially lower figure means cycles went somewhere the sample does
>   not attribute. If the basis changed inside the sample the derivation is ambiguous, and the server MUST
>   **omit `budgetPct` and emit `budgetPctOmitted`** naming the condition instead of averaging over the
>   change. Exactly one of the two keys is present.
>
> - **Arm, disarm and read are synchronous with the command, and none of the three requires a paused
>   machine.** A server MUST apply `set_profiler` before it replies: a client that arms, runs frames and
>   reads in three consecutive calls MUST see the sample those calls describe, with no sleep, no poll and
>   no extra frame needed for the arm to take effect. `set_profiler{enabled:true}` **resets the
>   accumulators** — there is no resume in this revision, and a second arm therefore discards an in-flight
>   sample — while `set_profiler{enabled:false}` **retains** the sample so it can be read afterwards, and
>   reading never clears. None of the three may be refused with `-32005` **for the machine's run state**:
>   the sample's edges are frame boundaries, not the instant the command landed, so a free-running arm is
>   exactly as well defined as a paused one, and both reads are pure. That exemption is about run state and
>   nothing else — the `perFrameNotArmed` refusal in the next behaviour is a refusal about the *instrument's*
>   state, and it applies whether the machine is running or paused.
>
> - **`routines` and `perFrame` are §2.4 policy-bounded containers, and their bounds are refused rather
>   than clipped.** Both take the nested container spelling `{items, total, returned, truncated, limit?}`,
>   because each is a *field* of a larger result rather than the whole of it; neither method accepts a
>   cursor, so neither may emit one (§2.4 clause (b)). `top` bounds `routines.items`, `routines` is ordered
>   by `cycles` descending so that a truncated list is the expensive end rather than an arbitrary slice, and
>   a `top` above the server's cap is **refused with `-32602`, never clamped** — the cap is a policy number,
>   so it is advertised as **`limits.maxProfilerRoutines`** (REQUIRED when the method is advertised, on the
>   `maxWriteLen` / `maxHashLen` precedent). **`perFrame` is opt-in**: present if and only if the sample was
>   armed with `set_profiler{perFrame:true}`, carrying whole-frame totals only (no per-routine breakdown)
>   and no division, since a per-frame row is already per frame. Its ring depth is
>   **`limits.maxProfilerFrames`**, required on the same condition. `frames` bounds the list to that many
>   most-recent frames, is refused above the ring depth with `-32602`, and is **refused with `-32005`
>   (`data.reason = "perFrameNotArmed"`)** when the ring was not armed, because a parameter that cannot
>   affect the answer is worse than one that is rejected. No `pct` key is emitted anywhere: the legacy one
>   divided inclusive cycles by a frame total and summed far past 100%, and a client that wants a share divides `cyclesSelf` by `totalCycles`, which is the ratio
>   that sums to at most 100.
>
> `get_profiler_frames` MAY emit a `caveat` (§2.4) — conditionally, as on an empty sample — and its
> fragment declares the key; the row above does not enumerate it, per §2.4 rule 4.
```

---

## 4. The §11.16 entry (verbatim, as it landed on the branch)

Appended at end of `protocol.md`, which is 2853 lines at that point. §11.15 is the previous entry and the
last one: `:2627-2724` before this CR, `:2755-2852` after §3's insertion pushed everything below it down by
**128** lines. *(Corrected per ruling S1: the draft this CR first carried said 119 where the arithmetic gave
118 — 120 group lines plus a blank, less the three rows lifted out of* status / misc *— and M1-M3 have since
grown the group to 130 lines, which puts the shift at 128. Every number in this paragraph, and both block
ranges, was recounted mechanically against the amended branch rather than adjusted by hand.)*

**Byte-identical to `contract/protocol.md:2854-2953` on `profiler-amendment` at `a4002fb`**, re-extracted
after M1-M3.

```markdown
### 11.16 — 2026-08-19: the profiler stops being a status field

**CR-26**, raised in `oracle-next/docs/2026-08-19-cr26-profiler.md` off the design in
`oracle-next/docs/2026-08-19-profiler-recon.md`, the controller ruling in
`oracle-next/docs/2026-08-19-ruling-profiler-recon.md`, and the consumer's own source transcribed in
`oracle-next/docs/2026-08-19-aeon-profiler-demand.md`. **This is an amendment to three rows this catalog
already had** — `emulator/set_profiler`, `emulator/get_profiler`, `emulator/get_profiler_frames` — and not
a new family. The three names survive unchanged, deliberately: they are already catalogued, the legacy MCP
already exposes them, and the consumer's tool already calls them, so renaming would buy tidiness and cost
the migration its drop-in property. What changes is everything under the names. The shape the rows
carried was the legacy C++ server's, warts included — `budgetPct` is a hardcoded NTSC constant written
into the contract, and `routines[]` and `interrupts{}` had no field-level specification at all — and no
profiler reply had ever been closed against a schema fragment, because there was none to close it against.

| Item | The defect | What this amendment changed |
|---|---|---|
| **CR-26 — the profiler rows** | Three rows catalogued in *status / misc* with summarised results and **no schema fragment**: the schema's own top-level description names `profiler` among *"the deferred families … completed as each is implemented, per §8 item 20"*, so a server advertising one of these methods would ship a result nobody had checked — and `UNCOVERED_METHODS` being pinned empty in the reference suite makes that a red test rather than a convention. The catalogued shape also reproduced three structural defects of the instrument it was transcribed from, each anchored in the recon: **(1)** `interrupts` keyed on the handler's entry PC tested against vector-table constants (`oracle/linux-port/gui/ControlSocket.cpp:1995`, duplicated verbatim at `:2016`), so on the consumer's own ROM — `VBlank_Handler` at `$2310`, the HBlank trampoline at `$FFB452` — both fall into the `else` and `interrupts.hint` is HBlank **plus** VBlank while `interrupts.vint` is structurally `0`; **(2)** four truncating integer divisions by the *requested window* rather than by anything each row appeared in, with two fabricated values — `if (avgTotal == 0) avgTotal = 1;` (`:2032`) and `if (avgCalls < 1) avgCalls = 1;` (`:2043`) — and no error bound reported anywhere; **(3)** a cycle base that **excludes bus and DMA stall time** by construction, because `_currentCycle` advances only by the static per-opcode table while wait states go into a separate real-time accumulator (`oracle/Devices/M68000/M68000.cpp:1029-1031`). | The three rows are **re-specified and moved to their own §6 group**, with a normative blockquote carrying ten behaviours: exact per-invocation attribution with **`cycles` inclusive and `cyclesSelf` additive**; **`stallCycles` as a subset of `cycles` keyed identically to it**, with the three stall conditions enumerated so a fourth is an amendment and not a drift; division **inside the server** over a sample **delimited by frame boundaries at both ends**, so a partial first frame is inexpressible; `perFrameExact` as a typed key with nothing floored up and no error for an empty sample; `frameCount` **counted**, never derived from frame indices; `interrupts` keyed by **cause** with the bucket opening at the acknowledge cycle, closing at the matching `RTE`, nesting by depth and never double-counted, and `calls` meaning *taken*; a canonical 24-bit `addr` with a bare `name` and a numeric `disp`; `budgetPct` **derived from `timingBasis`** or omitted for `budgetPctOmitted` when a mid-sample basis change makes it ambiguous; synchronous arm/disarm/read that no server may refuse with `-32005` for the machine's run state; and `routines` / opt-in `perFrame` as §2.4 policy-bounded containers whose bounds are **refused, never clipped**. Schema gains the family's **first three fragments — 33 → 36** — plus two `initialize.limits` keys (`maxProfilerRoutines`, `maxProfilerFrames`) on the `maxWriteLen`/`maxHashLen` precedent, and the top-level `description`'s count is recounted again. |

**★ Why rows existed with no fragment, and why that is the reason this is contract-first.** The rows were
catalogued from the legacy socket's op list, as a record of a surface that existed somewhere. Nothing was
wrong with recording them; what was missing is that a summarised result column is not a wire shape. §8 item
20 closes every result against its fragment **at test time**, and the reference server pins its
`UNCOVERED_METHODS` list empty, so the first attempt to advertise `emulator/get_profiler_frames` turns that
test red until a fragment exists. That is a mechanism forcing the contract to lead, not a convention asking
it to — and it is why this amendment lands before a line of the handler is written.

**★ The stall correction is the one non-additive change, and it is reported loudly rather than suppressed.**
Every other item here is additive: a consumer reading `cycles` and `calls` off a row keeps reading the same
two numbers with the same meanings. `stallCycles` is not additive in its *effect*, because it exists to
name a difference that is already there — a reference figure computed from a stall-free clock and a figure
computed from a clock that includes the stalls are not the same number for a routine that touches the VDP.
Suppressing the difference to preserve parity would mean reporting a number this bus knows to be short. The
amendment's answer is to make it **visible and subtractable**: `cycles - stallCycles` recovers the
ideal-cycle figure the older instrument reported, which is exactly why the field is pinned to the same
keying and the same division as its cycle partner. The demand side confirmed the form and asked for both
pins in the schema, and it corroborated the finding from its own side within the day — their in-flight
Phase-0 measurement corrected its DMA-stall task **mid-session** to record the old instrument's blindness
as the finding instead of shipping a confident undercount, and every row of the parity corpus will now
carry an "ideal-cycle by construction" caveat of its own.

**★ Three questions went to the demand side before this was written, and all three came back.** They were
sent the day the design was adopted, and the shape here is the shape they answered against rather than a
guess this catalog would have had to revisit. *Which cycle field their fit wants:* inclusive — every reader
they have consumes rows inclusively, so `cycles` is the migration-parity field and `cyclesSelf` is welcome
but changes nothing today. *What form the stall subtraction should take:* `cycles - stallCycles` against
constants derived from engine source, with two pins requested explicitly and both taken — `stallCycles`
keyed **identically** to `cycles` on the same row, and a **one-line normative definition of what counts as
a stall**, so that a future stall source is a schema and amendment event rather than a silent semantic
drift under green gates. *Whether they want per-frame rows:* not yet — recorded for later lag and famine
analysis — which is exactly the opt-in default-off shape ruled, unchanged by the answer. Their own gates
have matched the older instrument exactly **because** its clock is ideal-cycle by construction; the finding
explains their instrument's behaviour rather than merely contradicting it, which is what gives the
correction its provenance.

**★ What does not change.** The three method names. The meaning of `cycles` — the consumer confirmed on
2026-08-19 that every reader they have consumes rows inclusively, so inclusive is the migration-parity
field and `cyclesSelf` is additive information nobody has to adopt. `initialize`'s `profiler` capability
key, which is already schema-typed and merely flips to `true` when a server implements this. The legacy
MCP's three tool tuples, which keep their names and their `{enabled}` / `{}` / `{frames, top}` parameter
shapes and go on working against the legacy server exactly as before — that server does not become newly
non-conformant here, having never carried the §2.2 stamp in the first place, and the descriptions on those
tuples want amending only because they already overstate what the legacy instrument delivers. And no other
row, group or `$defs` entry in this document is touched.

**★ What this amendment does not carry.** Four items are registered rather than folded in.
**`maxContiguousStallCycles`** — the consumer's Task 5 row, which *no* profiler provides today and which
this shape makes cheap once the stall channel exists — is deliberately later, so that v1 ships the
subtraction rather than the second statistic. **Per-scanline cost attribution** (`F-PROF-SCANLINE`) is the
highest-value follow-on and gets its own CR: folding it here would smuggle a second instrument under one
amendment. **Positional (symbol-range) attribution** (`F-PROF-POSITIONAL`) stays a second lens, valuable
mostly because diffing it against the call-graph rows localises exactly what a jump-table dispatch
mis-attributes. **Per-row exact totals** (`F-PROF-TOTALS`) — every row carrying its undivided sample sum
beside its per-frame figure — is not shipped because `perFrameExact: true` already makes the totals
recoverable by multiplication and the false case is reported; the result-level `sampleCycles` covers the
whole-machine sum. An event tap for a flame view is **declined**, visibly: it is the legacy ring buffer
with its overflow class, and nothing in the demand asks for it.

*Adoption condition, per §11.6 / §11.8 / §11.10 / §11.11 / §11.14 / §11.15, in CR-24's two-part structure —
suite gates executable in the reference repo, plus a demand-side acceptance protocol:* registered when
**(1)** a conformant reply passes each of the three fragments **closed**, happy path plus one refusal per
catalogued bound (`top` above the advertised cap → `-32602`; `frames` with no per-frame ring armed →
`-32005` with `data.reason = "perFrameNotArmed"`); **(2)** **division exactness** holds on a deterministic
fixture — a sample whose frames are identical reports `perFrameExact: true` with
`sampleCycles == frameCount × totalCycles` exactly, and a fixture built so the division cannot be exact
reports `perFrameExact: false` with no value floored up, no substituted denominator and no error;
**(3)** **`calls` is exact** against a count the fixture *builder* chose rather than against any measured
number; **(4)** **cause-keying is proven by a fixture that takes both interrupts** — HBlank-only puts zero
cycles in `vint`, VBlank-only puts zero in `hint`, and a fixture taking both attributes each to its own
bucket with neither carrying the other's cycles, which is the defect above made into a test; **(5)** the
**disabled path is byte-identical** — an untapped and a tapped run agree on `export_state_hash` *and* on
the whole machine struct, with a liveness control proving the instrument observed something, since without
that clause an instrument that records nothing passes; and **(6)** three boots of the same ROM produce
**byte-identical** profiler replies, spread exactly `0`. Clauses 1–6 are executable in the reference repo.
The demand-side protocol is the A/B against the consumer's Phase-0 parity corpus (**merge SHA pending on
their signal — not invented here**), read under four rules: `calls` matches **exactly**; `cycles` matches
**exactly on stall-free paths**; a stall-touching row reconciles through `stallCycles` or the arc stops
rather than widening a bound; and the interrupt buckets **must legitimately disagree** with the older
summed counter by the falsifiable equation *their* `hint` ≈ *our* `hint + vint`, a prediction that fails
loudly rather than a tolerance that absorbs anything. A spread is never a tolerance: theirs is the old
instrument's noise floor, ours is required to be `0`.
```

---

## 5. The schema — three fragments, two limits keys, one recount

**Counts, parsed rather than transcribed.** Before this CR the source
`contract/schema/bus-protocol.schema.json` `methods` object held **34 top-level keys = 33 fragments + one
`$comment`**; after it holds **37 = 36 + `$comment`**. The three added are `emulator/set_profiler`,
`emulator/get_profiler` and `emulator/get_profiler_frames`. Both numbers come from
`json.load(...)` over the two revisions — the same accounting CR-25 used
(`docs/2026-08-19-cr25-subline-convention.md` §5 item 2), and the reason it is stated as a parse and not as
a number: a transcribed count is a third answer that has to be kept equal to two others by hand.

**What the fragments carry that the prose cannot.** Five rules are mechanical here rather than advisory:

- `name` is `$ref: $defs/symbolName`, whose pattern **forbids a trailing `+$hex`** — so the house's
  symbol/displacement split (§4) is structural on these rows and not a discipline. `disp` is a separate
  integer, and `dependentRequired` makes the two travel **in both directions**: `name` without `disp` is
  refused, and so is `disp` without `name`.
- `budgetPct` and `budgetPctOmitted` are an **exclusive pair**, enforced by `anyOf` + `not` in the result's
  `allOf`: exactly one is present, never both and never neither.
- `routines` and `perFrame` are `$defs/boundedList` containers, so `total` / `returned` / `truncated` are
  required and `truncated` is required **even when false** (§2.4 clause (a)). Neither carries a cursor,
  because neither method accepts one (clause (b)).
- Row and bucket objects are `additionalProperties: false`, so a stray key — the legacy `pct`, say — is a
  refusal rather than a surprise.
- `caveat` is **declared** on `get_profiler_frames` though the §6 row does not enumerate it, per §2.4 rule 4
  and the `emulator/screenshot` / `emulator/scanlines` precedent.

**`initialize.limits` gains `maxProfilerRoutines` and `maxProfilerFrames`**, both REQUIRED when
`emulator/get_profiler_frames` is advertised — exactly the shape CR-23 used for `maxWriteLen` and
`maxHashLen` (`protocol.md:2477` pre-amendment — **not** `:2593`, the anchor an earlier draft of this CR
carried twice, which is CR-21's row and merely contains the string `limits.maxWriteLen`; on the amendment
branch as it now stands the CR-23 row sits at `:2606`, and the pre-amendment anchor is the stable one to
cite because every other `protocol.md` anchor here is pre-amendment). They are advertised because they are
**policy** bounds: the server picks
them, `top` and `frames` are client hints against them, and a client that must be refused to learn a ceiling
learns it too late (§2.1's own reasoning). This is the one place CR-26 touches a fragment that already
existed, and it changes no existing key.

**Executed, not asserted.** The amended document passes `Draft202012Validator.check_schema`, and **20**
hand-built messages were validated against the new fragments, all 20 landing as intended:

| # | Case | Expected |
|---|---|---|
| 1-5 | `set_profiler` params `{enabled}`; its params `{}`; its result; its result missing `perFrame`; `get_profiler`'s result | accept, **refuse**, accept, **refuse**, accept |
| 6 | `get_profiler_frames` result, happy path (one row with `name`+`disp`, both buckets, `budgetPct`, a truncated `routines` container) | accept |
| 7-8 | a row with `name` and no `disp`; a row whose `name` is `"Foo+$1A"` | **refuse**, **refuse** |
| 9-11 | both budget keys present; neither present; `budgetPctOmitted` alone | **refuse**, **refuse**, accept |
| 12-13 | a stray `pct` on a row; a `routines` container missing `truncated` | **refuse**, **refuse** |
| 14-16 | a `perFrame` container; `interrupts` missing `vint`; the empty sample (`frameCount: 0`, empty lists) | accept, **refuse**, accept |
| 17-18 | params `{top: 200, frames: 30}`; params `{top: 0}` | accept, **refuse** |
| 19-20 | §8 item 20's test-time closure (`unevaluatedProperties: false` composed over the published fragment): the happy path; the happy path plus one unknown top-level key | accept, **refuse** |

No `cargo` was run and no emulator tooling was touched — the only thing executed was JSON.

**Re-validated after M1-M3.** Those MUSTs changed prose inside five `description` / `$comment` strings (the
two `-32005` scopings, the two divisor definitions, the bucket's matching-`RTE` edges) and no keyword, no
key and no shape. `check_schema` passes again, the fragment count is unmoved at **36**, and the harness was
re-run at **21 for 21** — the twenty classes above plus the opposite `dependentRequired` direction
(`disp` without `name`, refused), which the adjudicator exercised among its own eleven adversarial cases and
which is worth carrying here rather than leaving to a ruling nobody re-runs.

---

## 6. Pins and decisions

Items 1-8 are carried from the adopted design and the ruling. Items 9-12 are places where the recon left two
readings and a contract cannot; each states the alternative it did not take.

1. **Exact per-invocation attribution; sampling refused.** The consumer's gate compares with `==`
   (`aeon/tools/effects_gates.py:754-756`), so a sampled figure could not be gated at all. Ruling: adopted
   as recommended.
2. **`cycles` inclusive, `cyclesSelf` additive.** Inclusive is the floor's meaning in practice, and the
   demand side confirmed on 2026-08-19 that every reader they have consumes rows inclusively. Both ship
   (ruling Q3).
3. **`stallCycles` ships in v1**, not in a later slice — the ruling promoted it. It is a subset of `cycles`
   on the same row, keyed identically, so `cycles - stallCycles` recovers the ideal-cycle figure the older
   instrument reports.
4. **The stall definition is enumerated, not described.** Written from what this server's bus actually
   attributes, read today: a data-port **write** held off while the write FIFO is full
   (`crates/oracle-core/src/bus.rs:977-988`), a data-port **read** waiting for that FIFO to drain
   (`:956-971`), and the whole bus-hold window of a **68k→VDP DMA** billed to the instruction that armed it
   (`:1019-1047`, returned as CPU wait cycles at `:1047`). A VRAM **fill** and a VRAM **copy** contribute
   **nothing** — the 68000 keeps running through both, stated in the code at `:1003` and `:1008` — and every
   other arm of `read16` / `write16` / `read8` / `write8` returns a wait of `0` (`:1094`, `:1121`, `:1155`,
   `:1172`), including the Z80 window. The demand side asked for this pin explicitly so that a future stall
   source is a visible schema and amendment event rather than a silent widening under green gates.
5. **Interrupt buckets keyed by CAUSE, with the nesting rule pinned** (ruling Q6): the bucket opens at the
   acknowledge cycle, closes at the matching `RTE`, nests by depth, and a nested HInt inside a VInt
   attributes to `hint` while the suspended `vint` accrues nothing. `calls` counts interrupts **taken**, not
   raised. Cause-keying costs this server zero core change, because the fc = 7 acknowledge is already on the
   sink stream (`crates/oracle-core/src/bus.rs:1063`; corroborated in
   `crates/oracle-core/src/watchpoints.rs:439`).
6. **`name` bare + `disp` numeric**, per ruling Q5, which sidesteps the question by house precedent rather
   than deciding it: §4's rule that *"a displacement is never inside a name string"* and the
   `$defs/symbolName` retype §11.6 applied to `read_memory.symbol` (`protocol.md:2162-2165`). Since no
   `name+$hex` composite is ever emitted, `$defs/symbolName` conformance is structural here, and
   `F-SYMBOLNAME-DISP` — the recon's flagged divergence between `symbol_at`'s rendering and that pattern —
   stays TAGged without blocking this arc.
7. **`budgetPct` derived from `timingBasis`** (ruling Q2), never a hardcoded constant.
   `crates/oracle-core/src/system.rs:61-80` is the `TimingBasis` struct and its NTSC constant — derived from
   the scheduler's own constants, so the reported basis and the frame arithmetic cannot disagree — and
   `:548` is the accessor whose *value* goes live when region becomes machine state without its signature
   changing. Q2's second half is the `budgetPctOmitted` key: *"refuse the ambiguity explicitly in the reply
   rather than averaging over it"*, which §2.4 rule 3 then requires be a typed key and not a caveat.
8. **`perFrame[]` ships opt-in and bounded** (ruling Q1), with D13-style cap refusal.
9. **Rows carry the per-frame figures; the result carries the undivided total.** *(Resolution.)* The
   conflict is between the recon and the floor, not inside the recon: recon §F.2 and warts W13/W14 put
   **exact totals on the rows** — *"Rows carry exact integer totals over the sample"*, *"totals are
   totals"* — while the legacy row's `cycles` is a **per-frame** figure (`ControlSocket.cpp:2041`), read
   off the row and gated with `==` (demand doc §1.2-§1.5). Moving the row to totals would therefore
   multiply the consumer's expectations by the frame count silently. *(An earlier draft of this CR said
   recon §G.2 sketched per-frame rows and contradicted §F.2. It does not: §G.2's example is coherent only
   as totals-on-rows — its `vint` bucket shows `calls: 31` over a 31-frame sample, and its row's `cycles`
   exceeds the same example's `cyclesPerFrame`. Corrected per ruling M5; the resolution below is
   unchanged and was affirmed.)* Rows therefore keep the floor's per-frame meaning;
   `sampleCycles` carries the undivided whole-sample total; and per-row exact totals — the recon's argument
   that they let a client compute a marginal cost from exact integers — are registered as **F-PROF-TOTALS**
   rather than shipped, because `perFrameExact: true` already makes each row's total recoverable by
   multiplication and the false case is reported.
10. **`routines` takes §2.4's nested container spelling.** *(Resolution.)* Recon §G.4 rules the container
    form and recon §G.2's example sketches the flat one. §2.4 decides: the flat spelling belongs to a list
    that **is** the whole result (`checkpoint_list`), and this one is a field of a larger result, exactly
    like `lookup_symbol.otherMatches`. The cost is one line on the consumer's side —
    `prof["routines"]` becomes `prof["routines"]["items"]` — and it is named in the migration list below
    rather than discovered.
11. **`frames` is retargeted, and refused rather than ignored.** *(Resolution.)* Its legacy meaning is the
    divisor window, which is the defect itself (the divisor was the request, not the sample). Here the
    aggregate is always the whole sample, and `frames` bounds the per-frame list; with no ring armed it is
    **refused** with `-32005` (`data.reason = "perFrameNotArmed"`), because a parameter that silently cannot
    affect the answer is worse than one that is rejected — §5's rule against resolving a wrong-state case on
    the client's behalf, applied to a parameter.
12. **Arming is not refused on a running machine.** *(Resolution.)* Recon §F.3(1) offered *"refuses on a
    running machine **or** arms at the next frame boundary"*. Taking the second makes the first unnecessary:
    if the sample's edges are frame boundaries, a free-running arm is exactly as well defined as a paused
    one, and refusing a call the legacy accepted would break a consumer for nothing. Both reads are pure, on
    `emulator/scanlines`'s precedent.

**One place this CR is deliberately weaker than the recon, and it is the honest direction.** Recon §F.3
predicts that with a frame-aligned arm *"`frames: sample - 1` becomes `frames: sample`, and their sample
grows from 30 profiled frames to 31."* This CR does **not** pin that. `run_frames` advances against an
**anchored frame grid**, not from wherever the machine happens to sit: the target is
`frame_boundary_mclk + frames * MCLK_PER_FRAME` (`crates/oracle-core/src/system.rs:918`), and after a
mid-frame stop the anchor and the machine differ by construction (`:920-928`). The frame-boundary hook,
meanwhile, fires at line 224 (`system.rs:1176-1190`), which is not where that grid ticks. So whether a probe
that runs `S` frames sees `S` or `S − 1` **whole** frames depends on the phase between the two, and with a
grid-anchored target against a line-224 boundary the recon's "31" is doubtful rather than merely unpinned. What the amendment guarantees is the part that is structural: every counted
frame is whole, no runt is expressible, and `frameCount` is **reported** so the consumer reads its divisor
instead of computing it. The recon's prediction is recorded here as a prediction to be measured at
acceptance, not as a contract claim.

---

## 7. What does **not** change

1. **The three method names.** They are catalogued, the legacy MCP exposes them, and the consumer's tool
   calls them; renaming would buy tidiness and cost the migration its drop-in property (recon §G.2).
2. **The meaning of `cycles`** — inclusive, confirmed by the demand side on 2026-08-19 as what all their
   readers consume.
3. **The `profiler` capability key.** Already schema-typed (`bus-protocol.schema.json:200`) and already
   advertised `false` (`crates/oracle-aether/src/engine.rs:834`). A server flips it to `true` when it
   implements this; nothing in the key's type or meaning moves.
4. **The legacy MCP's three tool rows keep working.** The tuples at `oracle/linux-port/mcp/oracle_mcp.py:704`,
   `:719` and `:726` keep their names and their `{enabled}` / `{}` / `{frames, top}` parameter shapes, and
   they go on talking to the legacy server exactly as before. That server does not become newly
   non-conformant here — it has never carried the §2.2 stamp, which §8 already records as its standing state
   (`protocol.md:1587-1594`)
   — and the only thing wanting amendment on those tuples is prose that already overstates the instrument:
   *"cycle-accurate timestamps"* (`oracle_mcp.py:708`), of a clock that excludes stalls.
5. **No other row, group or `$defs` entry** in `protocol.md` is touched, and no existing schema fragment
   changes except `initialize`'s `limits`, which gains two optional keys and loses nothing.
6. **No currency in the reference repo moves as a consequence of this text.** CR-26 is contract and schema
   only; the arc's currency argument belongs to the implementation slices, where the recon's §H enumerates
   it as expected-zero by construction with one deliberate exception (the capability key) and one recorded
   break (the `crates/oracle-core/tests/` zero-file-diff record, ruled in Q7).

---

## 8. The adoption condition

In CR-24/§11.14's two-part structure — suite gates executable in the reference repo, plus a demand-side
acceptance protocol — and for the reason §11.14 states in the catalog's own voice: *"Registration gated on
an unexecutable clause is a condition that gets waived silently."* The entry's closing paragraph is the
normative text; this is the same six clauses with their fixtures named.

**Suite gates, executable here:**

1. **Fragment closure.** A conformant reply passes each of the three fragments **closed**
   (`unevaluatedProperties: false` at test time, §8 item 20), happy path plus one refusal per catalogued
   bound: `top` above `limits.maxProfilerRoutines` → `-32602`; `frames` with no ring armed → `-32005` with
   `data.reason = "perFrameNotArmed"`.
2. **Division exactness**, on a deterministic fixture — a `testrom`-built sample whose frames are identical
   reports `perFrameExact: true` with `sampleCycles == frameCount × totalCycles` **exactly**; and a fixture
   built so the division *cannot* be exact reports `perFrameExact: false`, with no value floored up, no
   substituted denominator and no error. The second half is the one that matters, because it is the clause
   the legacy behaviour would fail.
3. **`calls` exactness**, asserted against a count the **fixture builder** chose rather than against any
   measured number — the discipline the demand side's own derivation used when the hardware disagreed with
   it and they corrected the derivation instead of relaxing the check (demand doc §1.4).
4. **Cause-keying proven by a fixture that takes both interrupts.** HBlank-only puts zero cycles in `vint`;
   VBlank-only puts zero in `hint`; a fixture taking **both** attributes each to its own bucket with neither
   carrying the other's cycles. This is defect 1 turned into a test, and it is the single most important
   gate in the arc.
5. **The disabled path is byte-identical** — the currency gate. Copied from the four-assertion shape already
   in this tree (`frame_boundary_is_state_neutral`, `crates/oracle-core/tests/scanline_capture.rs:269`,
   body through `:301`): equal `export_state_hash`, equal **whole `System` struct**, a
   **liveness control** proving the instrument observed something, and a structural assertion on what it
   observed. Clause three is not optional — without it an instrument that records nothing passes.
6. **Three boots, byte-identical.** Spread exactly `0`, which is the demand side's own noise bar
   (*"every one matched its cost model to the cycle, 3 boots, spread 0."* —
   `aeon/docs/superpowers/plans/2026-08-18-scanline-p2-specialization-budget.md:56`, read today) expressed
   as our suite's gate. A spread is never a tolerance: theirs is the old instrument's noise floor, and ours is
   required to be zero.

**Demand-side acceptance protocol** (not a suite gate — the fixture, the ROM and the driver are theirs,
exactly as §11.14's verbatim A1/A2 sweep is): the A/B against their Phase-0 parity corpus, whose **merge SHA
is PENDING on their signal and is not invented here** (demand doc §8 holds the placeholder). Four rules,
from the ruling:

- **`calls` matches exactly.** No stall term, no truncation term, no tolerance column.
- **`cycles` matches exactly on stall-free paths** — the F0-F8 fixture set is the DMA-free control, so a
  delta there has no stall excuse.
- **A stall-touching row reconciles through `stallCycles`, or the arc stops.** It does not widen a bound.
- **The interrupt buckets must legitimately disagree**, by the falsifiable equation *their* `hint` ≈ *our*
  `hint + vint`, with their `vint` structurally `0` against our non-zero one. That is a prediction that can
  fail loudly, which is what makes it evidence that our split is the correct one.

**And the corpus now declares what its own cycles mean.** On receiving the stall finding, the demand side's
in-flight Phase-0 measurement corrected its DMA-stall task **mid-session**: that row now records the old
instrument's blindness as the finding — loud on unmeasurable, citing the recon — instead of shipping a
confident undercount, and every parity-corpus row will carry an *"ideal-cycle by construction"* caveat.
That simplifies the comparison above rather than complicating it: the corpus states its own basis, so a
delta on a stall-touching row starts from a documented difference instead of an argued one.

---

## 9. The three in-flight questions came back before this was written

The ruling's execution order had the demand-side shape-check running in parallel with this draft
(`docs/2026-08-19-ruling-profiler-recon.md:84-91`). All three answers arrived on 2026-08-19, **before**
adjudication, so this CR states them as answered rather than open. **None of them changes the proposed
shape.**

**Source, since CR-26's own adjudication recorded these as `COULD NOT VERIFY` — *"relayed, exist in no
committed artifact"* (`docs/2026-08-19-ruling-cr26.md:184-186`).** They are now a committed artifact:
`docs/2026-08-19-aeon-profiler-demand.md` **§10**, a dated addendum recording all three answers as relayed,
plus the Task-5 correction. The channel is named there and its limits are stated rather than implied — a
cross-session message from the Aeon overseer on 2026-08-19, relayed into this session by the controller and
controller-attested, transcribed rather than quoted from an `aeon` file, with an **aeon-side anchor PENDING**
on the same discipline demand doc §8 applies to the parity corpus. What is independently checkable is not
the relay but its effect: every pin below is present in the amendment text and in the fragments, where the
adjudicator verified it.

| Question | Their answer | Effect here |
|---|---|---|
| Which cycle field does the walker fit want? | **Inclusive.** Every consumer they have reads rows inclusively — their F-series takes the HBlank trampoline's row as the whole fire including callees, and their Task-4 marginal method prices callees inside the caller by construction. `cyclesSelf` is welcome and changes nothing today. | Shape unchanged. Ruling Q3's "asked of the demand side" is now answered, and §6's rationale cites the answer instead of an open question. |
| What form should the stall-gate subtraction take? | **`cycles - stallCycles` against `.emp`-derived constants**, with two pins requested explicitly: *(a)* `stallCycles` keyed **identically** to `cycles` on the same row — same routine keying, same divided-inside exactness, since a stall figure with different aggregation semantics would poison the subtraction; *(b)* a **normative one-line definition of what counts as a stall**, pinned so a future stall source is a visible schema/amendment event. | Both pins are in the normative text and in the fragment (§6 decision 4). |
| Do they want `perFrame[]`? | **Record-and-later** — not consumed now, wanted someday for lag and famine analysis. | Unchanged: opt-in, default off, bounded, exactly as ruled in Q1. |

---

## 10. Migration deltas the demand side must be told, not left to discover

Four, and all four are one-line changes on their side except the last, which is a semantic:

1. **The two `asyncio.sleep(0.4)` calls go away** (`aeon/tools/raster_cost_probe.py:531`, `:533`). Arm,
   disarm and read are synchronous with the command here, which the §6 text now says in as many words, so
   the race those sleeps paper over is structurally absent.
2. **The `frames` argument goes away entirely — not just the `- 1`** (`:535`, today
   `get_profiler_frames{"frames": sample - 1, "top": 200}`). The `- 1` was a hand-compensation for a
   partial first frame, and a runt is now inexpressible, so the divisor is read from `frameCount` instead
   of computed. But deleting only the `- 1` leaves `frames: sample`, and under this amendment a `frames`
   param with no per-frame ring armed is **refused with `-32005` (`perFrameNotArmed`)** — and they answered
   *record-and-later* on `perFrame`, so no ring will be armed. **The first migrated run would refuse.** The
   migrated call is `get_profiler_frames{"top": 200}`.
3. **The address reconcile goes away** (`:558-561` — a `lstrip("$")` and an `int(…, 16)` at `:558`, the
   two `& 0xFFFFFF` masks at `:561`; the demand doc transcribed the first as `:559` and this CR inherited
   it, corrected here per ruling S5). `addr` is canonical 24-bit, and rows now carry a resolved `name`, so matching
   `name == "HBlank_Vector_Slot"` works.
4. **`routines` is a container, not a bare array** (decision 10). `prof["routines"]` becomes
   `prof["routines"]["items"]`, and `total` / `returned` / `truncated` come with it, which is what tells
   them a `top` bound clipped the list rather than the sample being small.

---

## 11. Claims, anchors, and what could not be verified

**Verified firsthand for this CR** (read in the file named, today):

| Claim | Anchor |
|---|---|
| The three §6 rows read exactly as quoted, in *status / misc* | `empyrean/contract/protocol.md:1311-1313` (pre-amendment) |
| The rows have been catalogued since the spec's first draft | `git log -S "emulator/set_profiler" -- contract/protocol.md` → first hit `a25bac1`, the commit that introduced `protocol.md` |
| §8 already records the legacy C++ server as non-conformant (it stamps nothing) | `protocol.md:1587-1594` |
| *"Registration gated on an unexecutable clause is a condition that gets waived silently"* | `protocol.md:2581-2582` (§11.14) |
| The demand side's noise bar, and their statement that oracle-next has no profiler | `aeon/…/2026-08-18-scanline-p2-specialization-budget.md:56`, `:19-25`; `aeon/tools/effects_gates.py:56-66`, `:754-756` (aeon `18af84f3`) |
| §11.15 is the last entry; §11.16 appends at EOF | `protocol.md:2627`; `wc -l` = 2724 pre-amendment |
| The schema calls `profiler` a deferred family, in the sentence quoted | `contract/schema/bus-protocol.schema.json:5` |
| `methods` holds 33 fragments before / 36 after (34 / 37 keys with `$comment`) | both revisions parsed with `json.load` |
| `profiler` is a typed capability key, advertised `false` today | `bus-protocol.schema.json:200`; `crates/oracle-aether/src/engine.rs:834` |
| `UNCOVERED_METHODS` is pinned empty | `crates/oracle-aether/tests/schema_conformance.rs:142` |
| The interrupt conflation, both duplicated sites, and the `0x78` / `0x70078-0x7FFFF` test | `oracle/linux-port/gui/ControlSocket.cpp:1995`, `:2016` |
| The four truncating divisions and the two fabricated values | same file `:2031`, `:2032`, `:2041`, `:2042`, `:2043`, `:2070-2071` |
| The legacy row keys the wire actually carries (`name`, `addr` as `"$%06X"`, `cycles`, `pct`, `calls`) and the reply keys (`frame_count`, `total_cycles`, `budget_cycles`, `budget_pct`) | same file `:2045-2068`, `:2080-2090` |
| `budget_cycles` is the hardcoded `128000` | same file `:2033` |
| The reference's cycle base excludes `additionalTime` | `oracle/Devices/M68000/M68000.cpp:1029-1031` |
| The dead cycle binding in our run loop | `crates/oracle-core/src/system.rs:1035` |
| The fc = 7 acknowledge is already emitted to the sink | `crates/oracle-core/src/bus.rs:1063` |
| The three stall sources, the two zero-wait DMA modes, and the four zero-wait returns | `crates/oracle-core/src/bus.rs:956-971`, `:977-988`, `:996-1012`, `:1003`, `:1008`, `:1019-1047`, `:1094`, `:1121`, `:1155`, `:1172` |
| `TimingBasis` (struct + NTSC constant) and its accessor | `crates/oracle-core/src/system.rs:61-80`, `:548` |
| `run_frames` targets `frame_boundary_mclk + frames * MCLK_PER_FRAME`; the boundary hook fires at line 224 | `crates/oracle-core/src/system.rs:918`, `:1176-1190` |
| The MCP's three profiler tuples, and the *"cycle-accurate timestamps"* wording | `oracle/linux-port/mcp/oracle_mcp.py:704`, `:719`, `:726`; the phrase at `:708` |
| The neutrality-gate shape this CR's clause 5 copies | `frame_boundary_is_state_neutral`, `crates/oracle-core/tests/scanline_capture.rs:269-301` |
| No shadow stack exists in this tree today | `grep -rlE "step_out\|step_over\|call_stack\|callStack" crates/` → 0 files, re-run today |
| The acknowledge addresses are already named in the recorder's own prose | `crates/oracle-core/src/watchpoints.rs:439` |
| §11.6's `$defs/symbolName` retype, the precedent ruling Q5 leans on | `protocol.md:2162-2165` |
| CR-23's `maxWriteLen` / `maxHashLen` limits precedent | `protocol.md:2477` (pre-amendment; `:2606` on the amendment branch) |

**One anchor drift, recorded rather than repeated.** Recon §I item 8 cites `bus.rs:1047` for
`dma_complete(record, …, cost)`. At the commit read today that call is at `bus.rs:1046`, and `:1047` is the
`cost.div_ceil(MCLK_PER_CPU_CYCLE)` that returns the wait. This CR anchors the DMA wait at `:1047` and the
completion at `:1046`. Nothing else in the recon's anchors failed re-verification.

**Claims this CR does not make.**

- It does not claim the reference server implements any of this. It does not: no line of `crates/` is
  touched by this arc, and the handler is slices 1-6 of the recon's plan. The contract text is drafted to
  merge in the same window as the implementation, on §11.15's sequencing precedent, so `main` is never wrong
  in either direction.
- It does not claim a number for what the consumer's 31-frame sample becomes. See §6's closing note: the
  recon predicts 31, this CR reports `frameCount` instead of pinning it, and the measurement belongs to
  acceptance.
- It does not claim any corpus figure. The parity corpus is in flight on their side and its **merge SHA is
  PENDING**; no anchor for it is invented here.
- It does not claim `perFrame` will be consumed. The demand side said record-and-later, and the key ships
  opt-in and default-off on that basis.

**Nothing is BLOCKED.** One item is recorded as unexplained rather than guessed at, and it is the recon's,
not this CR's: the legacy classifier's `0x70078…0x7FFFF` window has no explanation in code, comment or doc
(recon §A.4). CR-26 reproduces no heuristic, so it needs no explanation of that one to be complete.

---

## 12. Verification note

**Docs and schema only.** In `oracle-next`, two files under `docs/` — this one and §10 of the demand doc —
and nothing under `crates/`, so per the standing rule no `cargo test --workspace` run was required and
**none is claimed**. In `empyrean`, two files (`contract/protocol.md`,
`contract/schema/bus-protocol.schema.json`) on branch `profiler-amendment`, cut from `main` at `d72513c`,
across two commits — the draft, then M1-M3 — **not merged**.

**One correction that lives in a commit message, recorded here because a message cannot be amended without
rewriting an adjudicated commit** (ruling S2): the empyrean draft commit's body says the blockquote carries
*"nine behaviours"*. The text it committed says **ten**, and ten bullets is what it has — the message is
wrong, not the contract. The M1-M3 commit's body records the same correction, so the wrong count is not the
last word in `git log`. The vendored
copy at `crates/oracle-aether/tests/contract/bus-protocol.schema.json` is deliberately **untouched**: it
re-vendors from the empyrean source at the arc's merge window, and editing it now would fork the vendored
artifact from its source (§11.15's delta recorded the same reasoning).

**No emulator MCP tooling was used, at any point, for any purpose.** No live boot was required and none was
performed. The only thing executed was JSON: the amended schema parsed, its `methods` object counted, and 20
messages validated against the new fragments (§5).
