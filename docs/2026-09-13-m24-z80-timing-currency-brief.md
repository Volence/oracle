# M24-NEEDS-A-CURRENCY: the dispatched brief (design doc first)

> **Dispatch artifact, 2026-09-13 (overseer, session 15).** Order of work is the hub's ruling under the owner's
> goodnight delegation (empyrean `origin/main` `3bb082d`, `docs/OVERSEER.md` rulings (2) and (2a)): H22 (landed
> `1404a07`) → **M24** → F-MACHINEREPLACED-EVENT-RACE → M1-FILL-OVER-TIME. Shaped together with
> F-Z80-ACCESSES-UNWATCHED per this repo's `docs/OVERSEER.md` (registration of 2026-09-13).
> Premises measured at oracle `5ee7d6a`. No file under `crates/`, `tools/`, `Cargo.toml` or `Cargo.lock` changed
> between `c86e1c3` and `5ee7d6a` (`git diff --stat` empty), so the last full baseline stands: **release profile,
> `tools/land.sh --no-push` at `c86e1c3`: 89/89 legs, 2898 passed / 0 failed / 3 ignored.**
> The text below the rule is the agent prompt, passed with `isolation: worktree`.

---

You are a design agent in the **oracle** repo (a from-scratch Rust Mega Drive emulator and its Aether debug bus). Parcel: **M24-NEEDS-A-CURRENCY**, size M, one branch. **The deliverable is a design document, not a fix.** You may write measurement spikes to answer its questions; they are labelled `spike:` and removed from the tip before you report (the H22 pattern: kept in history, gone from the tip, so the merge is docs-only).

**First, tell me where this brief is wrong.** Every mechanism below is a hypothesis; your command output outranks it. The last five agents in this repo each corrected their brief on material points, measured. Do that here too.

## Why this parcel exists

1. **Lens finding M24** (`docs/superpowers/notes/2026-09-06-oracle-lens-sweep.md`, the `M24` bullet): *"`EI` has no one-instruction delay and `/INT` is held for the whole vblank rather than one line — both stated in comments, both invisible to the SST gate by construction."* At `5ee7d6a`: the `0xFB` arm in `crates/oracle-core/src/z80/mod.rs` sets `iff1`/`iff2` immediately (its comment: *"EI's one-instruction delay is unobserved by the SST-z80 gate"*); in `crates/oracle-core/src/system.rs`, `EventKind::VInt` calls `self.z80.set_int_line(true)` and the line is dropped only `if line == 0` on the next frame (the ZC14 comments). The triage row (`docs/2026-09-11-lens-triage.md`, row M24) cites older line numbers; find the sites by symbol.
2. **Nothing we record would show an M24 fix working or regressing.** From the M21 landing (lane log `2026-09-12T11:47:13Z`, merge `d86fe28`): *"a +27,360 mclk-per-grant mutation also moved none, so no Z80-timing currency exists (booked as the prerequisite for M24)."* "Currency" here means the frozen, committed artifacts the suite compares against (state hashes, `export_state` goldens, visual baselines, the aeon replay fixtures, scanline goldens, vendored-ROM scorecards). A fix nobody's check can see is a fix nothing protects.
3. **F-Z80-ACCESSES-UNWATCHED** (inbound from aeon, registered in `docs/OVERSEER.md` 2026-09-13): `Z80Bus` in `crates/oracle-core/src/z80/bus.rs` delivers exactly one kind of access to the event sink, the FM/PSG register-write tap (`on_event_at`). `read_window` (a `&self` method) returns cartridge/work-RAM bytes with no sink call; Z80 RAM accesses, window writes into 68k work RAM and the VDP mirror are undelivered too. Meanwhile `crates/oracle-core/src/watchpoints.rs`'s module header says *"the real 68000/Z80 bus adapters deliver every access"*, and `Watchpoints::caveats` does not mention the gap. Aeon measured the consequence: read watches on one sound effect's FM patch bytes in ROM, effect fired, **0 hits / 0 dropped**. So a zero on a Z80-read subject is the instrument being absent, reported as a negative finding.
4. **Hypothesis (the previous session's, unmeasured): a timestamped stream of every Z80 bus access might be the Z80-timing currency M24 lacks.** One build would then close aeon's gap and give M24 its check. Test this; do not assume it.

## What the design doc must answer

Write `docs/2026-09-13-z80-timing-currency-design.md`. Plain summary first (a reader who stops after it knows the recommendation and its cost), then the sections below.

1. **What quantity is a "Z80-timing currency"?** State the property precisely: a committed artifact that **moves** when *when the Z80 executes, is granted the bus, or accepts an interrupt* changes, and **does not move** under timing-neutral changes. Candidates to weigh (not exhaustive, and not ranked): (a) a digest of a timestamped Z80 access stream over a corpus run; (b) Z80 state sampled at fixed 68000 instants and folded into an existing currency (note: an `export_state`/`state_hash` layout change needs a version bump and invalidates saves, per `docs/export-state-v1.md`; say whether your option touches either); (c) the FM/PSG write tap that already exists and is already timestamped: **why did it not see M21's mutation?** Is there any committed golden built from it (`crates/oracle-core/src/vgm.rs`, `VgmLogger`)? Measure, do not reason; (d) a log of the instants the Z80 accepts `/INT`. **Better-approach pass, required:** pick one, say why, and say what the losers would have caught that yours does not.
2. **Sensitivity, measured, with a positive control.** Using spikes on the committed corpus: does your chosen currency move under (i) the M21 mutation (+27,360 mclk per grant, or an equivalent you derive and state), (ii) a one-instruction EI delay, (iii) `/INT` dropped after one line instead of at line 0? And does it stay still under a timing-neutral change you choose? A currency that moves under nothing, or under everything, fails. Report which tests/fixtures carry the signal. The M21 landing said 25 tests take the gated-on Z80 arm (aeon replay fixtures, vendored-ROM scorecards); **re-measure that, and say which of them actually run a sound driver with interrupts enabled** (a driver that never runs `EI` cannot see either M24 half).
3. **The Z80 access stream as `BusEvent`s: enumerate every consumer before proposing it.** Emitting Z80 accesses changes the stream every sink sees. Enumerate by what **implements `BusEventSink`** and by what **constructs or forwards a `BusEvent`** (at `5ee7d6a` the implementors include `Watchpoints`, `Profiler`, `VgmLogger`, `AudioSink`, `ScanlineCapture`, aether's `BreakStop`/`LineStop`/`StepStop`, the `Fanout`/`Observe`/`StopWhen` adapters and several test sinks; that list is a floor, find the rest). Per consumer, say whether Z80 events would break an assumption it makes. Hypotheses to test, not conclusions: watchpoints latch a 68000 PC at each step boundary, so a Z80 access would be attributed to whatever 68000 instruction is current; the profiler may count Z80 accesses as 68000 work; `fc = 0` already means "DMA/other non-CPU master" (`bus.rs`, `BusEvent`), so reusing it for the Z80 may conflate two masters; an address breakpoint may start firing on Z80 reads. Also decide **which address** a window access carries (Z80-space `$8000-$FFFF`, or the 68000-space address it resolves to). **If any of this reaches the Aether wire** (watch hits report a master or function code to clients), say so plainly: a wire-visible change is a contract change request, which is a ruling for me, not a design choice for you.
4. **The hot path.** The null sink (`()`) is monomorphised and must stay free. Say how your design keeps the no-instrumentation path unchanged (textually, if you can, as `watchpoints.rs` claims for itself), and measure it if you touch the emission sites.
5. **The cheap half of F-Z80-ACCESSES-UNWATCHED.** A caveat from `Watchpoints::caveats` for Z80-reachable subjects, plus making the module header true. Say whether it should land first and alone (it moves no bytes), and what exactly it says. Aeon's measurement (read watches on ROM bytes the sound driver reads, 0 hits) is the acceptance case for the full half: name the test that would reproduce it red today.
6. **What M24 then needs, from documentation only.** For each M24 half, the hardware behaviour, quoted: Zilog's Z80 user manual (UM0080) on `EI` and interrupt acceptance; Sega's hardware documentation, Plutiedev and SpritesMind prose on how long the Genesis holds the Z80 `/INT` asserted. **Clean-room: no emulator source in any form** (BlastEm, Genesis Plus GX, Ares, jgenesis, MAME, and this workspace's `oracle-old` C++ core). Where the documentation disagrees or is silent, say so; do not pick a number.
7. **Staging.** The parcels in order, what each moves (bytes, frozen currency, wire), which need a ruling from me, and how the currency verifies the M24 fix once it exists. Include how the new golden is regenerated: this repo's rule is that goldens never regenerate silently and every mover carries a named, measured mechanism in a `cause:` comment.

## 0. Base check, branch, vendor, scratch

- `cd` to your worktree root (absolute path) before any git operation.
- Base check: `git merge-base --is-ancestor 5ee7d6a HEAD && echo OK` must print OK and `docs/2026-09-13-m24-z80-timing-currency-brief.md` must exist. If it is missing, run `git fetch origin && git merge --ff-only origin/main` once and re-check; still missing means BLOCKED, stop.
- `git switch -c parcel/m24-z80-currency-design`. Record your tip SHA as you go. **The controller (me) merges your branch into main and then deletes the branch and your worktree**; a missing branch afterwards is expected. Check `git merge-base --is-ancestor <tip> main` first, and confirm by content if that says no (a squash merge rewrites commits).
- `ln -s /home/volence/sonic_hacks/oracle/vendor vendor` (gitignored), then check `vendor/` resolves (17 TestRoms entries). Without it 8 `save_state` rows fail and the 68000 SST sweep silently skips. `ls` is aliased to eza, so use `command ls`.
- Scratch: a run-unique dir you create, `/tmp/claude-1000/m24-currency-$(date +%s)/`. Nothing of yours goes in any shared scratch path.

## Hard boundaries

- **Files in scope:** the new design doc; spike code anywhere under `crates/` or `examples/`, committed as `spike:` and **removed from the tip in a final `spike:` commit** so that `git diff --stat 5ee7d6a...<tip>` names docs only. Anything else: say why in the report before touching it.
- **Never touch** `docs/lane-status.json`, `docs/lane-log.jsonl`, `docs/decisions.jsonl`, `docs/lens-findings.jsonl` or any `docs/OVERSEER*.md`, nor the vendored contract/schema. Queue outcomes go in your report; I transcribe them.
- **No fix to M24 or to F-Z80-ACCESSES-UNWATCHED on this branch**, not even the cheap caveat. Design them.
- **No emulator, ever.** Never call any `mcp__oracle__*` tool; never launch a window on any display. TAG anything that needs a live run or a listen (for example "does sound change on `aeon/s4.debug.bin`").
- **BLOCKED is always available**: stop that item, record why, finish the rest. Never degrade a design to reach a tidy answer.
- Do not merge, push, or open a PR.

## Every measurement and check follows these rules

(a) **A spike's mutation is shown applied** before its run (quote the mutated line from disk, or `git diff --stat`). (b) Restore from a **committed** baseline, never `git checkout --` over uncommitted work. (c) **Applied and unmoved is a finding about the instrument first**: cargo fingerprints by mtime, so watch for "Compiling"; an instrument that cannot move is not evidence of stillness. (d) Expected values **derived** from constants (`MCLK_PER_Z80_CYCLE`, `MCLK_PER_LINE`, instruction timings), never read back from the implementation; loud on unmeasurable, never a silent 0. (e) If you tighten a method midway, re-establish earlier claims under it and say which. **For every "it moved" or "it did not move", ask what else would produce the same result** (a fixture whose driver never runs `EI`, a corpus that never grants the bus, a digest that excludes the field you changed) and say how you ruled it out.

## Running and verifying

- **One cargo invocation at a time in your worktree**, and no other agent is running cargo in this repo right now. Targeted runs while you work. Every timing figure ships with the machine's load average (`cat /proc/loadavg`) and wall-clock uptime, since peers share this box.
- A known intermittent failure exists and is not yours: `crates/oracle-aether/tests/machine_replaced.rs:291`, *"expected exactly one emulator/machineReplaced in the stream, got 0"* (booked as F-MACHINEREPLACED-EVENT-RACE). If it appears, report it by name and do not chase it. Any OTHER failure that only appears under load is a defect with a narrow window, not a flake: report it by name.
- Final check, after your last commit (the spike-removal one), clean tree: **`./tools/land.sh --no-push`** from your worktree root. **Detach it** per its header (a run-unique wrapper in YOUR scratch dir writing `LAND-EXIT=$? AT $(date -Is)` to your log; `setsid nohup … &`), **then poll your own log in the FOREGROUND until LAND-EXIT appears**: `for i in $(seq 1 7); do grep -q '^LAND-EXIT=' "$LOG" && break; sleep 15; done`, repeated. **Do NOT end your turn while it runs, and never wait on a background-task notification.** Never foreground land.sh. Do not commit while it runs. The script is `tools/land.sh`; read the `LAND-EXIT` token in the log, never a notification's exit code.
- Baseline (release profile, `tools/land.sh --no-push`): **89/89 legs, 2898 passed / 0 failed / 3 ignored.** With a docs-only net diff you should reproduce it exactly; predict before the run and report prediction vs result.
- Report aggregate totals with the profile, never a tail; grep failures with `^test [^ ]+ \.\.\. FAILED`; capture exit codes outside pipes; match processes by exact name (`pgrep -x cargo`).
- Commit as each piece lands, **findings in the message** (a death must cost the run, never the work); write messages to a file and commit with `-F <file>` (never `-F -`, never backquotes inside `$(…)`), read back with `git log -1 --format=%B`. Exact-path `git add` (never `-A`), `git show --stat` after each. No `Co-Authored-By`.

## Report shape

1. Where this brief was wrong. 2. Branch, tip SHA (from git), commits (spike commits named as such), and `git diff --stat 5ee7d6a...<tip>`. 3. The recommendation in two sentences. 4. The sensitivity table: each candidate currency × each mutation (M21, EI delay, `/INT` width, timing-neutral control) → moved / did not move, with the fixtures carrying the signal, and each mutation's on-disk proof. 5. The consumer enumeration for Z80 `BusEvent`s, verdict per consumer, and whether anything reaches the Aether wire. 6. The hardware answers for both M24 halves, quoted with sources, and where documentation is silent. 7. The proposed parcels in order, with what each moves and which need my ruling. 8. `land.sh --no-push`: verdict token, LAND-EXIT, predicted vs observed legs, passed/failed/ignored, profile. 9. Open items, TAGs, anything BLOCKED.
