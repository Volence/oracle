# Ruling — CR-24, `emulator/scanlines` (2026-08-18)

Un-framed adjudication of `docs/2026-08-18-cr24-scanlines.md`, in the CR-18/19/20/21-23 mechanism
(`2026-08-18-ruling-cr21-23.md` is the previous sitting). Verdict: **ADOPT WITH CHANGES (7
required)**. The adjudicator's close: *"Evidence quality best of the last five rounds"* — with five
peripheral evidence defects (R7), none design-load-bearing.

## Evidence that checked out (verified firsthand by the adjudicator)

The CR's central claim — the handler is a pure read of engine state the tree already maintains — is
TRUE and verified end to end: `screen: ScanlineCapture` at engine.rs:447 (constructed :510), fed by
every run path (free_run_step :640-642, advance :673-676, advance_until :715-718, hosted
publish_capture :611-613 wired via oracle-frontend/src/bus.rs:164 → host.rs:346-348), latched
:741-746, read by framebuffer() :1061-1073 with the raster/stateRender split already on the wire
(engine.rs:1731, :1617). S/H-applied RGB confirmed to the bottom: system.rs:1044-1047 → report_rgb
(render.rs:1426) → cram_rgb_state per winning index+state. `source` semantics agree exactly with
screenshot's (contract prose :1216-1227 and the vendored fragment — same enum, spellings, underlying
tuple). The design pins (no frame param; top-level mode; neither truncated nor cursor per §2.4(d) at
protocol.md:542-543; pure read per read/pixel_attribution/sprites precedents at
:871-873/:1071-1073/:1107-1109; the demand-side shape ruling) are correctly grounded. ~40 anchors
checked; the design-load-bearing ones all held.

---

## Required changes

**R1. Restate the adoption condition in executable form.** The sweep-spec's A2 verbatim requires
the Aeon fixture: a ROM from aeon branch `parcel/raster-substrate-byte-moving` (unmerged; the spin
word at `Raster_Buf_A + 20` exists only post-item-1a) plus §5's game-state ritual (Camera_Y=144,
three live-pointer pokes). None of that is in this repo's suite: tests boot only
`oracle_core::testrom::build()`, and no testrom builder (`build`, `build_vint_counter`,
`build_pad_log`, `build_trap_on_frame`, `build_vram_poke`, `build_pad_poll`) writes CRAM mid-frame.
Registration gated on an unexecutable clause is a condition that gets waived silently. Replace
clause 3 with:

- **Suite gate (i), the two-timings poison**: a new testrom builder — e.g.
  `build_cram_midframe(line: u16)` — that polls the HV counter (`$C00008`) until the target line,
  then writes one CRAM entry the drawn art references (the content trap, inherited). Two builder
  arguments MUST yield different `rows[].rgb` at the boundary row, asserted against replies whose
  `source == "raster"`. This is the sweep fixture's SHAPE ported — what the demand doc's
  synthetic-fixture clause anticipates.
- **Suite gate (ii), executable today**: `color_1536` (vendored; pinned LIVE-DIFFERS
  `frame_hash=0x917371f07409cb25` at `crates/oracle-core/tests/scanline_goldens.rs:129-130`) — a
  raster reply's rows MUST differ from the same machine point's stateRender rows. (`cram_flicker`
  is pinned IDENTICAL-TO-POST-HOC at `:132` and is NOT a liveness discriminator — do not
  substitute it.)
- **Acceptance protocol, not a suite gate**: the verbatim A1/A2 sweep against the Aeon fixture,
  client-driven per sweep-spec §5 when that branch's ROM is in hand, with the two discriminator
  anchors as the first two data points.

Clauses 1 and 2 are executable as written and stand.

**R2. Mechanically tie mode ↔ rows[].width ↔ rgb length in the fragment** (the read precedent:
"enforced in the schema, in both directions, rather than left to prose", protocol.md:864-866). Add
to `result.allOf`:

```json
{
  "if": { "properties": { "mode": { "const": "h40" } }, "required": ["mode"] },
  "then": { "properties": { "rows": { "items": { "properties": {
    "width": { "const": 320 },
    "rgb": { "pattern": "^0x[0-9A-Fa-f]{1920}$" } } } } } },
  "else": { "properties": { "rows": { "items": { "properties": {
    "width": { "const": 256 },
    "rgb": { "pattern": "^0x[0-9A-Fa-f]{1536}$" } } } } } }
}
```

(320×3=1920 hex digits; 256×3=1536.) Keep the loose `^0x([0-9A-Fa-f]{6})+$` on the property as the
floor. Remains prose-only, correctly (JSON Schema cannot express them): startLine+count ≤ 224; rows
contiguous/ascending; rows.length == count.

**R3. State the crop direction.** `store_from_capture` (engine.rs:3483-3489) pads short lines AND
crops long ones. Replace the normalization sentence with:

> "A frame is not guaranteed rectangular (a game can switch width mid-frame); the answer is
> normalized to the width the frame ended on — what the VDP was scanning out by V-Blank — shorter
> lines black-padded and longer lines cropped to it, so every `rows[].width` in one reply equals
> `mode`'s width."

**R4. Pin the invalidation interaction.** `reset`, `reload_rom`, `restore` all drop the retained
frame (`invalidate_screen` at engine.rs:2120, 2148, 2293) — stateRender is NOT only
pre-first-frame; it recurs after each until a new frame completes. Add to the `source` bullet:

> "`reset`, `reload_rom`, and `restore` drop the retained frame — the machine under it was
> replaced — so the first call after any of them answers stateRender + `caveat` until a new frame
> completes. A server MUST NOT serve a frame drawn by a machine that is no longer there."

And "the pre-first-frame fallback" → "the fallback when no completed frame is retained"; `caveat`
description reworded to match.

**R5. Give `mode` a stateRender reading.** Append: "(a stateRender reply's `mode` is the width the
VDP would draw now.)" Matches the handler design (`render_line(0).len()`).

**R6. Record in contract text (blockquote or $comment), not only the CR:** (a) "Active display is
lines 0–223: this catalog's slice is the NTSC V28 display (the reference core is region-hardcoded);
a V30-capable server needs a §11 amendment raising the bound." (b) the `-32602`-here vs
`pixel_attribution`'s `-32004` contrast is a decision, not drift — land the contrast sentence in
the contract.

**R7. Factual corrections (wrong → true):**

| Wrong | True |
|---|---|
| "VRAM/CRAM/layers table holds nine rows" | TEN rows (protocol.md:1042-1051) |
| "§8's first must-not (protocol.md:1440)" | verbatim quote lives at protocol.md:1495; :1440 is §8 item 21's tail |
| "§11.13 amendment log's echo at :1775-1777" (twice) | that echo is §11.3's log (§11.3 spans :1742-1806; §11.13 begins :2364) |
| "store_from_capture is the single reader both the bus and blit_capture use" | TWO TWINNED implementations (engine.rs:3462; main.rs:503) kept aligned by cross-reference and tests; blit_capture does not call store_from_capture. Substance holds; letter does not |
| Pin 2's "verbatim" quote | demand doc :62-63 reads "'As of frame F' against the deterministic frame counter is fine — no sub-frame addressing needed." — resequenced paraphrase presented as verbatim; requote or unquote |

**Prose edit at protocol.md:1066-1067 — APPROVED**, precedented (§11.13's run-control append;
§11.12's deprecation marks; D14 makes the stale sentence a live bug). Replacement: "…a client that
needs the two to agree needs a per-scanline capability — `emulator/scanlines` (added 2026-08-18,
§11.14), which reads the drawn rows back." The §11.14 log entry must record this edit. Keep the
§11.3 log echo untouched (logs are records — correct principle, was mislabeled §11.13 per R7).

## Recommendations

- **(a)** caveat⇔stateRender mechanical enforcement — either way, record the choice.
- **(b)** drop `caveat`? from the §6 row (screenshot symmetry).
- **(c)** refresh cited sweep-spec commit to `2cf267a9` (A1/A2 verify verbatim against current
  file).
- **(d)** reword the garbled "declared here so its absence elsewhere is a decision" $comment to the
  precedent's actual phrasing.

## Cross-cutting

Fragment count 32 → 33 verified exactly (33 keys incl. one $comment). Paused-at-boot covered by
the fragment (stateRender + caveat + mode-from-current-width all validate; no refusal path needed,
"better than a black rectangle", engine.rs:1057-1059). After R2/R4 no conformant reply is rejected
and no constructible non-conformant reply admitted (the stale-frame-after-reset server was the one
admission; closed by prose since no schema can see staleness). `-32005` never applies; keeping it
off the state rule's list is correct. The no-GUI parity decision is properly grounded in D15
(:237-242). Evidence quality best of the last five rounds.

---

## Disposition (controller, 2026-08-18 — applied to the CR doc the same day)

All 7 required changes applied (R1–R7). The four recommendations, ruled:

- **(a) — caveat⇔stateRender left mechanically UNENFORCED**, matching `screenshot` (the direct
  precedent: its fragment does not tie `caveat` to `source` either). The decision is recorded in
  the fragment's $comment rather than silently taken.
- **(b) — ADOPTED.** `caveat`? dropped from the §6 row's result column, for symmetry with
  `emulator/screenshot`'s row (protocol.md:1208, which lists no `caveat`); the fragment still
  declares it (§2.4 rule 4).
- **(c) — ADOPTED.** The cited sweep-spec commit refreshed to `2cf267a9` ("spec(raster): add §6b —
  the two known-good anchors as disagreement discriminator"). Verified firsthand before applying:
  the file at that commit carries the A1/A2 text the CR quotes byte-identically.
- **(d) — ADOPTED.** The $comment sentence reworded to the write_memory precedent's actual
  phrasing ("declared…, not omitted by accident — the sprites precedent").

One consequence R1 forces on the CR's central claim, recorded there explicitly: suite gate (i)
requires a new `oracle_core::testrom` builder (`build_cram_midframe(line)`-shaped), a change in
`crates/oracle-core/src/testrom.rs` — so the slice is no longer literally "zero core changes"; it
is **"zero core MACHINE changes; one test-fixture builder added to testrom, precedented by
`build_trap_on_frame`"**. The CR's central claim now reads in that honest form.

The R7 corrections are applied with the corrections left visible in place — the record showing
what was wrong is the point, per the register's own recorded failure mode (an evidence defect that
recurs when silently patched).
