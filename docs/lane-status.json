{
  "focus": "I got the urgency wrong and the engine lane caught it. Nothing is broken, so the next pick is about value.",
  "inFlight": [
    {
      "what": "The emulator, running on the shared connection for every lane. Detached from this session, so clearing or rotating me does not stop it",
      "where": "process 2837321, reparented to user systemd",
      "agent": false
    }
  ],
  "blockedOnOwner": [
    {
      "id": "d-5",
      "what": "Which tooling I build first. My earlier reasoning was wrong: nothing is broken by the switch, so I have re-ranked on who is actually waiting and now recommend the timing fix.",
      "since": "2026-08-24"
    }
  ],
  "nextBoundary": "Whichever tooling you pick, built and verified against the live emulator.",
  "queue": [
    {
      "id": "CUTOVER-MECH",
      "title": "DONE. Emulator live on the shared socket, 41 methods, verified end to end through the shim",
      "state": "done",
      "size": "S",
      "blockedBy": null
    },
    {
      "id": "CR-A",
      "title": "Breakpoints + wait_for_break. NO consumer after all: aeon's gates spawn their own legacy emulator, so nothing breaks without it",
      "state": "open",
      "size": "L",
      "blockedBy": null
    },
    {
      "id": "CR-C",
      "title": "The server cannot say which server it is, so a session can swap implementations with no signal",
      "state": "open",
      "size": "M",
      "blockedBy": null
    },
    {
      "id": "Q-PROF-STRADDLE",
      "title": "DONE — the per-frame ring billed a straddling interrupt's whole cost to the frame it returned in, where the row could exceed its own cycles. Confirmed by test, fixed, aggregate and wire untouched",
      "state": "done",
      "size": "M",
      "blockedBy": null
    },
    {
      "id": "PROF-RING-SELF",
      "title": "Per-frame interrupt self-cycles aeon asked for — but a bucket's cyclesSelf is the exception entry ALONE, so the field as specified would not answer the question. Recommend REFUSING it and telling aeon why; owner call",
      "state": "open",
      "size": "S",
      "blockedBy": null
    },
    {
      "id": "ROMPATH",
      "title": "status reports the ROM path relative, which the protocol says it should not",
      "state": "open",
      "size": "S",
      "blockedBy": null
    },
    {
      "id": "SIGIL-DUMP",
      "title": "Per-instruction cycle dumper for sigil. They own the join after all; it needs branch outcomes per execution, and its consumer is deferred",
      "state": "blocked",
      "size": "S",
      "blockedBy": "no filed ask exists; sigil offered to write one"
    },
    {
      "id": "Z80",
      "title": "z80_read / z80_write — nothing calls them yet, and the legacy write silently corrupts on overrun",
      "state": "blocked",
      "size": "M",
      "blockedBy": "no programmatic consumer found — demand has not ordered it"
    },
    {
      "id": "WIKI-SPIKE",
      "title": "The wiki-emulator spike you approved twice",
      "state": "open",
      "size": "L",
      "blockedBy": "sits behind the acceptance parcels"
    }
  ],
  "updatedAt": "2026-08-24T02:41:01Z"
}
