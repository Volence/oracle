# CR-28 — the profiler's per-routine caller breakdown, and an absence that stops meaning three things

**Status: ADJUDICATED 2026-08-21 — ADOPT WITH CHANGES, and the changes are applied.** The ruling is
`docs/2026-08-21-ruling-cr28.md` (seven MUSTs, four SHOULDs; the three open questions of §8 all ruled, two
of them **against** the drafted spelling; additivity, fragment invariance, the closure sweep and the
41-message harness all reproduced independently, from payloads the adjudicator rewrote rather than from
this document's scripts). M1–M5 and S2–S4 edited the empyrean amendment; M1, M3, M4, M6, M7 and S1 edited
this document, and M2/M3/M4 moved counts that were re-derived by parse afterwards rather than adjusted.
Every §3, §4 and §5 block below was **re-extracted and re-byte-compared** against the amended branch after
the edits — CR-25's D-M1 lesson, inherited through CR-26 and CR-27: a block labelled verbatim stops being
verbatim the moment its source is edited — and every coordinate in this document was recomputed with them.

**★ Two things this document previously asserted are false, and they are named rather than quietly
overwritten.** First, §9 item 5 quoted §11.16's *"the four fields live in exactly two shapes — the routine
row and the interrupt bucket — and that is a bound on where they may appear, not an omission"* and said
**"it holds here unchanged."** It did not: the edge tuple carries `callsTotal`, one of the named four, in a
**third** shape — and after M4 it carries `cyclesTotal` and `cyclesSelfTotal` too. The amendment left a
normative sentence false and this CR cited it as intact, which in a register whose named failure mode is
true-sounding claims cited later as settled fact is the worst available combination. Per M1 the sentence is
now amended in §11.16 itself and §9 item 5 is rewritten; its only surviving true content was that
`perFrame[]` rows take no callers list. Second, the §7 attribution note declared *"one absence must not mean
three facts"* to be this CR's own compression **and then quoted the anchor containing those exact words**;
per M6 it is corrected below.

**Raised 2026-08-21 as DRAFT** against `empyrean/contract/protocol.md` §11.16's profiler family, landing as
amendment **§11.18**. Nothing here is merged, no handler exists, and no line under `crates/` is touched by
it.

**Where this lives.** The CR is `oracle` branch `cr28-callers`, cut from `main` at `5b42312`. The contract
edits are `empyrean` branch `callers-amendment`, cut from `main` at `2735ac7`, **two** commits — the draft,
then this ruling's M1–M5 and S2–S4 — committed as a **DRAFT** and **not merged, not pushed**.

**It adds no schema fragment.** The source `bus-protocol.schema.json` `methods` object holds **37 fragments
before and 37 after** — counted by `json.load` over both revisions rather than transcribed, both numbers
reproduced in §6 — because the whole change is new surface *inside* three fragments that already exist. Its
entire schema movement is **nineteen newly declared properties** (seventeen before M4 folded the edge's two
undivided partners in; every count in this document is a fresh parse of the post-ruling bytes), the
applicators tying them together, one new `initialize.limits` key among them, and prose reworded on five
existing nodes. **Zero properties removed; zero existing keyword values changed** — and that second figure
is executed rather than asserted: §6.4 prunes the six new key names out of the amended schema and shows the
result **deep-equal** to the pre-amendment document with prose stripped.

**Two sentences outside the new surface did have to move, and neither was found by the drafter.** Per M1,
§11.16's undivided-four bound is amended in place, because the edge tuple falsifies it. Per M2, §2.4's
*"Two spellings, and which one applies"* selection rule gains a third case, because the 8.1 ruling adopts a
spelling that paragraph did not name — without it the contract would instruct one form in §2.4 and ship
another in §6. Both are prose, neither is a wire change, and both are enumerated in §5(h)–(i) and in
§11.18's own movement paragraph.

**This CR does not redesign anything.** The design is `docs/2026-08-19-streaming-asks-recon.md` §4 (ask C3);
the demand is `docs/2026-08-21-cr28-shape-answer.md`, whose positions are **binding** and which is itself
transcribed from the requesting client's **own committed anchor** — aeon
`docs/benchmarks/streaming/CR28-DEMAND.md` at `6edb08ef`, read firsthand in the aeon tree for this document
and not carried from the relay. Where this CR reaches a different answer than the recon recommended, or
where the recon left a choice a contract cannot leave open, **§7 says so out loud and names the alternative
it did not take** — there are **eight** such places, four of them departures from the recon's own
recommendation, and one of them (§7.1) a departure from the recon that exists because the recon's
recommendation collides with a normative MUST it did not cite. **One position of the demand's is also
departed from**, and it is the only one: the adjudicator overruled the consumer's literal three-value
`entryKind` spelling in favour of a four-value split by cause. That is recorded as an adjudicated departure
in §8.2 and in §11.18's own ★, never absorbed — a change request may overrule a demand, but not quietly.

Every line anchor in this document was read in the file it names, in this session, at the revision named
beside it.

---

## 1. Why this is being raised

### 1.1 The instrument answers *how much*, and the client needs *who*

§11.16 (CR-26) gave this bus an exact per-invocation CPU-cycle accountant: rows keyed by routine entry
address, inclusive and self cycles, a stall subset, counts, undivided partners for all four, two interrupt
buckets keyed by cause, and a reconciliation identity a client can check exactly. What a row cannot express
is a **caller**. `routines[]` is keyed by entry address — `protocol.md` §6, *"rows are keyed by entry
address, never by symbol"* — so a routine reached from four places is one row with one number, and the
consumer's actual question (*which* of the four is driving this burst) has no expressible answer.

The requesting client is **aeon**, and their use is on record in their own tree: burst attribution for the
streaming-variance work — *"who invokes `S4LZ_DecompressDict` / `TileCache_FindStagedBlock`, per 31-frame
window, with callee self-time attributed by caller"* (`CR28-DEMAND.md` at `6edb08ef`). This is the third of
the three streaming asks the 2026-08-19 recon answered. The first (attribution under preemption) was already
correct by construction and needed a witness fixture, not a contract change. The second (`callsTotal` as an
exact integer) landed as a delta inside §11.16. Only this one is CR-sized, and §4.3 of the recon says why in
three parts: a new `set_profiler` param changes **what the instrument does**; a new `initialize.limits` key
changes what a server **advertises about itself**; and a nested shape with its own ordering and truncation
rules is new **surface**, not a new column.

### 1.2 The cheap answer is barred, and the reason is a migration this catalog already sold

Re-keying rows by `(addr, caller)` is the cheapest accumulator change of the three shapes the recon priced,
and it is the one it rejects outright. §11.16's migration story rests on a consumer's address-keyed lookup
continuing to work; a consumer doing `rows[addr]` against a re-keyed reply would suddenly get **N rows per
address, silently, with every one of them smaller than the figure it used to read**. That is the exact
silent-wrong-number class this family was re-specified to end. The demand asks for *"`callers: [...]` per
routine row"*, not for a re-key, and this CR takes the ask literally.

### 1.3 The mechanical claim the design rests on, restated as something refutable

The recon's §4.1 verifies aeon's *"your shadow-stack design may get this nearly for free"* hint and states
it precisely: the caller is available **at the push**, as one `self.stack.last()` read, and the per-caller
cost is proportional to **calls, not to instructions** — *because* cycles are folded at invocation
boundaries rather than keyed per step. A sampling profiler, or any design keying cycles by PC on every step,
would need the caller at every step, which is the expensive version of this feature. The hot path
(`charge()`, every retired step) is untouched; the added work is one word stored per push and one extra map
entry-and-add on the checkpoint/pop path.

**This CR does not re-verify that trace and does not need to.** It is an implementation-cost claim about the
reference server, it is anchored in the recon at `profiler-s1` `b46bbbc`, and nothing in the contract text
below depends on it being true — the wire shape would be the same if the accounting were expensive. It is
reproduced here because it is the reason the lens is armed for the **reply's** sake rather than the
accumulator's, which is a distinction §7.3 turns into contract text.

### 1.4 The demand's one change to the design, and why it is the interesting part

The recon's §4.1 named three edges where a caller has no address, and proposed the cheapest honest spelling:
*"omit `callerAddr` on that entry and let its absence mean 'entered from an interrupt'. The latter fits
§2.3's presence discipline better and needs no new enum on the wire."*

The consumer read that and refused it — quoted here **from the relay** (`cr28-shape-answer.md`), which is
the fuller wording of the two records:

> **Absence collapsing three meanings is the one thing I'd change.** Interrupt-entered vs root-edge vs
> depth-cap are different facts, and for streaming attribution the interrupt-vs-mainline distinction is one
> I will actually consume (a Tile_Cache_Fill edge from VBlank means something different from one from the
> game loop).

*(Their committed anchor at `6edb08ef` states the same position more compactly: **"Absence must carry its
reason: when `callerAddr` is absent, a required `entryKind: "interrupt" | "root" | "depthCap"` on the edge —
one absence must not mean three facts."** Substance identical on every point; where the two could ever
disagree, the anchor governs, and the relay doc itself says so. Per ruling S1 each quotation below names
which document its **wording** comes from.)*

Their proposal — a required `entryKind: "interrupt" | "root" | "depthCap"` whenever `callerAddr` is absent —
is what this CR was drafted to specify, and their **stated fallback floor** if the enum spelling lost
adjudication was, in the anchor's wording, *"interrupt-entered edges keyed distinctly from the other two,
however spelled"* (the relay's wording: *"…distinctly from the other two absence cases, however spelled"*).
**The spelling did lose**, and the floor is what shipped — exceeded, in fact, by a split into `hint` and
`vint`. §8.2 records the ruling and its grounds.

Two things also arrived binding and were not disturbed:

- **Per-edge `callsTotal` is kept**, for row symmetry — the anchor's wording: *"keep per-edge `callsTotal`
  (row symmetry), per-edge `cyclesSelf` is the money field."* The relay's fuller form is *"Symmetry with the
  routine row matters — an asymmetric tuple invites exactly the mis-read your docs warn about elsewhere."*
  That principle is what carried M4's fold-in past what they literally listed; see §8.3.
- **Per-edge `stallCycles` is EXPLICITLY DECLINED** — anchor: *"Per-edge stall explicitly DECLINED (measured
  flat and VBlank-owned at every state taken; dead weight)."* A drafter adding one would be contradicting
  the demand rather than gold-plating it. This CR therefore bars the key mechanically
  (`additionalProperties: false` on the edge shape) rather than leaving it merely undeclared, so the
  decision is enforced instead of remembered; §6.3 case 29 is the message that proves it. **This is the one
  explicit decline on record, and M4's fold-in does not touch it.**

---

## 2. What exists today

**In the contract** (`empyrean` `main` at `2735ac7`, `contract/protocol.md`, 3457 lines):

- The three profiler rows in their own §6 group at `:1442-1444`, and the twelve-behaviour normative
  blockquote at `:1446-1670`.
- §11.17 is the last amendment entry, at `:3304`, so a new entry appends at end of file.
- §2.4's bounded-list rule (`:497-559`), §2.5's params closure (`:561-611`), §8 item 20's harness-side result
  closure and item 22's server-side params closure are all in force.
- `-32005`'s `data.reason` register is §5's open string with an *e.g.* list (`:795-799`).
  **`perFrameNotArmed` is not in that list** — it lives in §6's profiler blockquote (`:1646`) and in the
  `frames` param's fragment description. §7.8 records why `callersNotArmed` is registered the same way and
  why no §5 edit is proposed.

**In the schema** (`contract/schema/bus-protocol.schema.json` at `2735ac7`): **37 fragments** (38 `methods`
keys with the `$comment`), parsed. `initialize.limits` declares `maxProfilerRoutines` and
`maxProfilerFrames` among its optional keys — `limits.required` is `["maxRunFrames","maxReadLen",
"maxLineBytes"]`, so **every profiler cap is already schema-optional with its conditional requirement
carried in prose**, which is the precedent §7.2 leans on. All 37 `params` objects declare
`unevaluatedProperties: false`; `handshake.initialize.params` is exempt.

**In the reference server, and this is the fact that shapes the whole design.** `oracle` `main` at
`5b42312` advertises **`"profiler": true`** (`crates/oracle-aether/src/engine.rs:1034`, introduced by
`1185625`, *"the profiler bus surface — three methods, and an identity a client can check"*), and all three
methods have handlers (`engine.rs:187-201`, `:2092`, `:2158`, `:2188`). §11.16's licence for **required**
result additions named its own expiry — *"the arc's merge window, the moment a handler serves a reply and
the capability flips to `true`"* — and that day has passed. **CR-28 is the first profiler movement that must
live without it**, which is exactly why it can: per the recon's §4.3, C3 uses no required addition at all,
so it never depended on the window being open. §7.2 is where that constraint stops being an observation and
becomes a design decision.

**No caller accounting exists in the reference server.** This CR makes no claim about how it would be built
beyond §1.3's cost claim, and specifies only what is **observable**.

---

## 3. The proposed §6 text (verbatim, as it landed on the branch)

Four edits in §6, plus the two outside it that M1 and M2 required (§5(h)–(i) carry those). **Every block
below was re-extracted mechanically from `callers-amendment` at `7c4b9fc` — the post-ruling commit — and
never retyped** — CR-25's D-M1 lesson, which CR-26 and CR-27 both inherited: a block labelled verbatim stops
being verbatim the moment its source is edited. Every coordinate moved when M1 and M2 inserted text above
these blocks, and every one was **recomputed from the extraction** rather than adjusted by hand.

**(a) The three method rows** — `contract/protocol.md:1458-1460` on the branch:

```markdown
| `emulator/set_profiler` | `enabled` (bool), `perFrame`? (bool, def false), `callers`? (bool, def false) | `enabled`, `perFrame`, `callers`? |
| `emulator/get_profiler` | — | `enabled`, `perFrame`, `callers`?, `framesRecorded`, `routineCount` |
| `emulator/get_profiler_frames` | `frames`? (≥1, ≤`limits.maxProfilerFrames`), `top`? (≥1, ≤`limits.maxProfilerRoutines`), `topCallers`? (≥1, ≤`limits.maxProfilerCallers`) | `frameCount`, `sampleCycles`, `totalCycles`, `unattributedCycles`, `abandonedFrames`, `depthExceeded`, `perFrameExact`, `budgetPct` \| `budgetPctOmitted`, `routines` (§2.4 container of `{addr,name?,disp?,cycles,cyclesSelf,stallCycles,calls,cyclesTotal,cyclesSelfTotal,stallCyclesTotal,callsTotal, callers?[]{callerAddr?,callerName?,callerDisp?,entryKind?,cycles,cyclesSelf,calls,cyclesTotal,cyclesSelfTotal,callsTotal}, callersTotal?, callersReturned?, callersTruncated?}`), `interrupts{hint,vint}{cycles,cyclesSelf,stallCycles,calls,cyclesTotal,cyclesSelfTotal,stallCyclesTotal,callsTotal}`, `perFrame`? (§2.4 container of `{frame,cycles,stallCycles,hintCycles,vintCycles}`) |
```

**(b) The blockquote header, recounted** — `contract/protocol.md:1462-1465`:

```markdown
> **What the profiler measures, and what a row means** *(added 2026-08-19, §11.16; the caller lens added
> 2026-08-21, §11.18)*. `set_profiler` arms an
> exact per-invocation CPU-cycle accountant, `get_profiler` reports its state, and `get_profiler_frames`
> returns the accumulated sample. Thirteen behaviours are normative.
```

*The count is twelve on `main` and thirteen here, verified by counting `^> - \*\*` inside the blockquote on
both revisions rather than by adding one to a remembered number.*

**(c) The arm/disarm behaviour's tail, amended** — `contract/protocol.md:1654-1660`:

```markdown
>   nothing else — the `perFrameNotArmed` and `callersNotArmed` refusals in the two behaviours below are
>   refusals about the *instrument's*
>   state, and they apply whether the machine is running or paused. **Every arming flag resets together**:
>   `set_profiler{enabled:true}` starts a fresh sample under exactly the lenses that call names, so a client
>   arming `callers` on an already-armed instrument gets caller data for a new sample and not for the one it
>   was watching *(the second sentence of this behaviour, restated for the lens §11.18 adds, because a
>   second arming flag is where "a second arm discards an in-flight sample" first becomes easy to forget)*.
```

**(d) The thirteenth normative behaviour**, inserted after the `routines`/`perFrame` container behaviour and
before the `abandonedFrames`/`depthExceeded` one — `contract/protocol.md:1679-1738`:

```markdown
> - **The caller breakdown is a second lens on the same rows, opt-in, and it is where an absence carries
>   its own reason** *(added 2026-08-21, §11.18)*. Armed with `set_profiler{callers:true}`, every routine
>   row gains four keys that arrive **as a set** — `callers`, `callersTotal`, `callersReturned`,
>   `callersTruncated` — and unarmed it gains none of them, so a client that never asks reads a reply
>   identical to the one this surface already sends. An entry of `callers` is **one call edge**: the
>   cycles, self-cycles and invocations this routine accrued *when reached from that particular caller*,
>   in the routine row's own spelling wherever the two describe the same quantity. The list is ordered by
>   `cycles` **descending**, so a bounded list is the expensive end rather than an arbitrary slice — the
>   rule `routines` already carries, one level down — and it is bounded by **`limits.maxProfilerCallers`**
>   (REQUIRED when a server implements the lens, and **omitted when it does not**, which is how a client
>   discovers the lens exists at all) and narrowed by the `topCallers` param, **refused with `-32602`
>   above the cap, never clamped**. `topCallers` sent against a sample not armed for callers is
>   **`-32005` (`data.reason = "callersNotArmed"`)**, on `frames`/`perFrameNotArmed`'s reason word for
>   word: a parameter that cannot affect the answer is worse than one that is rejected. That refusal is
>   `get_profiler_frames`' alone — `get_profiler` reports the instrument's **state** and carries no rows,
>   so it echoes `callers` as a third arming fact and can never refuse for this reason.
>
>   **Per-edge figures are divided like every other divided figure here**, which has one consequence a
>   client must be told rather than left to discover: arming the lens **adds** divided figures to the
>   reply, so `perFrameExact` ranges over them too and a sample that reported `true` without the lens may
>   report `false` with it. Nothing became less exact; more figures are being reported on. **Every divided
>   edge figure therefore carries its undivided partner** — `cycles`/`cyclesTotal`,
>   `cyclesSelf`/`cyclesSelfTotal`, `calls`/`callsTotal` — which is the routine row's own discipline at a
>   smaller denominator, and a smaller denominator is where division does more damage, not less.
>
>   **Two sums are normative, both guarded by `callersTruncated: false`, and both are exact**, because
>   every invocation has exactly one caller edge and the edges therefore *partition* the row: a row's
>   edges' **`callsTotal` sum to the row's `callsTotal`**, and their **`cyclesSelfTotal` sum to the row's
>   `cyclesSelfTotal`** — undivided on both sides of both, so each is an `==` a gate can assert rather than
>   a bound it must relax. The cycle sum is stated on **self** and not on the inclusive figure for the
>   reason §11.16 states the reconciliation identity that way: inclusive time double-counts by
>   construction. `callersTruncated` is **per row**, not per reply, because one reply legitimately carries
>   some rows complete and others clipped; when it is `true`, both sums become lower bounds.
>
>   **`callerAddr` is absent rather than fabricated, and `entryKind` says which absence it is.** A
>   fabricated `0x0` is barred here exactly as a fallback-to-address-string is barred on `name`. When
>   `callerAddr` is absent, `entryKind` is **REQUIRED** and names one of four genuinely different facts —
>   `hint`, the routine was reached from within a level-4 (HBlank) interrupt's context, whose caller is
>   that **bucket** and so has no routine address to give; `vint`, the same from the level-6 (VBlank)
>   bucket; `root`, this row's routine is the frame the sample opened on and no call into it was ever
>   observed; `depthCap`, the calling frame was one the accountant declined to track at its shadow-stack
>   bound — the same event `depthExceeded` counts in the aggregate. Collapsing them into one silent
>   absence would make an interrupt-entered edge indistinguishable from a mainline one, which is a
>   distinction the client this lens was built for branches on. **The interrupt case is split by cause and
>   not carried as one `interrupt` value**, which is this behaviour's neighbour rule reaching one level
>   down: interrupt accounting here is keyed by cause and `hint`/`vint` are never collapsed anywhere else
>   in this family, so a single value would be the one place in this surface where the two causes are
>   indistinguishable — and these two **join the edge to the bucket it came from by name**, which a
>   collapsing value cannot. It is also the one spelling that could not be deferred: widening an emitted
>   enum later is **not additive**, because a client validating against a copy closed over the old value
>   set refuses the new member. When `callerAddr` **is** present, `entryKind` is absent; the fragment
>   enforces the biconditional rather than leaving it to this sentence.
>
>   **And one caveat about a `callerAddr` that IS present.** The frame the sample opened on is keyed by
>   *whatever PC retired first*, which is mid-routine — the accountant started watching a program already
>   running. An edge whose caller is that frame carries a **real** address that is **not an entry point**,
>   so it may resolve to a bare label at a **non-zero `callerDisp`**, or to nothing at all. A client that
>   assumes every `callerAddr` resolves like a row key will mis-render exactly one edge per sample. Note
>   that this is a different fact from `entryKind: "root"`, which says *this row* had no observed caller;
>   the two meet on one shape and are not the same thing.
```

---

## 4. The §11.18 entry (verbatim)

Appended at end of `protocol.md`, which is **3457** lines on `main` at `2735ac7`. The amendment's
insertions above it — §2.4's widening (M2), §11.16's amended bound (M1) and §3's four §6 edits — push
everything below them down by **90** lines, counted mechanically against the amended branch rather than
adjusted by hand, which is why the entry begins at `:3548` in a file that is **3701** lines there.

**Byte-identical to `contract/protocol.md:3548-3701` on `callers-amendment` at `7c4b9fc`.**

```markdown
### 11.18 — 2026-08-21: the profiler learns who called, and one absence stops meaning three things

**CR-28**, raised in `oracle/docs/2026-08-21-cr28-callers.md` off the design in
`oracle/docs/2026-08-19-streaming-asks-recon.md` §4, with the demand transcribed in
`oracle/docs/2026-08-21-cr28-shape-answer.md` from the requesting client's **own committed anchor** —
aeon `docs/benchmarks/streaming/CR28-DEMAND.md` at `6edb08ef`. It appends after **§11.17** (CR-27) and
amends **§11.16** (CR-26), the family it extends. It is the third of the three streaming asks that recon
answered; the first two needed no change request and no contract movement at all, which is why only this
one is here.

**No method is added and no key is renamed.** The schema's `methods` object holds
**37 fragments before and 37 after** — re-derived by parsing both revisions, never carried over, per
§11.17's clause-7 rule — because the whole amendment is new surface *inside* three fragments that already
exist. The movement is **nineteen newly declared properties** and the applicators that tie them together;
**zero** properties removed, and **zero** existing keyword values changed. That last figure is not an
assertion: pruning the six new key names out of the amended schema yields a document **deep-equal** to the
pre-amendment one with prose stripped, which is the strongest form the additivity claim has been made in
on this bus.

**Two sentences outside the new surface did have to move, and both were found at adjudication rather than
by the drafter.** §2.4's *"Two spellings, and which one applies"* selection rule resolved **every**
field-list to the container spelling, so this amendment would have shipped a flat list in §6 while §2.4
instructed a nested one — that paragraph gains the third case (a list that is a field of an **item**), with
`routines.items[].callers` as its registered example. And §11.16's *"the four fields live in exactly two
shapes … a bound on where they may appear, not an omission"* is **false** the moment a call edge carries
`callsTotal`, `cyclesTotal` and `cyclesSelfTotal`; that sentence is amended in place to name the third
shape and to keep the bound that actually survives — `perFrame[]` rows carry no undivided partner, ever.
Neither is a wire change. Both are recorded here because an amendment that leaves a normative sentence
false, in a register whose named failure mode is true-sounding claims cited later as settled fact, has not
finished.

| Item | The gap | What this amendment adds |
|---|---|---|
| **CR-28a — the lens, and its arming** | §11.16 gave this surface exact per-invocation attribution and then answered only *how much did this routine cost*. The client that asked for it needs *who invoked it* — burst attribution for a streaming-variance instrument, per window — and a `routines[]` row keyed by entry address cannot express a caller at all. Re-keying rows by `(addr, caller)` was the cheap answer and is barred: §6 pins that rows are keyed by entry address, and a consumer's `rows[addr]` lookup would silently start returning N rows per address, each smaller than the figure it used to read. | An **opt-in** `set_profiler{callers: true}`, on the `perFrame` precedent exactly: unarmed, the reply is what it already was and the second accumulator is never allocated. `set_profiler` and `get_profiler` **echo** it — conditionally, the one shape a required addition could not take here, since §11.16's pre-release window **closed** when the profiler arc merged and a server shipped `"profiler": true`. Its absence therefore means *this server has no caller lens*; `false` means *the lens exists and is off*. `initialize.limits` gains **`maxProfilerCallers`**, and that key's **presence is the capability signal** — the `maxWriteLen`/`maxHashLen` conditional-requirement precedent, reached for one lens rather than one method. |
| **CR-28b — the edge tuple** | The instrument's own attribution is *per-invocation folded* rather than per-step keyed, so the caller is available at the push for one `O(1)` read and the per-caller cost is proportional to **calls**, not to instructions. The wire half is the part that is not free: what an edge carries decides what a consumer can check. | `callers[]` on every routine row: `{callerAddr?, callerName?, callerDisp?, entryKind?, cycles, cyclesSelf, calls, cyclesTotal, cyclesSelfTotal, callsTotal}`, ordered by `cycles` descending so a bounded list is the expensive end. **Symmetric with the routine row wherever the two describe the same quantity** — the consumer's stated principle, and it is what carried the tuple past what they literally asked for: **every divided edge figure has its undivided partner beside it**, folded in at adjudication rather than deferred, because §11.16's own *quiet gap* argument (a `==` gate on cycles fails silently; a shortfall of at most `frameCount − 1` reads exactly like agreement) applies with **more** force at a smaller denominator, not less. **Two normative sums** follow from every invocation having exactly one caller, and both are **exact** under `callersTruncated: false`: the edges' `callsTotal` sum to the row's, and their `cyclesSelfTotal` sum to the row's — undivided on both sides of both, so a suite gate asserts `==` rather than relaxing to a bound. **No per-edge `stallCycles`** — the requesting client **declined** one on measured grounds, so its absence is a demand-side decision and the fragment bars the key outright rather than leaving it undeclared. |
| **CR-28c — the absence that meant several things** | The design this amendment is drawn from proposed letting a missing `callerAddr` simply *mean* entered-from-an-interrupt, which fits §2.3's presence discipline and needs no enum. The consumer read it and refused: interrupt-entered, root-entered and depth-capped are different facts, and the interrupt-versus-mainline half is one they will branch on — a cache-fill edge reached from VBlank means something different from the same edge reached from the game loop. | **`entryKind: "hint" \| "vint" \| "root" \| "depthCap"`, REQUIRED exactly when `callerAddr` is absent and forbidden when it is present** — a biconditional the fragment enforces with `if`/`then`/`else` rather than leaving to prose. A fabricated `0x0` is barred, as a fallback-to-address-string already is on `name`. **The interrupt case splits by cause, which is one value more than the requesting client proposed**, and the departure is recorded rather than absorbed: see the ★ below. And the contract carries the **other** root fact beside it, because the same word covers both and they are not the same: an edge whose caller is the sample's **inferred root frame** carries a `callerAddr` that is real, is mid-routine, is **not an entry point**, and may resolve at a non-zero `callerDisp` or not at all. |

**★ Why no required addition appears here, and why that is the whole sequencing story.** §11.16 took eight
REQUIRED fields in twelve places on an explicit, dated licence: *"no server speaking this bus implements
these three methods, and no client has ever received a reply any fragment governed"*, with the expiry named
as *"the arc's merge window, the moment a handler serves a reply and the capability flips to `true`."*
**That day has come** — the reference server's `initialize` advertises `"profiler": true` and all three
methods have handlers — so this amendment is the first profiler movement that had to live without the
licence, and it does: every key it adds is **optional-by-arming** or **optional-by-capability**, an unarmed
reply is byte-identical to what the surface already sends, and a client that ignores the new keys is
unaffected. The one shape that *would* have needed the closed window — a required `callers` echo on the two
state replies — is the one shape this amendment deliberately did not take. That is not a footnote; it is
why CR-28 could be sequenced last while its two siblings could not, and it was the correct call before the
window shut as well as after.

**★ The lens is opt-in for the reply's sake, not the accumulator's, and the reason inverts `perFrame`'s.**
A per-frame ring is armed because it costs **memory across a long sample** while the aggregate accumulates
in place. Per-caller accounting costs almost nothing to accumulate — the hot path is untouched, the added
work lands once per call and once per checkpoint — and is armed because **the reply** grows by rows ×
edges. Both reach the same answer from opposite directions, and stating which one applies keeps a later
reader from optimising the wrong half.

**★ `maxProfilerCallers` is a REPLY bound and not a retention bound.** `maxProfilerFrames` really is a ring
depth: rows beyond it are gone. This cap is `maxProfilerRoutines`' kind — the accumulator keeps every
observed edge and the cap decides how many are *sent* — which is exactly what makes a row's `callersTotal`
the true count of distinct callers rather than the count that survived a ceiling. A client reading
`callersTotal: 40` beside a nine-entry list is being told the truth about its own instrument.

**★ One position of the requesting client's was overruled, and it is named here rather than absorbed.**
Their committed anchor asks for `entryKind: "interrupt" | "root" | "depthCap"` — three values, with a
single `interrupt`. This amendment ships **four**, splitting the interrupt case into `hint` and `vint`, and
the grounds are three: **(i)** §6 keys interrupt accounting by **cause** and this family collapses `hint`
and `vint` **nowhere else**, so a single value would be the one place in the profiler surface where the two
causes are indistinguishable — the defect §11.16 was raised to end, in miniature; **(ii)** the cause values
**join the edge to the bucket it came from by name**, which the blockquote's own sentence (*"its caller is
a bucket"*) makes the natural read and which a collapsing value cannot do; and **(iii)** widening an
emitted enum later is **not additive** — a client or vendored validator closed over three values refuses
the fourth — so this is the one spelling on this row that could not be deferred cheaply. The client's own
stated fallback, *interrupt-entered edges keyed distinctly from the other two, however spelled*, is
satisfied **twice over**, and their named branch (interrupt versus mainline) survives as a two-value
membership test in place of one equality. A change request may overrule a demand; it may not do so quietly,
and the requester is pointed at this paragraph rather than asked to re-open.

**★ What does not change.** Every existing key of all three methods, and every figure in them: the
aggregate rows, both interrupt buckets, the reconciliation identity, `unattributedCycles`,
`abandonedFrames`, `depthExceeded` and the undivided set are untouched in value and in meaning. `routines`
and `perFrame` keep §2.4's nested container spelling — the widening above adds a **third** case for lists
one level deeper and re-spells neither of them. The fragment count. The rows-keyed-by-entry-address
pin — the one thing the rejected re-keying would have broken. The interrupt buckets take **no** `callers`
list: a bucket is keyed by cause and what it preempted is not a call edge. `perFrame[]` rows take none
either, on §11.16's own bound — whole-frame totals carry no per-routine breakdown, so there is nothing for
a caller to attach to. And §8's checklist gains **no** item: item 22 is bus-wide policy, this is one
family's second lens, and a checklist that grows with every feature stops being a checklist.

**★ What this amendment does not carry.** Three items, named rather than folded in — and a fourth that was
registered as a deferral in the draft and **dissolved at adjudication**, recorded here in that state
because a register that quietly deletes its own debts stops being evidence. **The separate-method
form** — `emulator/get_profiler_callers{addr}` — stays **on record and unbuilt**: the accumulator work is
identical, so it can be revived late without waste, and it loses today on **cross-read skew**, since two
pure reads a frame apart describe different samples and a client correlating them compares figures with
different divisors, silently. **`F-PROFILER-EDGE-UNDIVIDED` is FOLDED IN, not deferred**: the draft carried
`callsTotal` alone on the edge and registered the missing cycle partners as a follow-up, which would have
shipped a **normative sum no suite gate could assert with `==`** — §11.16's own *quiet gap* at the exact
level the argument was quoted from. `cyclesTotal` and `cyclesSelfTotal` are therefore required on the edge,
and required is safe **there** where it was not on the two state replies: the edge shape has never shipped,
so §11.16's expired licence does not reach inside it. The id is retired by this sentence and carries no
debt. **`F-PROFILER-ROOT-EDGE-KIND`**: an edge
whose `callerAddr` is present carries no marker saying whether that address is an entry point or the
inferred root's mid-routine PC, so the caveat above is a thing a client must **know** rather than a thing
it can **read**; nobody asked for a typed key and inventing one here would be surface nobody requested.
**A per-reply `callersLimit`** is not emitted: §2.4 makes `limit` optional, the applied ceiling is one
number for the whole reply, and repeating it on every row would be a constant field wearing signal's
clothes.

*Adoption condition, per §11.6 / §11.8 / §11.10 / §11.11 / §11.14 / §11.15 / §11.16 / §11.17, in CR-24's
two-part structure — suite gates executable in the reference repo, plus a demand-side acceptance protocol:*
registered when **(1)** a conformant reply passes all three fragments **closed**
(`unevaluatedProperties: false` at test time, §8 item 20), happy path plus **one refusal per catalogued
bound** — `topCallers` above the advertised `maxProfilerCallers` → `-32602`, refused and **not clamped**;
`topCallers` against a sample armed without callers → `-32005` (`callersNotArmed`); and, on a server that
implements the lens, `set_profiler{callers: false}` **accepted** rather than refused; **(2)** the
**arming biconditional** is proven in both directions by messages that **fail** — a row carrying `callers`
without `callersTruncated`, a row carrying `callersTotal` alone, and an unarmed reply carrying any of the
four — since a closure nobody has watched reject is a closure nobody has tested; **(3)** the **`entryKind`
biconditional** is proven the same way: an edge with no `callerAddr` and no `entryKind` **refused**, an
edge carrying both **refused**, each of the **four** enum values accepted on its own, and the word
`"interrupt"` — the collapsing spelling this amendment declined — **refused by the enum**, since the value
a ruling removed from the wire is the one a later drafter is likeliest to reintroduce; **(4)** both
normative sums are gated with **`==`** on a **deterministic fixture** against a call graph the test itself
established — edges' `callsTotal` == the row's `callsTotal` **and** edges' `cyclesSelfTotal` == the row's
`cyclesSelfTotal`, both under `callersTruncated: false`, both **relaxed to lower bounds** when it is
`true` — since an assertion that still passes when the handler returns a fixed breakdown is vacuous, and
since a sum that can only be bound-checked is a sum this catalog has already learned reads like agreement;
**(5)** the **unarmed reply is proven byte-identical** to the pre-amendment
one, which is this entry's central claim and the one a later simplification into an always-on accumulator
would break first; **(6)** `perFrameExact` is shown to **range over the edge figures** — one fixture where
arming the lens flips it from `true` to `false` — because that is the one behaviour a client could
reasonably have assumed was unaffected; and **(7)** the schema's top-level `description` states a fragment
count **re-derived by parsing the merged `methods` object** in the same run as clause 1, never carried over
— **37, unmoved**, which is a number that must still be *parsed*, because §11.17's own merge measured what
a carried-forward count is worth — and the same run re-derives the params **closure**, every
`methods.<name>.params` declaring `unevaluatedProperties: false` with the handshake exempt, since this
amendment edits **two** params objects and §11.17's merge is the recorded case of three arriving open
through a textually clean merge. Clauses 1–7 are executable in the reference repo. The demand-side
protocol is the requesting client's own: they consume the lens for burst attribution — who invokes
`S4LZ_DecompressDict` and `TileCache_FindStagedBlock`, per 31-frame window, with callee self-time
attributed by caller — and the acceptance is that an interrupt-entered edge is **distinguishable from a
mainline one in their reader without asking us**, which is the property they named as the reason the enum
was worth a round — and which the four-value spelling serves at a finer grain than they asked for. That
protocol is not a suite gate — the client is theirs — exactly as §11.14's A1/A2 sweep, §11.16's parity
corpus and §11.17's editor probe are not.

**One further check is named here and is deliberately NOT a clause**, on the same precedent: the
**no-lens direction** — a server that omits `limits.maxProfilerCallers` refusing `set_profiler{callers:
true}` with `-32602`, and never emitting the echo — cannot be executed in the reference repo, because that
server will implement the lens. Clause 1 proves only the other side (`callers: false` accepted on a
lens-implementing server). It is recorded rather than left unstated so that the first server that
legitimately lacks the lens has a written expectation to conform to, instead of inventing one; a
registration gated on an unexecutable clause is a condition that gets waived silently (§11.14).
```

---

## 5. The schema and the two prose amendments — no new fragment, nineteen new properties (verbatim)

All blocks re-extracted mechanically from `callers-amendment` at `7c4b9fc`, the post-ruling commit.

**(a) `initialize.limits` gains one key:**

```json
              "maxProfilerCallers": { "type": "integer", "minimum": 1, "description": "Largest `topCallers` emulator/get_profiler_frames accepts, and the ceiling it applies to each routine row's `callers` list (added 2026-08-21, §11.18, on the maxProfilerRoutines precedent). A REPLY bound, not a retention bound: the accumulator keeps every observed call edge, which is what makes a row's `callersTotal` the true count of distinct callers rather than the count that survived a cap — the opposite of maxProfilerFrames, which really is a ring depth. Refused, never clamped. ITS PRESENCE IS THE CAPABILITY SIGNAL for the caller lens: a server that supports set_profiler{callers:true} MUST advertise this key, and a server that does not MUST omit it, so a client branches on capability rather than on a version integer (D5). That conditional requirement is prose here for the reason maxWriteLen's is — limits.required cannot express `required iff the server implements a lens`." }
```

**(b) `emulator/set_profiler` — the arming param and its echo:**

```json
          "callers": { "type": "boolean", "description": "OPT-IN per-caller accounting, default false (added 2026-08-21, §11.18). When true the server also accumulates, per routine, the call edges that reached it, readable as the `callers` list on each get_profiler_frames routine row; when false those keys are absent from every row and `topCallers` there is refused. Opt-in on the perFrame precedent and for the perFrame reason INVERTED: the accumulator cost is small — per-caller work is proportional to CALLS, not to instructions, because cycles are folded at invocation boundaries rather than keyed per step — but the REPLY grows by rows x edges, and this catalog's pattern is that a second lens is opt-in. A server that does not implement the lens omits limits.maxProfilerCallers and REFUSES this param with -32602 (§2.5); a server that does implement it MUST NOT refuse `callers:false`. That refusal is CAPABILITY-CONDITIONAL rather than a plain undeclared-key case, and §2.5 is being applied rather than analogized: it binds a server against the fragment AT THE REVISION IT IMPLEMENTS, and a lens a server does not implement is outside the surface it implements — so this key, though declared by the shared fragment post-amendment, is undeclared FOR THAT SERVER and refuses by name like any other." }
```

```json
          "callers": { "type": "boolean", "description": "Whether per-caller accounting is armed after this call (added 2026-08-21, §11.18). Echoed for perFrame's reason: it is the key that decides whether get_profiler_frames answers a `topCallers` param or refuses it. REQUIRED of a server that advertises limits.maxProfilerCallers, and ABSENT from one that does not — a conditional this fragment cannot express in `required` without making every pre-§11.18 server non-conformant, which D5 bars now that a server ships this family (§11.16's pre-release window closed at the profiler arc's merge). Absence therefore means `this server has no caller lens`, never `the lens is off`; `false` means the lens exists and is off." }
```

**(c) `emulator/get_profiler` — the state read gains the third arming fact:**

```json
          "callers": { "type": "boolean", "description": "Whether per-caller accounting is armed (added 2026-08-21, §11.18). REQUIRED of a server that advertises limits.maxProfilerCallers and ABSENT from one that does not, for the reason set_profiler's echo carries it that way; absence means `no caller lens on this server`, `false` means `the lens exists and is off`. This method reports the instrument's STATE and never its data, so a routine row — and therefore a `callers` LIST — cannot appear here at all, and callersNotArmed is structurally unreachable on this method." }
```

**(d) `emulator/get_profiler_frames` — the `topCallers` param:**

```json
          "topCallers": { "type": "integer", "minimum": 1, "description": "Added 2026-08-21, §11.18. Bound on each routine row's `callers` list — the N most expensive edges by `cycles`, per row, independently. It does NOT gate whether the lists appear: presence is decided by the ARM (set_profiler{callers:true}), exactly as the per-frame list's presence is, and this param only narrows what an armed reply carries. Above the server's advertised maxProfilerCallers it is REFUSED with -32602, never clamped (the `top` precedent). REFUSED with -32005 (data.reason = 'callersNotArmed') when the sample was not armed for callers, because a parameter that cannot affect the answer is worse than one that is rejected — the `frames`/perFrameNotArmed rule with the lens changed and nothing else. D9 category 2." }
```

**(e) The routine row's `callers` list and the call-edge shape** — ten properties, six of them required after M4's fold-in:

```json
                    "callers": {
                      "type": "array",
                      "description": "Added 2026-08-21, §11.18. OPT-IN per-caller breakdown of THIS row: one entry per distinct call edge that reached this routine, ordered by `cycles` DESCENDING so a bounded list is the expensive end rather than an arbitrary slice — the rule routines.items already carries, applied one level down. Present if and only if the sample was armed with set_profiler{callers:true}; `topCallers` narrows an armed list and never conjures one. Bounded by maxProfilerCallers, a POLICY bound, so §2.4 clause (a)'s three companions ride beside it as row siblings (callersTotal / callersReturned / callersTruncated) rather than as a nested {items,...} container — §2.4's FLAT spelling scoped to an ITEM, which that section's own selection rule now names for this case (widened by §11.18, with this list as the registered case) rather than leaving §6 to ship one form while §2.4 instructs another. NO cursor: this method accepts no continuation param and §2.4 clause (b) forbids emitting a token that can never be handed back. No per-row `limit` sibling either: the applied ceiling is one number for the entire reply (the client's topCallers, or the advertised cap), so repeating it on every row would be a constant field wearing signal's clothes — §2.4's constant-caveat advisory in field form — and §2.4 makes `limit` OPTIONAL.",
                      "items": {
                        "type": "object",
                        "required": ["cycles", "cyclesSelf", "calls", "cyclesTotal", "cyclesSelfTotal", "callsTotal"],
                        "properties": {
                          "callerAddr": { "allOf": [{ "$ref": "#/$defs/hex" }], "description": "ENTRY address of the routine that made these calls, masked to the 24 address lines the 68000 drives — the same key space the row's own `addr` uses, so an edge joins to its caller's row by string equality and needs no client-side mask. ABSENT when the edge has no calling routine address to name, and its absence is never a fabricated 0x0: the three ways that happens are enumerated by `entryKind`, which is REQUIRED exactly then. ONE CAVEAT A CLIENT MUST NOT ASSUME AWAY: when the calling frame is the sample's INFERRED ROOT — the frame the accountant opened on whatever PC retired first, having started mid-program — this address is real and is where the call came from, but it is NOT an entry point, so it may resolve to a bare label at a NON-ZERO callerDisp or to nothing at all. A client that assumes every callerAddr is a routine entry will mis-render exactly one edge per sample." },
                          "callerName": { "allOf": [{ "$ref": "#/$defs/symbolName" }], "description": "The BARE label at callerAddr — §4's naming rule, which binds every symbol field on this bus. Never a `name+$hex` composite: the displacement is callerDisp, and the concatenation is the client's to render. Absent, together with callerDisp, when nothing resolves — a server MUST NOT fall back to the address string, for the row field's reason: a client cannot tell a symbol spelled like an address from a lookup that failed. Requires callerAddr, since a name with no address to name is not a fact about anything." },
                          "callerDisp": { "type": "integer", "minimum": 0, "description": "Bytes past callerName. 0 is the normal case here exactly as on the row, because a call edge is keyed by the caller's ENTRY address — but unlike the row there is one SYSTEMATIC exception a client must expect rather than treat as a lookup failure: the inferred-root caller is keyed mid-routine and carries a non-zero displacement (see callerAddr). Travels with callerName in both directions (dependentRequired)." },
                          "entryKind": { "type": "string", "enum": ["hint", "vint", "root", "depthCap"], "description": "Added 2026-08-21, §11.18. REQUIRED exactly when callerAddr is absent, and forbidden when it is present — enforced mechanically by this shape's if/then/else rather than left to this sentence. It exists because ONE ABSENCE WOULD OTHERWISE MEAN SEVERAL DIFFERENT FACTS: `hint` — the routine was entered from within a level-4 (HBlank) interrupt's context, so its caller is that BUCKET and there is no calling routine address to give; `vint` — the same, from the level-6 (VBlank) bucket; `root` — this row's routine IS the frame the sample opened on, so no call into it was ever observed; `depthCap` — the calling frame was one the accountant declined to track because its shadow stack was at its bound, which is the same event `depthExceeded` counts in the aggregate. A fabricated 0x0 in place of any of them is barred. THE INTERRUPT CASE IS SPLIT BY CAUSE, not carried as one `interrupt` value, and that is this family's own rule reaching one level down: §6 keys interrupt accounting by CAUSE and never collapses hint and vint anywhere else, so a single collapsing value would be the one place in this surface where the two are indistinguishable — and these two values JOIN the edge to the bucket it came from BY NAME, which a collapsing value cannot do. It is also the one spelling decision that could not be deferred: widening an emitted enum later is NOT additive, since a client or vendored validator closed over the old value set refuses the new member. Note the two senses of ROOT that meet on this shape and are NOT the same fact: `entryKind: 'root'` says THIS ROW had no observed caller, while the inferred-root caveat on callerAddr describes an edge that DOES carry an address which happens to be mid-routine." },
                          "cycles": { "type": "integer", "minimum": 0, "description": "Per-frame CPU cycles this routine accrued when reached from THIS caller, INCLUSIVE of everything it called — the row's own `cycles`, partitioned by caller. Divided by the same frameCount as every other divided figure in this reply, so perFrameExact ranges over these too: arming the caller lens ADDS divided figures, and a reply that would have been exact without it may report false with it. Inclusive figures double-count by construction, so a client checking that the edges account for the row should sum cyclesSelfTotal (below) and not this." },
                          "cyclesSelf": { "type": "integer", "minimum": 0, "description": "The same span with callee time subtracted — callee self-time attributed by caller, which is the figure the requesting consumer named as the one they came for. Per-frame, divided by frameCount; its undivided partner is cyclesSelfTotal, which is the term a client should actually reconcile with." },
                          "calls": { "type": "integer", "minimum": 0, "description": "Per-frame invocations of this routine made from this caller — a COUNT, never floored up, so an edge exercised fewer times than the sample has frames reports 0 here and the exact number is callsTotal. Counts COMPLETED invocations, on the row field's rule." },
                          "cyclesTotal": { "type": "integer", "minimum": 0, "description": "Added 2026-08-21, §11.18 (folded in at adjudication). UNDIVIDED inclusive cycles over the whole sample from this caller — cycles BEFORE the division, not a second measurement of it: when frameCount > 0, cycles == cyclesTotal / frameCount under integer division, so this figure also BOUNDS its partner's truncation. Exact regardless of perFrameExact. Inclusive figures double-count by construction, so this is NOT the term to sum against the row; cyclesSelfTotal is." },
                          "cyclesSelfTotal": { "type": "integer", "minimum": 0, "description": "Added 2026-08-21, §11.18 (folded in at adjudication). UNDIVIDED self cycles over the whole sample from this caller — cyclesSelf BEFORE the division, exact regardless of perFrameExact, bounding its partner's truncation the same way. NORMATIVE SUM: every invocation of a routine has exactly one caller edge, so when callersTruncated is false the edges' cyclesSelfTotal sum to the ROW's cyclesSelfTotal EXACTLY, with no floor slack in either term. It is required for the reason the row's own partner is required (§11.16's third delta) and one the delta could not have anticipated: without it this partition would be a normative sum no suite gate could assert with ==, which is §11.16's own QUIET GAP reproduced one level down — a shortfall of at most frameCount-1 per summed figure reads exactly like agreement." },
                          "callsTotal": { "type": "integer", "minimum": 0, "description": "UNDIVIDED invocations over the whole sample from this caller — calls BEFORE the division, exact regardless of perFrameExact, and the LOUD half of the same pair: a per-frame count is the one figure division routinely DESTROYS rather than merely truncates (§11.16's third delta), and an edge is a smaller denominator than a row, so it destroys counts MORE often, not less. NORMATIVE SUM: when callersTruncated is false, the edges' callsTotal sum to the row's callsTotal exactly." }
                        },
                        "additionalProperties": false,
                        "dependentRequired": { "callerName": ["callerDisp", "callerAddr"], "callerDisp": ["callerName", "callerAddr"] },
                        "if": { "required": ["callerAddr"] },
                        "then": { "not": { "required": ["entryKind"] } },
                        "else": { "required": ["entryKind"] },
                        "description": "ONE call-edge shape. Symmetric with the routine row wherever the two describe the same quantity, deliberately: an asymmetric tuple invites the mis-read this catalog warns about elsewhere. Every divided figure it carries has its undivided partner beside it (cycles/cyclesTotal, cyclesSelf/cyclesSelfTotal, calls/callsTotal), which is the routine row's own discipline at a smaller denominator. It carries no stallCycles — the requesting consumer DECLINED a per-edge stall figure on measured grounds (flat and VBlank-owned at every state they took), so its absence is a demand-side decision rather than an oversight, and the closure below bars the key rather than merely omitting it."
                      }
                    }
```

**(f) The three §2.4 companions, as row siblings:**

```json
                    "callersTotal": { "type": "integer", "minimum": 0, "description": "Added 2026-08-21, §11.18. Distinct call edges observed for this routine, BEFORE maxProfilerCallers or topCallers bounded the list — §2.4 clause (a)'s `total`. The cap is a REPLY bound and not a retention bound, which is what makes this number the true count of callers rather than the count that survived a ceiling. 0 IS THE TYPE'S FLOOR AND NOT AN EXPECTED READING: under this contract every row that exists acquired at least one edge — called gives the caller's edge, an unobserved caller gives `root`, a lost frame gives `depthCap`, an interrupt context gives the cause edge — so an implementer meeting an armed row with an empty edge list should treat it as a defect in the accountant rather than as an ordinary answer." },
                    "callersReturned": { "type": "integer", "minimum": 0, "description": "Added 2026-08-21, §11.18. §2.4 clause (a)'s `returned`: the length of `callers`, restated so a client can compare it against callersTotal without walking the array and so the three numbers are checkable against each other rather than individually plausible." },
                    "callersTruncated": { "type": "boolean", "description": "Added 2026-08-21, §11.18. §2.4 clause (a)'s `truncated`, and REQUIRED even when false — absence and false must not both mean `you have every caller` (§2.3's reason). It is also the guard on both normative edge-versus-row sums: they hold when this is false and are lower bounds when it is true. Per-row rather than per-reply, because one reply legitimately carries some rows complete and others clipped: the bound is applied to each row's edge list independently." }
```

**(g) The row's `dependentRequired`, extended so the four arrive as a set:**

```json
                  "dependentRequired": {
                    "name": ["disp"],
                    "disp": ["name"],
                    "callers": ["callersTotal", "callersReturned", "callersTruncated"],
                    "callersTotal": ["callers", "callersReturned", "callersTruncated"],
                    "callersReturned": ["callers", "callersTotal", "callersTruncated"],
                    "callersTruncated": ["callers", "callersTotal", "callersReturned"]
                  }
```

**(h) §11.16's undivided-four bound, amended in place (M1)** — `contract/protocol.md:1563-1572`. The
amendment falsified this sentence and did not touch it; the draft of this CR then quoted it as intact. Both
halves of that are fixed — the sentence here, and §9 item 5 below:

```markdown
>   **They are not carried on `perFrame[]` rows.** Those hold whole-frame totals with no per-routine
>   breakdown and are already undivided, so there is nothing to un-divide; a total there would be a second
>   spelling of the same number. The four fields lived in exactly two shapes — the routine row and the
>   interrupt bucket — **until §11.18's call edge**, which carries the undivided partners of its own three
>   figures (`cyclesTotal`, `cyclesSelfTotal`, `callsTotal`) one level down, for this behaviour's own
>   reason applied at a smaller denominator. *(Amended 2026-08-21 by §11.18, which is what made the older
>   sentence false; it read "the four fields live in exactly two shapes … a bound on where they may appear,
>   not an omission." The bound that survives is the one that was always the point:* **`perFrame[]` rows
>   carry no undivided partner, ever**, *because a whole-frame total has nothing to un-divide. A shape that
>   divides may publish what it divided; a shape that never divided may not restate it.)*
```

**(i) §2.4's selection rule gains a third case (M2)** — `contract/protocol.md:561-575`, appended to the
*"Two spellings, and which one applies"* paragraph. Without it the contract would instruct the container
spelling in §2.4 and ship the flat one in §6:

```markdown
**And one level deeper: a list that is a field of an ITEM of a container takes the flat spelling scoped to
that item.** *(Added 2026-08-21, §11.18.)* The two spellings above resolve every *field*-list to the
container object, which was right while every field-list sat directly on a result. A policy-bounded list
that hangs off an **item** — `routines.items[].callers`, the registered case — takes clause (a)'s field set
in full, as **prefixed siblings of the item**: `<name>`, `<name>Total`, `<name>Returned`,
`<name>Truncated`, arriving **as a set** and tied that way mechanically, with `<name>Cursor` where a
cursor is supported at all (clause (b) still governs whether one may exist). Nothing is relaxed — the same
three companions are still MUST — and the prefixing is what the flat form needs at this depth, since an
item that already carries its own `total` would collide with a bare one. *Why not simply nest a second
container:* that puts `{items, total, returned, truncated}` inside an item of a container that already
carries them, so a client reaches an element through **three** levels of `.items` and reads the word
`total` at two different scopes in one reply. The nesting buys the same indirection this section already
declined once. *And why this sentence exists rather than one blockquote quietly shipping it:* without it,
§6 would ship the flat form while this paragraph instructed the container form, and the next drafter one
level down would re-litigate against a selection rule the catalog had already outgrown.
```

Beyond these, the amendment reworded prose on **five** existing nodes and nothing else: the schema's
top-level `description` (recount), the three profiler fragments' `$comment`s, and the routine row shape's
own `description`. §6.4 enumerates them by parse.

---

## 6. Counts and controls, all executed

**No `cargo` of any kind was run** — this is a docs-and-schema pass and another lane may hold the serialized
build lock — and **no emulator MCP tooling was used at any point, for any purpose**.

### 6.1 Fragment count, parsed on both sides

```
fragments BEFORE: 37   AFTER: 37
methods top-level keys BEFORE: 38   AFTER: 38
added fragments: []    removed: []
```

`json.load` over `2735ac7` and over `callers-amendment` at `5a7def4`. The count is **unmoved**, which is the
answer §11.18's clause 7 requires to be *parsed* rather than carried — §11.17's merge is the recorded case
of what a carried-forward number is worth, and an unmoved count is exactly the case a drafter is most
tempted to assert.

### 6.2 The params closure, re-swept

```
params objects: 37   OPEN (must be empty): []
handshake initialize params unevaluatedProperties: None
check_schema: OK
fragment halves compiled: 74
```

§11.17's merge lesson, applied to the two params objects this CR edits: the closure is re-derived by parse
rather than assumed to have survived the edit, and `initialize` is confirmed still exempt.

### 6.3 A 43-message harness — 17 accepts, 26 refusals, **43 for 43**

Every message validated against the fragment **closed** (`unevaluatedProperties: false` wrapped at test
time, §8 item 20), with `$defs` resolved through the real document. Refusals are the point: a closure nobody
has watched reject is a closure nobody has tested. **Re-run against the post-ruling schema**, where M3 and
M4 flip validity on four of them: case 22 becomes four probes instead of three, case 24's invalid value is
re-pointed from `"vint"` (which the ruling makes **valid**) to `"interrupt"` (which it removes from the
wire), every edge fixture gains the two undivided partners, and case 28b is new.

| # | Message | Expect |
|---|---|---|
| 1 | `set_profiler{enabled, callers: true}` | accept |
| 2 | `set_profiler{callers: "yes"}` | refuse (type) |
| 3 | `set_profiler{calers: true}` (misspelt) | refuse (§2.5 closure) |
| 4 | `set_profiler` result **with** the `callers` echo | accept |
| 5 | `set_profiler` result **without** it (a no-lens server) | accept |
| 6 | `set_profiler` result `callers: 1` | refuse |
| 7–8 | `get_profiler` result with / without `callers` | accept, accept |
| 9 | `topCallers: 5` | accept |
| 10 | `topCallers: 0` | refuse (below minimum) |
| 11 | `topCallers: "all"` | refuse |
| 12 | `topCaller: 5` (misspelt) | refuse |
| 13 | `top` and `topCallers` together | accept |
| 14 | a row carrying the full four-key set | accept |
| 15 | a row carrying **none** of them (unarmed) | accept |
| 16 | `callers` + `callersTotal` + `callersReturned`, **no** `callersTruncated` | refuse |
| 17 | `callersTotal` alone | refuse |
| 18 | `callersTruncated` alone | refuse |
| 19 | the four keys plus an undeclared `callersLimit` | refuse |
| 20 | edge with `callerAddr`, no `entryKind` | accept |
| 21 | edge with **neither** | refuse |
| 22 ×4 | edge with no `callerAddr` and `entryKind` = `hint` / `vint` / `root` / `depthCap` | accept ×4 |
| 23 | edge with `callerAddr` **and** `entryKind` | refuse (the biconditional's other half) |
| **24** | **`entryKind: "interrupt"`** — the spelling the ruling removed from the wire | **refuse (enum)** |
| 25 | `callerName` without `callerDisp` | refuse |
| 26 | `callerName` + `callerDisp` without `callerAddr` | refuse |
| 27 | `callerName: "Game_Loop+$1A"` | refuse (`$defs/symbolName`'s suffix ban) |
| 28 | edge missing `callsTotal` (both cycle partners present, so the case isolates) | refuse |
| **28b** | **edge missing `cyclesSelfTotal`** — M4's new required key | **refuse** |
| 29 | edge carrying **`stallCycles`** — the field the consumer declined | refuse |
| 30 | `callerAddr: 65280` (a number, not a hex string) | refuse |
| 31 | `cyclesSelf: -1` | refuse |
| 32 | `initialize` result whose `limits` carries `maxProfilerCallers` | accept |
| 33 | `initialize` result whose `limits` omits it (a no-lens server) | accept |
| 34 | `maxProfilerCallers: 0` | refuse |
| **C1** | an **unarmed** `get_profiler_frames` reply, validated at the **pre-amendment** revision | accept |
| **C2** | an **armed** reply, validated at the pre-amendment revision | refuse |
| **C3** | `topCallers: 5` at the pre-amendment revision | refuse |
| **C4** | `set_profiler{callers: true}` at the pre-amendment revision | refuse |
| **C5** | the `set_profiler` `callers` echo at the pre-amendment revision | refuse |

C1 against C2–C5 is the additivity claim demonstrated in both directions at once: what this CR adds is
genuinely new surface (C2–C5 all refuse before it), and a server that never arms the lens is **unaffected by
the amendment** (C1 passes at both revisions). Cases 5, 8 and 33 are the same property for the
capability-conditional keys: a server with no caller lens conforms without them. Case 24 is the one the
ruling asked for by name — *"the word this ruling removed from the wire is the exact string that must now
refuse."*

**One thing this harness does not do, stated because the adjudicator probed it independently and it is
worth having on the record:** it does not gate arithmetic. `callersReturned` ≠ `len(callers)` **accepts**,
as does a 32-bit `callerAddr` (the 24-bit mask is prose-only here exactly as it is on the row's own `addr`).
Both are suite-gate territory, as for every container on this bus; neither is new with this CR.

### 6.4 The additivity proof, executed rather than asserted

With `$comment` and `description` stripped from both revisions:

```
SCALAR leaves BEFORE: 1470   AFTER: 1531
leaves REMOVED: 0
leaves with a CHANGED VALUE: 0
leaves ADDED: 61
PRUNED-new-keys(NEW) == OLD (prose stripped)?  ->  True
```

The last line is the strong form: delete the six new key names (`maxProfilerCallers`, `topCallers`,
`callers`, `callersTotal`, `callersReturned`, `callersTruncated`) wherever they are *declared* in the
amended schema, and the document that remains is **deep-equal** to the pre-amendment one. **M4's two new
edge keys ride inside `callers` and prune away with it**, so the deep-equal form survives the fold-in
unchanged — which the ruling named as the thing to check rather than assume. Nothing existing was touched,
in either direction, by anything but prose.

*(A counting-convention note, because the adjudicator reproduced these figures and got 1471→1525 against
the draft's 1470→1524. The divergence is one node — `get_profiler.params.properties`, an empty object,
which their counter counts as a leaf and this one does not — and it is **isolated to the absolute totals**:
the movement figures, which are what the claim rests on, agree exactly. The convention is stated here so the
next reproduction does not have to rediscover it.)*

The newly declared properties, enumerated by parse — **19**:

| Where | Keys |
|---|---|
| `initialize.limits` | `maxProfilerCallers` |
| `set_profiler.params` / `.result` | `callers`, `callers` |
| `get_profiler.result` | `callers` |
| `get_profiler_frames.params` | `topCallers` |
| the routine row | `callers`, `callersTotal`, `callersReturned`, `callersTruncated` |
| the call-edge shape (new) | `callerAddr`, `callerName`, `callerDisp`, `entryKind`, `cycles`, `cyclesSelf`, `calls`, `cyclesTotal`, `cyclesSelfTotal`, `callsTotal` |

The routine row goes from **11** declared properties to **15**; its `dependentRequired` from **2** entries to
**6**. The edge shape declares **10** properties with **6** required — eight and four before M4's fold-in.

Prose-only edits, by parse — **5 nodes**: the schema's top-level `description`, the `$comment` of each of
`set_profiler` / `get_profiler` / `get_profiler_frames`, and the routine row shape's `description`. The two
protocol-side prose amendments M1 and M2 require are **not** in that figure — they are `protocol.md`, not
the schema, and are shown whole in §5(h)–(i).

---

## 7. Pins and decisions

Eight places where the recon left two readings, or where this CR reaches a **different** answer than the
recon recommended. Each names the alternative it did not take. Four are departures from the recon; **none**
departs from the demand-side answer, which is binding.

### 7.1 The list spelling — *(DEPARTS from the recon, and the departure exists because the recon's recommendation collides with a MUST it did not cite. Presented as an open question in §8.1 rather than settled here.)*

The recon recommends *"a plain array plus a per-row `callersTruncated` boolean"*, objecting — correctly —
that a full §2.4 container inside an item of a container is a third level of `{items, total, returned,
truncated}` scaffolding for a top-N list. What it does not cite is that **§2.4 clause (a) is a MUST**: *"A
list bounded by policy MUST carry `total` and `truncated` … `returned` is required with them."* `callers` is
policy-bounded by construction — `maxProfilerCallers` is a number the server picked. So the recon's
recommendation is not merely "less scaffolding"; taken as written it is a **carve-out from a normative MUST**,
and adopting it silently would be exactly the kind of quiet weakening this catalog's §11.9 exists to record.

This CR takes a **third** answer the recon did not consider, and §2.4 already blesses it: the **flat
spelling**. §2.4's *"Two spellings, and which one applies"* gives a container object for a list that is a
*field*, and a flat form — *"the array under its own name beside `total`, `returned`, `limit`, `truncated`
and `cursor?` as siblings"* — for a list that **is** the whole result, with the note that *"the two spellings
carry the identical field set and the identical rules; only the nesting differs."* Scoping the flat spelling
to a **row** rather than to a whole result satisfies clause (a)'s field set exactly, costs no third level of
`{items: …}`, and is a smaller extension of an existing spelling than a carve-out from a MUST would be.

The one thing that is genuinely new is the *scope* the flat spelling is applied at, and §8.1 put that to
the adjudicator with all three candidate spellings written out, because — in the recon's own words — *"what
must not happen is the question going unasked."*

**Ruled: (γ) adopted, conditional on M2**, and the condition is the half this CR had missed. §2.4's
selection sentence resolves **every** field-list to the container spelling, so a §2.4 reader after this
merge would conclude `callers` must nest and would find §6 shipping otherwise — the contract instructing one
form and shipping another. The widening is therefore **recorded in §2.4 itself** (§5(i)), with the prefixing
(`callersTotal` rather than a bare `total` as a row sibling) named there as part of the row-scoped form,
since an item that already carries a `total` of its own would collide with a bare one. Shipping (γ) on a
blockquote's authority alone would have left the next drafter one level down re-litigating against a
selection rule the catalog had already outgrown.

### 7.2 The `callers` echo is capability-conditional, not REQUIRED — *(a constraint the recon did not reach, resolved here)*

`set_profiler.result` requires `["enabled","perFrame"]` and `get_profiler.result` requires four keys. Adding
`callers` to either `required` list is the natural symmetry and is **barred**: `"profiler": true` is
advertised by a shipped server today, so §11.16's pre-release licence — *"an old-shape reply refusing against
the amended fragment is the wanted behaviour today and would be a breaking change the day after the first
server ships one"* — has expired. A required addition would make every server that does not implement this
lens non-conformant, which is precisely the direction D5's additivity does **not** cover.

The resolution is the catalog's own existing shape for exactly this: `limits.maxWriteLen` and
`maxHashLen` are **schema-optional with a conditional requirement stated in prose** (*"REQUIRED when the
server advertises `emulator/write_memory`"*), because `limits.required` cannot express *required iff the
server implements X*. `maxProfilerRoutines` and `maxProfilerFrames` already ride that same pattern. CR-28
applies it one notch further out: the requirement is conditioned on a **lens** rather than on a method, and
**`limits.maxProfilerCallers`'s presence is the capability signal** — a client branches on it, per D5's *"no
client ever branches on a version integer at runtime — it branches on capabilities."*

The consequence is stated in the fragments rather than left to be discovered: **absence of the `callers`
echo means *this server has no caller lens*, and `false` means *the lens exists and is off*.** Those are
different facts and the wire distinguishes them — §2.3's presence discipline honoured rather than waived.
*The alternative not taken* is a `capabilities.profilerCallers` boolean beside `capabilities.profiler`; it is
a second way to say the same thing, and the limits key has to exist anyway for the bound.

### 7.3 Opt-in for the **reply's** sake — *(sharpens the recon, which gave both reasons without ranking them)*

The recon offers *"not because the accumulator cost is scary (it is not), but because the reply grows by rows
× N sub-objects."* This CR makes the ranking normative text because it **inverts `perFrame`'s** reason —
`perFrame` is armed for memory across a long sample; `callers` is armed for reply size, its accumulator being
proportional to calls rather than instructions. Stating which one applies is what stops a later reader
optimising the wrong half; §11.18's second ★ carries it.

### 7.4 `topCallers` exists, and it is what gives `callersNotArmed` a subject — *(a gap in the recon, filled)*

The recon writes the refusal as *"`get_profiler_frames{...}` refusing `-32005` `callersNotArmed` mirrors
`perFrameNotArmed`"* — with the params **elided**. The elision hides a real question, because
`perFrameNotArmed` is not a refusal of a *read*: it is the refusal of the **`frames` param**, on the stated
reason that *"a parameter that cannot affect the answer is worse than one that is rejected."* A design with
no read-side param has nothing to refuse, and `callersNotArmed` would be a dead code.

So `get_profiler_frames` gains **`topCallers`**, and the mirror is exact in every part: it *bounds* the list
(as `frames` bounds the per-frame list and `top` bounds `routines.items`); it is refused above the advertised
cap with `-32602`, **never clamped**; and sent against an unarmed sample it is `-32005 callersNotArmed`.

**It does not gate presence.** `callers[]` appears **iff armed**, exactly as `perFrame` appears iff armed —
`frames` narrows an armed ring and never conjures one. *The alternative not taken* is a boolean read-side
`callers?` request flag, which would make emission depend on two independently settable gates, put a second
weaker refusal case beside this one, and depart from the `perFrame` precedent the demand accepted this
design *for*.

### 7.5 The refusal is `get_profiler_frames`' alone, and `get_profiler` gains only an echo — *(derived, and it corrects a plausible reading)*

It is natural to assume the caller lens attaches to `get_profiler`, since that is the read whose name says
*profiler*. It does not: `get_profiler` returns `{enabled, perFrame, framesRecorded, routineCount}` and
**carries no routine rows at all** — the rows live on `get_profiler_frames`. So `callers[]` can only appear
on `get_profiler_frames`, `callersNotArmed` is **structurally unreachable** on `get_profiler`, and what
`get_profiler` gains is one echo: `callers`, the third arming fact, beside `enabled` and `perFrame`. That is
in the fragment's own `$comment` so the next reader does not have to re-derive it.

### 7.6 No per-row `limit` sibling — *(a decision §2.4 leaves open, taken)*

§2.4 makes `limit` **OPTIONAL** ("reports the ceiling the server actually applied"). It is not emitted here:
the applied ceiling is **one number for the entire reply** — the client's `topCallers`, or the advertised cap
— so repeating it on every row would be a constant field, which is §2.4's own constant-caveat advisory
(*"documentation wearing signal's clothes"*) in field form. A client that sent `topCallers` already knows it;
one that did not reads `limits.maxProfilerCallers`.

### 7.7 `maxProfilerCallers` bounds the **reply**, not retention — *(a distinction neither source drew)*

`maxProfilerFrames` is a **ring depth**: rows past it are gone, and no `total` can be honest about them.
`maxProfilerCallers` is `maxProfilerRoutines`' kind: the accumulator keeps every observed edge and the cap
decides how many are *sent*. That is what makes `callersTotal` a true count of distinct callers rather than
the count that survived a ceiling — and it is worth pinning, because the two caps sit adjacent in `limits`
and an implementer copying the wrong neighbour produces a `callersTotal` that is quietly a lie.

### 7.8 `callersNotArmed` is registered where `perFrameNotArmed` is, and §5 is not edited — *(derived)*

§5's `-32005` paragraph carries an **open** `data.reason` string with an *e.g.* list
(`machineRunning`, `checkpointCapReached`, `unknownCheckpoint`, `watchCapReached`), and the schema types
`reason` as a plain string with the same three named as examples. **`perFrameNotArmed` is in neither** — it
lives in §6's profiler blockquote and in its own param's fragment description, which is the register this
catalog actually uses for method-local discriminants. `callersNotArmed` is registered the same way, in the
same two places. *The alternative not taken* is extending §5's example list; adding `callersNotArmed` there
while its own sibling `perFrameNotArmed` is absent would make the list look like an enumeration it is not,
and closing `reason` into an enum is a separate bus-wide change nobody asked for.

**Carried from the demand side without reopening** (their answers are binding; §1.4 records them): in-row
`callers[]` over the separate method; the tuple exactly as proposed, `callsTotal` included; **no per-edge
stall**; and `entryKind` required whenever `callerAddr` is absent.

**One attribution stated precisely, because this register's failure mode is true-sounding claims — and the
draft of this paragraph got it backwards, which is ruling M6.** The words *"one absence must not mean three
facts"* are **the consumer's**, as a clause of item 3 of their committed anchor: *"Absence must carry its
reason: when `callerAddr` is absent, a required `entryKind: "interrupt" | "root" | "depthCap"` on the edge —
one absence must not mean three facts."* The draft declared that phrase to be this CR's own compression and
then quoted the anchor containing it verbatim, in a paragraph whose entire stated purpose is attribution
precision. **This CR's** compression is the different sentence in its own title and in §11.18's:
*"one absence stops meaning three things."* The contract text in §3(d) is written against the consumer's
position, generalised off their engine's names — and, per the 8.2 ruling, now distinguishes **four** facts
rather than the three their sentence names, which §8.2 records as an adjudicated departure.

---

## 8. The three questions, as asked and as ruled

Three were put to the adjudicator; **all three are now ruled, two of them against the drafted spelling.**
The questions are kept as they were asked — a register that rewrites its questions to match the answers
stops being a record of what was decided — with each ruling appended to the question it answers.

### 8.1 The bounded-list spelling, one level deeper than this catalog has gone — **three candidates, all written out**

`callers` is a **policy-bounded list that is a field of an item of a container**. §2.4 has never been applied
at that depth. Three spellings are available and the CR is drafted at **(γ)**; swapping to either of the
others is mechanical and the CR's harness cases 16–19 move with it.

**(α) The recon's recommendation — a plain array plus one boolean.**

```json
"callers": { "type": "array", "items": { … } },
"callersTruncated": { "type": "boolean" }
```

*For:* smallest surface; a top-N list of ≤ `maxProfilerCallers` entries is walkable, so `returned` buys
little and `total` is the only genuinely new fact. *Against:* **§2.4 clause (a) is a MUST** and this drops
two of its three required companions. Choosing it is a deliberate carve-out and should be recorded as one,
not slipped in as a simplification.

**(β) The full §2.4 nested container, one level down.**

```json
"callers": {
  "allOf": [{ "$ref": "#/$defs/boundedList" }],
  "properties": { "items": { "type": "array", "items": { … } } }
}
```

*For:* zero new precedent — it is the spelling `routines` and `perFrame` already use, reached by `$ref`, so
a client's container-handling code works unchanged at both depths. *Against:* `{items, total, returned,
truncated}` repeated **per row**; a client reaching an edge writes
`routines.items[i].callers.items[j]` — three levels of `.items` — and the recon's objection lands.

**(γ) §2.4's FLAT spelling, scoped to a row — what the CR is drafted at.**

```json
"callers": { "type": "array", "items": { … } },
"callersTotal": { "type": "integer", "minimum": 0 },
"callersReturned": { "type": "integer", "minimum": 0 },
"callersTruncated": { "type": "boolean" }
```

tied by `dependentRequired` so all four arrive or none does. *For:* §2.4 clause (a)'s **field set satisfied
in full**, no MUST carved out, and no third `.items` — it is the spelling §2.4 already defines, applied at a
scope §2.4 has not yet named. *Against:* §2.4 attaches the flat form to *"a list that **is** the whole
result"*, so applying it to a row is a small widening of when flat applies, and it spends four row keys
where (α) spends two.

**What the adjudicator is being asked:** whether clause (a)'s field set binds a list at this depth (→ β or
γ), and if so whether the flat spelling may be scoped to a row (→ γ) or must nest (→ β); or whether a
top-N list of this size earns an explicit carve-out from clause (a) (→ α), **recorded as a carve-out**.
The CR has no stake beyond wanting the question ruled on deliberately.

> **RULED: (γ) — ADOPTED, conditional on M2.** (α) is declined as a carve-out from a MUST whose two dropped
> facts are *"exactly the facts that make a top-N list checkable against itself"*, with `callersTotal`
> load-bearing here in a way `returned` usually is not — it is the true edge count the reply-bound cap makes
> meaningful (§7.7). (β) is declined on the recon's own objection, which survived contact with the text.
> (γ) satisfies clause (a) in full at a scope §2.4 had not named — **and the naming is the condition**: the
> widening is now recorded in §2.4 itself (§5(i)), prefixing included, because that paragraph's selection
> sentence would otherwise instruct (β) while §6 shipped (γ). See §7.1.

### 8.2 The `entryKind` enum spelling — with the demand side's floor, stated by them

The CR specifies the consumer's own three-value enum, `"interrupt" | "root" | "depthCap"`, and carries it as
the demand-side position because that is what the shape answer instructs. Their **floor** if the spelling
loses — the anchor's wording at `6edb08ef`:

> Floor position if the enum spelling loses adjudication: interrupt-entered edges keyed distinctly from the
> other two, however spelled (interrupt-vs-mainline is a distinction the streaming consumer will actually
> branch on).

*(The relay's fuller phrasing of the same position: "If the adjudicator rules against the enum, the fallback
I need is: interrupt-entered edges keyed distinctly from the other two absence cases, however spelled."
Substance identical; per ruling S1 the source of each wording is named.)*

Two alternatives satisfy that floor, and one of them is arguably better than what is drafted:

- **A split by cause — four values: `"hint" | "vint" | "root" | "depthCap"`.** §6's own strongest profiler rule
  is that *"`interrupts` is keyed by interrupt CAUSE, never by handler address,"* and *"a silently wrong
  number is worse than a missing one"* — yet `entryKind: "interrupt"` **collapses `hint` and `vint`**, the
  exact two things that section refuses to collapse anywhere else. Splitting them is strictly more
  informative, satisfies the consumer's floor twice over, and lets an interrupt-entered edge join directly
  to the bucket it came from. It costs a client that only wants interrupt-versus-mainline one extra value to
  test.
- **`entryKind: "interrupt"` plus a sibling `entryCause: "hint" | "vint"`.** Keeps the consumer's literal
  spelling and adds the cause beside it; costs one more key on the rarest rows.

**A related sub-question the CR did not take a position on:** whether `entryKind` should be *permitted* when
`callerAddr` **is** present, with a fourth value marking the inferred-root edge (§7's caveat: a real
mid-routine address that is not an entry point). The CR bars it — `entryKind` is present **iff** `callerAddr`
is absent, enforced by `if`/`then`/`else` — because nobody asked for it and the caveat is a documented
property rather than an unrepresentable one. It is registered instead as **`F-PROFILER-ROOT-EDGE-KIND`**. If
the adjudicator wants that fact typed, this is the round to say so, since widening the biconditional later is
a shape change and not merely a new value.

> **RULED: AGAINST the drafted three-value spelling, FOR the split by cause —
> `"hint" | "vint" | "root" | "depthCap"`.** This **overrules the consumer's literal proposal** and lands on
> the floor they named themselves, which the split satisfies twice over. The grounds, all four argued in the
> ruling: **(i)** §6's own strongest profiler rule keys interrupt accounting by **cause**, and `hint`/`vint`
> are collapsed **nowhere else** in the family, so a lone `"interrupt"` would be the single place in this
> surface where the two causes are indistinguishable; **(ii)** the cause values **join the edge to its
> bucket by name**, which the blockquote's own *"its caller is a bucket"* sentence makes the natural read
> and which a collapsing value cannot do; **(iii)** widening an emitted enum later is **not additive** — a
> client or vendored validator closed over three values refuses the fourth — so this is the one spelling
> decision that could not be deferred cheaply, the same shape-stability argument this CR makes about the
> biconditional; and **(iv)** the cause is **free at the accountant**, since the recon records the caller
> frame kind as `Interrupt { level }`, so the level is already in hand at the push. The consumer's cost is
> one two-value membership test in place of one equality, and their named branch survives verbatim. **The
> departure is recorded, never absorbed** — here, in §11.18's own ★, and in this document's header — and the
> demand side is pointed at the ruling's 8.2 paragraph rather than asked to re-open.
>
> *Two corrections to the question as drafted, both the ruling's:* it called this alternative a
> **"five-value split"** while listing four values — an arithmetic slip, fixed above. And the
> **sub-question is ruled as drafted**: the biconditional **stands**, the inferred-root fact stays
> documented rather than typed, and `F-PROFILER-ROOT-EDGE-KIND` **stays registered** — nobody asked, the
> mis-render risk is one edge per sample, and this CR's own warning that widening the biconditional later is
> a shape change is accepted as the price of not inventing unrequested surface now.

### 8.3 The tuple's missing undivided partners — **flagged, not reopened**

The banked tuple carries `calls` **and** `callsTotal`, but `cycles`/`cyclesSelf` with **no** undivided
partners. That is asymmetric against the routine row, which carries all four pairs — and the demand's own
stated reason for keeping `callsTotal` was *"symmetry with the routine row matters."*

The consequence is precise and worth having on the record: of the two normative edge-versus-row sums this CR
specifies, the `callsTotal` one is checkable **exactly** and the `cyclesSelf` one only **up to floor error** —
and `cyclesSelf` is the field the consumer named as *"the money field for my burst work."* §11.16's own
argument for publishing the undivided set applies verbatim one level down, and with more force: *"A `==` gate
on a count fails loudly … a `==` gate on cycles fails quietly,"* and an edge is a **smaller denominator** than
a row, so its divided figures are floored more often, not less.

**This is flagged and not reopened.** The consumer declined per-edge **stall** explicitly and on measured
grounds; they were **not** asked about undivided cycle partners and declined nothing there. Adding
`cyclesTotal`/`cyclesSelfTotal` to the edge tuple would be additive under D5 and is registered as
**`F-PROFILER-EDGE-UNDIVIDED`**. The adjudicator may fold it in now (two more optional-by-arming integers,
no shape change, and it makes clause 4 of the adoption condition exactly checkable rather than
bound-checkable) or leave it to the first client or gate that cannot be written without it. The CR's own
preference, stated so it can be overruled: **fold it in**, because the alternative ships a normative sum that
no suite gate can assert with `==`.

> **RULED: FOLD IN** — this CR's own stated preference, adopted. `cyclesTotal` and `cyclesSelfTotal` join
> the edge tuple, **required** on the edge as `callsTotal` already is. The grounds: the CR ships a
> **normative sum** no suite gate could assert with `==`, and its own adoption clause 4 had to relax to a
> bound — *"§11.16's quiet gap reproduced at the exact level the CR quotes the argument from"*, on the field
> the consumer named as the money field. The demanded **symmetry principle argues for it**; their *"nothing
> missing"* answered a question that never put undivided cycle partners on the table; and the one explicit
> decline, per-edge stall, is untouched.
>
> **Required-on-the-edge is safe where required-on-the-reply was not**, and the distinction is worth
> keeping: §7.2's licence-expiry constraint bars a required addition to a shape a server has already
> shipped, and **the edge shape has never shipped**, so §11.16's expired licence does not reach inside it.
> With the partners in, **both** edge-versus-row sums are exact under `callersTruncated: false`, clause 4
> gates with `==` in both, and **`F-PROFILER-EDGE-UNDIVIDED` dissolves into this record** rather than
> waiting for the first gate that could not be written — recorded as dissolved in §11.18, not deleted,
> because a register that quietly drops its own debts stops being evidence.

---

## 9. What does **not** change

1. **Every existing key of all three profiler methods**, in value and in meaning: the aggregate rows, both
   interrupt buckets, the reconciliation identity, `unattributedCycles`, `abandonedFrames`, `depthExceeded`,
   `perFrameExact`, `budgetPct`/`budgetPctOmitted` and the undivided set.
2. **`routines` and `perFrame` keep §2.4's nested container spelling.** The 8.1 ruling's widening adds a
   **third** case for lists one level deeper and re-spells neither of them; re-spelling a shipped container
   was never on this CR's table. *(§2.4 itself does move, per M2 — a sentence added, none rewritten.)*
3. **The rows-keyed-by-entry-address pin**, which is the one thing the rejected `(addr, caller)` re-key would
   have broken.
4. **The interrupt buckets take no `callers` list.** A bucket is keyed by cause and what it preempted is not
   a call edge. The interrupt *side* of the relationship is carried instead by `entryKind: "hint"`/`"vint"`
   on a routine row's edge, which is where the fact is actually useful — and, after the 8.2 ruling, keyed by
   the same cause the bucket is, so the edge and the bucket join by name.
5. **`perFrame[]` rows take no `callers` list either**, on §11.16's own reasoning: whole-frame totals carry
   no per-routine breakdown, so there is nothing for a caller to attach to.

   **★ And here the draft of this CR asserted something false, which is ruling M1.** It went on to quote
   §11.16's *"the four fields live in exactly two shapes — the routine row and the interrupt bucket — and
   that is a bound on where they may appear, not an omission"* and to say **"it holds here unchanged."** It
   does not hold. The edge tuple carries `callsTotal` — one of the named four — in a **third** shape, and
   after M4's fold-in it carries `cyclesTotal` and `cyclesSelfTotal` there too. The amendment falsified a
   normative sentence it never touched, and this document then cited that sentence as intact; in a register
   whose named failure mode is true-sounding claims cited later as settled fact, that is the pairing that
   must not ship. Per M1 the sentence is **amended in §11.16 itself** (§5(h)) to name the third shape, and
   what survives of the bound is stated there in the form that is actually true: **`perFrame[]` rows carry
   no undivided partner, ever** — a shape that divides may publish what it divided; a shape that never
   divided may not restate it. Item 5's own claim above — that `perFrame[]` rows take no callers list — is
   the part that was always sound, and it is all that is claimed here now.
6. **§8's conformance checklist gains no item.** Item 22 is bus-wide policy; this is one family's second
   lens. A checklist that grows with every feature stops being a checklist.
7. **§5's error table and the schema's `reason` description.** See §7.8.
8. **No currency in the reference repo moves as a consequence of this text.** CR-28 is contract and schema
   only. The implementation slice's currency argument is expected-zero by construction — an unarmed reply is
   the reply the server already sends — but that is a claim for the slice to make with a run, not for this
   document to make without one.
9. **The vendored schema at `crates/oracle-aether/tests/contract/bus-protocol.schema.json` is deliberately
   untouched.** It re-vendors from the empyrean source at the implementation's merge window; editing it now
   would fork the vendored artifact from its source and turn
   `the_vendored_schema_is_byte_identical_to_the_upstream_contract` red in between. §11.15's delta, CR-26 and
   CR-27 all recorded the same reasoning.

---

## 10. Claims, anchors, and what could not be verified

**Verified firsthand for this CR** — read in the file named, in this session, at the revision named:

| Claim | Anchor |
|---|---|
| The demand's three positions, the declined per-edge stall, and the floor position — **and, per S1, which document each quotation's wording comes from** | aeon `docs/benchmarks/streaming/CR28-DEMAND.md` @ `6edb08ef` (`git show`), cross-read against `oracle/docs/2026-08-21-cr28-shape-answer.md` @ `5b42312`; the two differ in wording on the floor sentence and agree on every position |
| The recon's shape (ii), the three absence edges, the nested-container open question, the CR-28-not-a-delta sizing | `oracle/docs/2026-08-19-streaming-asks-recon.md:448-574` |
| The three §6 profiler rows and the twelve-behaviour blockquote as they stand pre-amendment | `empyrean/contract/protocol.md:1442-1444`, `:1446-1670` @ `2735ac7` |
| §11.16's undivided-four bound, **unchanged from `main` and falsified by this amendment** (M1) | `protocol.md:1548` @ `2735ac7`, `:1549` on the pre-ruling branch — the draft's four protocol hunks did not touch it; amended text at `:1563-1572` on `7c4b9fc` |
| §11.17 is the last amendment entry; `protocol.md` is 3457 lines on `main` and **3701** on the post-ruling branch, with §11.18 at `:3548` | `grep`/`wc -l` on both revisions; the **+90** offset counted after M1's and M2's insertions, not adjusted — it was +56 before them |
| §2.4's two spellings, clause (a)'s MUST, clause (b)'s no-cursor rule, clause (d)'s dichotomy, the constant-caveat advisory | `protocol.md:456-559` (§2.4 whole), bounded-list rule at `:497-559` |
| §2.3's *"0 is the answer, not the absence of one"* presence discipline | `protocol.md:438-454` |
| §5's `-32005` paragraph and its open `data.reason` example list; `perFrameNotArmed` absent from it | `protocol.md:768-805` @ `2735ac7`, the `reason` examples at `:795-799`; `grep` for `perFrameNotArmed` → `:1631`, `:1646`, `:3276` only, none of them in §5 |
| §11.16's pre-release licence, its named expiry, and the eight-fields-in-twelve-places enumeration | `protocol.md:3240-3270` |
| §11.17's clause 7 (count parsed, never carried) and its merge-time extension (closure parsed too) | `protocol.md`, §11.17's adoption paragraph |
| `limits.required` is the three base keys (`:235`), so `maxWriteLen`/`maxHashLen`/`maxProfilerRoutines`/`maxProfilerFrames` (`:241-244`) are already schema-optional with prose-conditional requirements | `bus-protocol.schema.json:233-247` @ `2735ac7`, parsed |
| The reference server advertises `"profiler": true` and has handlers for all three methods | `oracle/crates/oracle-aether/src/engine.rs:1034`, `:187-201`, `:2092`, `:2158`, `:2188` @ `5b42312`; `git log -S '"profiler": true'` → `1185625` |
| `routines.items[]`'s pre-amendment shape: 11 declared properties, `dependentRequired {name,disp}`, `additionalProperties: false` | parsed at `2735ac7` |
| 37 fragments before and after; 37 params objects all closed; `initialize` exempt; `check_schema` passes; 74 fragment halves compile | executed, §6.1–6.2 |
| 0 leaves removed, 0 leaf values changed, **61** added (54 before M4); pruning the six new key names yields a document deep-equal to the pre-amendment one, **and still does after the fold-in** | executed, §6.4 |
| The **43**-message harness, **17** accepts and **26** refusals, 43 for 43, including 5 pre-amendment controls — re-run against the post-ruling schema | executed, §6.3 |
| **19** newly declared properties (17 before M4); routine row 11 -> 15; row `dependentRequired` 2 -> 6; edge shape **10** properties with **6** required | parsed after the ruling's edits, §6.4 |

**Claims this CR does not make.**

- **It does not claim the reference server implements any of this.** Nothing under `crates/` is touched; the
  handler is a separate slice, and the contract text is drafted to merge in the same window as it.
- **It does not re-verify the recon's accumulator trace.** §1.3's cost claim is the recon's, anchored at
  `profiler-s1` `b46bbbc`, and the contract text does not depend on it.
- **It does not claim a test count, and no `cargo` was run.** This is a docs-and-schema pass and another lane
  may hold the serialized build lock.
- **It does not claim any behaviour of a live server.** **No emulator MCP tooling was used at any point.**
  Anything wanting runtime confirmation — that an unarmed reply really is byte-for-byte what the server sends
  today, that `perFrameExact` really flips under a real ROM — is **TAGGED for the controller** and is
  written into §11.18's adoption clauses (5) and (6) as gates rather than asserted here.
- **It does not claim `callersTotal` is cheap to keep truthful.** §7.7 pins the *contract* meaning (the cap
  bounds the reply, not retention); whether an unbounded edge map is acceptable in a long sample is an
  implementation question the slice must answer, and if it cannot, the honest move is an amendment changing
  the meaning of `callersTotal` — not a server that quietly reports the capped count.

**Nothing is BLOCKED**, and no SHOULD was skipped: S1 is applied in §1.4 and §8.2, S2 and S3 in the schema's
`callersTotal` and `set_profiler.callers` descriptions, S4 as a named non-reference-executable check beside
§11.18's demand-side protocol.

---

## 11. Verification note

**Docs and schema only.** In `oracle`, one file under `docs/` — this one — on branch `cr28-callers`, cut
from `main` at `5b42312`; nothing under `crates/`. In `empyrean`, two files (`contract/protocol.md`,
`contract/schema/bus-protocol.schema.json`) on branch `callers-amendment`, cut from `main` at `2735ac7`,
**two** commits — the draft, then this ruling's M1–M5 and S2–S4 — **DRAFT, not merged, not pushed**; worked
in a **separate worktree**, because the main empyrean tree carries another session's live uncommitted edits.

**One drift recorded rather than discovered at merge.** `empyrean` `main` advanced during this pass —
`2735ac7` → `7ddfcb9`, a parallel session's wiki and plan work — and **`git diff 2735ac7 main -- contract/`
is empty**, so this branch sits on an unmoved contract and every coordinate above still holds. That is a
live condition, not a settled one: it must be re-checked at the merge window, and §11.18's clause 7 exists
because the last two amendment branches met each other at exactly one line of this file.

**Executed, and re-executed after the ruling:** `json.load` over both schema revisions;
`Draft202012Validator.check_schema` on the amended document; a standalone compile of all 74 fragment halves;
a params-closure sweep over all 37 fragments; the **43**-message harness of §6.3 against the amended and
pre-amendment revisions; and the prune-and-compare additivity proof of §6.4. **Not executed:** anything at
all in Rust, and any emulator tooling.

**What the re-run moved, and what it did not.** M3 and M4 change validity on four harness cases and add one,
so the harness was rewritten rather than re-asserted: every edge fixture gained the two undivided partners,
case 22 became four probes, case 24's invalid value moved from `"vint"` — which the ruling makes **valid** —
to `"interrupt"`, and case 28b is new. **43 for 43.** The counts that moved are the property total
(17 → 19), the edge shape (8/4 → 10/6), and the leaf figures (54 → 61 added); the ones that did not are the
fragment count (37, still), the closure sweep (37 closed, `initialize` exempt), the leaf figures that carry
the claim (**0** removed, **0** changed), and the prune-to-deep-equal control, which the ruling specifically
flagged as a thing to re-run rather than assume — M4's two keys ride inside `callers` and prune with it, so
it survives, and had it not, that would have been a finding rather than a formatting problem.

Every verbatim block in §3, §4 and §5 was **re-extracted** mechanically from the post-ruling commit rather
than retyped, and every coordinate this document cites was recomputed from that extraction.

**The block accounting, corrected per ruling M7.** The draft's verification note said *"of the 16 fenced
blocks here, 13 for 13 …"* and named only two classes. The document held **19** fenced blocks, not 16: the
sentence had silently forgotten the **three execution-output blocks** in §6.1, §6.2 and §6.4. One digit, in
the one place a document must not drift — its own self-audit — which is CR-27 M2's class.

After the ruling's edits the count is **21 fenced blocks, in three named classes**, checked by fence count
and by substring match against the branch:

- **15 verbatim extracts** (§3(a)–(d), §4, §5(a)–(i)) — **15 for 15 byte-identical** to
  `callers-amendment` at `7c4b9fc`. Fifteen rather than thirteen because M1's amended §11.16 bound and M2's
  §2.4 widening are new blocks.
- **3 candidate-spelling sketches** (§8.1's α, β, γ) — deliberately elided, with `{ … }` standing in for the
  edge shape. Not extracts, not labelled as such, and the substring check correctly reports them as
  non-matching.
- **3 execution-output blocks** (§6.1, §6.2, §6.4) — this run's stdout, not contract text.

The substring check's scope is now stated with it, which is the other half of M7: it matches
` ```markdown ` and ` ```json ` fences against the two branch files, so the three bare-fenced execution
blocks were never in its denominator and the draft's "13 for 13 of 16" conflated two different totals.
