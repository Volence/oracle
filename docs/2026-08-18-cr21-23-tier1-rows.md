# CR-21 / CR-22 / CR-23 — the Tier 1 rows: `write_memory`, `reset`, `memory_hash`

**Status: PROPOSED 2026-08-18, awaiting adjudication.** One document, three CRs — the §11.5 precedent,
where CR-13 and CR-14 shared one amendment because they were found by one pass. These three are the same
shape: found by one gap analysis (`docs/2026-08-17-aeon-switchover-gap-list.md`, Tier 1), unblocking one
consumer (the Aeon side's `ab_runner` re-pointing at `oracle-next`), and landing as one amendment
(§11.13) if adopted.

**Sequencing.** Contract-first, per the 2026-08-17 owner ruling: §6 row and schema fragment before any
handler. That ruling cites the measured discipline — of 21 methods audited, the only ones with zero
undocumented result keys were those specified before they were implemented (gap-list §"Assessment").
`memory_hash` additionally *requires* this order: it has no row anywhere in `empyrean`, and §8 is
explicit that the emulator side must not *"invent new ops not in this spec"* — deviations are raised as
*"change requests against this file, not implemented unilaterally — the contract leads"*
(`protocol.md:1440-1443`). This document is that raising.

Every line anchor below was read in the file it names on 2026-08-18; every quoted sentence exists
verbatim at its anchor. The proposed row / prose / fragment text is copied verbatim from the committed
plan (`docs/superpowers/plans/2026-08-18-aeon-tier1-bus-methods.md`, Task 3), so the adjudicator reads
exactly what would land.

---

## CR-21 — `emulator/write_memory`: a row that predates every rule now governing it

### What exists today

One table row, `protocol.md:825`, legacy vintage:

```
| `emulator/write_memory` ← `write` | `addr`\|`symbol`; `bytes`\|(`value`+`width`) | `addr`, `len` *(68k RAM `$FF0000–$FFFFFF` only)* |
```

That is the entire specification. Measured against the rules the contract has grown since:

- **No prose.** The memory table's blockquote (`protocol.md:829` onward) specifies `emulator/read` in
  five normative bullets; `write_memory` has none.
- **No schema fragment.** The vendored `bus-protocol.schema.json` holds 29 method fragments (counted
  2026-08-18 — see the close below); `emulator/write_memory` is not among them, so §8 item 20's
  closed-result conformance cannot reach it.
- **Untyped `width`.** The row says `value`+`width` and never says `width` ∈ {1, 2, 4}, what happens
  when `value` overflows it, or what byte order multi-byte values land in.
- **No refusal code.** *"(68k RAM `$FF0000–$FFFFFF` only)"* names a bound with no error code and no
  refuse-vs-clip rule — the exact gap CR-4 closed for `run_to` and §11.10's item-19 work closed
  elsewhere.
- **Silent on the run-control state rule.** `protocol.md:793-801` names `run_to`, `run_to_scanline`,
  `run_frames`, `step*`, `press`, `play_input` and `reload_rom` as paused-machine-only, closing with:
  *"Leaving them unnamed would let one server refuse and another accept, both conforming."* `write_memory`
  is unnamed, so today that split is exactly what the contract permits for it.

**The ruling that opens this CR.** Whether the keep-dead register's "register-write op" entry covered
memory writes *"was never ruled"* — the owner ruled it on 2026-08-17
(`docs/2026-08-17-aeon-switchover-gap-list.md`, §"Assessment — two collisions with standing rulings"):
**"ADOPT, scoped. The keep-dead entry covers register writes only, not memory writes. `emulator/write_memory`
is to be built contract-first"** (quoted from lines 73-74; the ruling's own words are *"Owner ruling,
2026-08-17: ADOPT, scoped"*). So adoption is already decided; this CR exists to make the row specifiable
before the handler, per that same ruling.

### The proposed row, prose, and rule change (verbatim, plan Task 3 Steps 2-3)

Replace `protocol.md:825` with:

```markdown
| `emulator/write_memory` ← `write` | `addr`\|`symbol`; `bytes`\|(`value`+`width` 1\|2\|4); `len` ≤ `limits.maxWriteLen` | `addr`, `len` |
```

and add to the memory table's blockquote (alongside the `emulator/read` prose):

```markdown
> - **`emulator/write_memory`** is the poke primitive. Scope: the work-RAM window
>   `$E00000–$FFFFFF` (mirror-masked, exactly the window `read`'s `bus` space serves) — a base
>   outside it, a range whose **end** runs past it, or any ROM/IO target is `-32004`, **refused,
>   never clipped**. Exactly one payload spelling: `bytes` (hex string, even digit count) or
>   `value`+`width` (`width` ∈ 1|2|4; `value` must fit `width`); both, neither, `width` with
>   `bytes`, or `value` without `width` is `-32602`. Multi-byte values land **big-endian**, as the
>   68000 stores. Writes travel the bus path (hardware mirror masking), not a buffer poke.
>   Requires a **paused** machine (see the run-control state rule) — strict by design: relaxing a
>   refusal later is additive (D5); introducing one is not.
```

In the run-control state rule (`protocol.md:793-806`), extend the named list: `run_to`,
`run_to_scanline`, `run_frames`, `step*`, `press`, `play_input`, `reload_rom` **and `write_memory`**, and
append one sentence to the *why*: *"`write_memory` is named for `press`'s reason — a poke mid-free-run
mutates the timeline just as surely, and leaving it unnamed would let one server refuse and another
accept, both conforming."*

### The proposed schema fragment (verbatim, plan Task 3 Step 6)

```json
"emulator/write_memory": {
  "$comment": "protocol.md §6 (memory), specified 2026-08-18 by §11.13 (CR-21). The poke primitive. Work-RAM window $E00000-$FFFFFF only, refused (-32004) never clipped; exactly one payload spelling (bytes XOR value+width, else -32602); requires a paused machine per the §6 run-control state rule (-32005 machineRunning). Values land big-endian. The keep-dead 'register-write op' entry was ruled (2026-08-17) to cover register writes only.",
  "params": {
    "type": "object",
    "properties": {
      "addr": { "$ref": "#/$defs/hex", "description": "First byte to write. D9 category 1." },
      "symbol": { "allOf": [{ "$ref": "#/$defs/symbolName" }], "description": "Resolved to an address (D7); alternative to addr." },
      "bytes": { "$ref": "#/$defs/hex", "description": "Byte payload, even digit count. Alternative to value+width; passing both is -32602." },
      "value": { "type": "integer", "minimum": 0, "maximum": 4294967295, "description": "Numeric payload; must fit width. Requires width." },
      "width": { "enum": [1, 2, 4], "description": "Byte width of value. Valid only with value." }
    }
  },
  "result": {
    "allOf": [{ "$ref": "#/$defs/replyFields" }],
    "required": ["addr", "len"],
    "properties": {
      "addr": { "$ref": "#/$defs/hex", "description": "Echoed resolved base address." },
      "len": { "type": "integer", "minimum": 1, "maximum": 4096, "description": "Bytes written. Bounded by limits.maxWriteLen." },
      "caveat": { "type": "string", "description": "protocol.md §2.4. Conditional, never constant." }
    }
  }
},
```

### The demand evidence

- **Three committed Aeon scenes poke.** `aeon/tools/scenes/` holds exactly three scene files
  (`effects_raster_mid_band.json`, `effects_raster_suppressed.json`, `effects_raster_above_screen.json`),
  each with `poke` steps (`{"poke": {"symbol": "Debug_Scene_Freeze", "value": 1, "width": 1}}` and
  the like — note the scenes already speak the `value`+`width` spelling). The gap list records this as
  *"their stated blocker"* for the pixel gate.
- **The bus-path poke already runs in this tree.** `crates/oracle-replay/src/runner.rs:605-609` arms a
  replay by `sys.mega_bus(&mut sink)` + `write16`/`write8` with `FC_SUPERVISOR_DATA` (`= 5`, defined at
  `runner.rs:23` with the reasoning: *"what the guest's own `move` to a RAM cell carries, and what the
  poke should therefore look like on the bus"*). The handler is a generalization of code that exists,
  not a new capability.
- **The legacy MCP row has declared this exact shape since the MCP's first commit.**
  `/home/volence/sonic_hacks/oracle/linux-port/mcp/oracle_mcp.py:305-316` declares `bytes` as
  *"Hex string, even length"* OR `value`+`width` *"1, 2, or 4"* — and `git log -S` dates that
  declaration to `1836a09`, 2026-04-19, the commit that created the MCP server. Four months of a
  stable, client-visible shape. (An earlier draft of this evidence said "for years"; the git history
  says four months, and the corrected number is the one recorded.)

### Open questions: none — pins 1-4, each with its reasoning

1. **Window = work RAM `$E00000–$FFFFFF`** (the mirror window `debug_read` already serves), not the
   row's legacy `$FF0000–$FFFFFF`: an address you can `read` you can write, and watch hits can report
   mirror addresses — a client handed a mirror coordinate must be able to hand it back, which is
   §11.12's own argument for `emulator/read`. Refusal `-32004`, refused-never-clipped, ROM/IO refused.
2. **Requires a paused machine** (`-32005`, `data.reason="machineRunning"`), named in §6's run-control
   state rule. Strict-first: relaxing a refusal later is additive (D5); adding one later breaks clients.
   The rule's own closing sentence (`protocol.md:800-801`) is the reason it must be *named* rather than
   left to server taste.
3. **Exactly one payload spelling**: `bytes` (hex string, even digits) XOR `value`+`width`
   (width ∈ {1,2,4}, value must fit, big-endian as the 68000 stores). Both, neither, `width` with
   `bytes`, or `value` without `width` → `-32602`. Cap `len ≤ limits.maxWriteLen` (4096, mirroring
   `maxReadLen`). This is the legacy MCP row's shape made total — every ambiguous combination gets a
   defined refusal instead of an implementation-defined guess.
4. **Writes go through the bus path** — `mega_bus().write8(...)` with FC=5 (supervisor data), byte at a
   time. Hardware mirror masking for free; no `ram_mut()` debug back door added to core. The replay
   runner already took this route for the same reason (`runner.rs:22-23`).

---

## CR-22 — `emulator/reset`: a result key two servers share and no text defines

### What exists today

One table row, `protocol.md:783`:

```
| `emulator/reset` | — | `deferred` |
```

`deferred` appears **nowhere else in the contract** (checked by grep 2026-08-18: the word's only §6
occurrence is this row). No prose says what `true` or `false` means, no schema fragment types it, and
§5 already anticipates the method can fail by timeout — `-32010` is glossed *"operation timed out (e.g.
`reload_rom`, `reset`)"* (`protocol.md:731`) — which is more than the result itself gets.

Meanwhile **the reference server serves it**: `oracle/linux-port/gui/ControlSocket.cpp`'s `OpReset`
(from `:459`) emits `.addBool("deferred", false)` at `:469` (the fallback path — reset applied inline
before the reply) and `.addBool("deferred", true)` at `:492` (the main-thread hand-off path — reset
queued and drained on the GUI thread, with a 5-second deadline that returns an error on expiry). So the
key is not speculative: two distinct truthful values are already emitted by a shipping server, and a
second server has no contract text telling it which to say.

**This repo's own books flagged the gap.** `docs/2026-08-15-mcp-as-aether-client-validation.md:144-145`:
*"**`reset` is the conspicuous absence** — the most basic control op on the machine, missing from a
25-method control surface."* And the player has had reset the whole time — Tab (with F1 as hidden
alias), `crates/oracle-frontend/src/commands.rs:102-108`, handled at
`crates/oracle-frontend/src/main.rs:1369` (`sys.reset()`), which under D15's parity discipline is a live
gap: a GUI surface with no bus method behind it.

### The proposed prose (verbatim, plan Task 3 Step 1 — the row at `:783` stays as-is)

In the run-control table's blockquote region (after the run-control state rule, before the checkpoints
group), add:

```markdown
> **`emulator/reset`** drives the machine's `/RESET` sequence against the current cartridge.
> Deliberately **not** subject to the state rule above, for the checkpoint methods' reason: it
> advances nothing and replaces state wholesale between frames, so it cannot fight a free-run loop.
> `deferred` reports *when* it landed — `false`: applied before this reply; `true`: queued for the
> next frame boundary. Both are conformant; a server answers with the one its threading makes
> honest, and a client that needs the reset visible waits for a reply with `deferred: false` or one
> subsequent frame. The master clock restarts from 0, so §2.2 stamps on later replies jump
> backwards — that is the reset, not a bug. Battery SRAM contents survive (as on real hardware);
> loaded symbols survive (the image is unchanged — contrast `reload_rom`); checkpoints and
> watchpoints survive; held pads clear. Can time out (`-32010`).
```

### The proposed schema fragment (verbatim, plan Task 3 Step 6)

```json
"emulator/reset": {
  "$comment": "protocol.md §6 (run-control), result defined 2026-08-18 by §11.13 (CR-22) — the row predates the schema and its `deferred` key was never defined. NOT subject to the run-control state rule (the checkpoint precedent: replaces wholesale, advances nothing). The master clock restarts at 0, so stamps on subsequent replies jump backwards by design.",
  "params": { "type": "object" },
  "result": {
    "allOf": [{ "$ref": "#/$defs/replyFields" }],
    "required": ["deferred"],
    "properties": {
      "deferred": { "type": "boolean", "description": "false: the reset was applied before this reply. true: it is queued for the next frame boundary. Both conformant — a server answers with the one its threading makes honest." }
    }
  }
},
```

### The demand evidence

- The Aeon gap list, Tier 1 item 2: *"scene preambles need a cold start"* — and each of the three
  committed scenes opens with `"reset": true`.
- The reference server implements it today (`ControlSocket.cpp:459-493`), the legacy MCP offers it as a
  tool (`oracle_mcp.py:134`), and the player binds it to Tab/F1 — three of the four surfaces exist,
  and the bus row is the one absent, which is D15 parity inverted.
- The 25-method-surface quote above, from this repo's own MCP-as-client validation.

### Why the row must *define* `deferred` — the conformance argument, spelled out

The two servers give different truthful answers by construction. Oracle's threading forces `true` in its
normal path (`:492`) because the reset drains on the GUI thread after the reply is composed; our engine
thread applies it before replying, so the honest answer is `false`. If the contract leaves `deferred`
undefined, either (a) one of those servers is retroactively wrong when a definition finally lands, or
(b) a client invents its own reading — the *"third answer that must be kept equal to the other two by
hand"* failure §6's count note names. Defining it as *when the reset landed* — with **both values
conformant** and a stated client recipe for each — is the only definition under which both shipping
behaviours are legal and a client can act on the answer. That is CR-8's (`droppedEvents`) lesson applied
before the fact rather than retroactively: a key two servers put on the wire belongs in the contract
before a third server has to guess.

### Open questions: none — pins 5-6, each with its reasoning

5. **`reset` is NOT run-state gated** — the checkpoint-block precedent, quoted from
   `crates/oracle-aether/src/engine.rs:2017-2029`: checkpoint methods are *"Deliberately NOT
   `require_paused`"* because none of them *advances* the machine — *"they read or replace the machine
   wholesale, on the engine thread, between frames"*. A reset is the same shape: it advances nothing and
   replaces state wholesale between frames, so it cannot fight the free-run loop, and §5's ban on
   implicit mode changes means gating it would force a pause-call-resume dance the call never needed.
   Result `deferred: bool` defined honestly: `false` = applied before the reply (our server, always),
   `true` = queued for the next frame boundary (Oracle's behaviour while free-running). Both conformant.
6. **`reset` semantics on the wire**: master clock restarts at 0 (stamps jump backwards — stated in
   prose so a stamp-watching client does not file the jump as a bug); SRAM contents survive (as on real
   hardware — the player's own reset flushes the pending `.srm` first, `main.rs:1364-1368`); loaded
   symbols survive (the image is unchanged — unlike `reload_rom`, whose row reports `symbolsDropped`);
   checkpoints and watchpoints survive; held pads clear; `rom_generation` bumps (the `restore`
   precedent, so a hosted player resyncs). Each of these is a question a second implementer would
   otherwise answer by reading our source, which is the definition of a contract gap (§11.1, second
   sitting).

---

## CR-23 — `emulator/memory_hash`: the genuinely new row, raised before it is built

### What exists today

**Nothing.** `grep -rn memory_hash /home/volence/sonic_hacks/empyrean/` returns no matches (run
2026-08-18). No §6 row, no schema fragment, no deferral entry. Unlike CR-21 and CR-22 — both of which
amend rows that exist — this is a new op, and §8's rule reaches it directly: *"invent new ops not in
this spec"* is the first item on the must-not list (`protocol.md:1440`), so the row must exist before
the handler does. Hence this CR.

**The de-facto spec is the legacy MCP row**, `oracle/linux-port/mcp/oracle_mcp.py:239-254`, backed by a
socket op landed 2026-08-01 (`1c54004`, *"memory_hash op — parametric bus-memory content hash"*). Its
declared behaviour, quoted from the tool description: fnv1a64 *"(same family as state_hash)"* + crc32
*"(zlib/`crc32`-compatible, so a cart-window hash matches CRC32 over the ROM-file slice)"*; *"no
4096-byte cap"* with `len` max 4194304; *"Auto-routes RAM ($FF0000-$FFFFFF) vs cart ROM (<$400000)
exactly like read_memory"*; and *"A range that runs past the target device's last byte is rejected,
never wrapped or clamped."* The proposal below is that shape, ported to this contract's conventions
(D9 hex spellings, `limits.*` advertisement, `-32004`).

**CR-20 already anticipated this method and deliberately left it out.** Its ruling on question 3
(`docs/2026-08-16-cr20-unified-read.md`): *"**`memory_hash` is struck from this CR** — digest-shaped,
with its own obligations (the algorithm must be pinned as `state_hash`'s is, since cross-server
comparability is its whole point)."* This CR is the discharge of that obligation: both algorithms pinned
(FNV-1a-64 = `state_hash`'s family; CRC-32 = IEEE/zlib), and — per the same ruling's reservation — it
does **not** take the `space` vocabulary, because it is not adopted with multi-space reach: it routes
exactly as `read`'s `bus` space and nothing else.

### The proposed row and prose (verbatim, plan Task 3 Step 4)

In the memory table, directly under `emulator/read`:

```markdown
| `emulator/memory_hash` | `addr`\|`symbol`, `len` (required, ≤ `limits.maxHashLen`) | `addr`, `len`, `region`, `fnv1a64`, `crc32` |
```

and its blockquote entry:

```markdown
> - **`emulator/memory_hash`** fingerprints a byte range without moving it — no byte payload
>   crosses the wire, so large ranges are cheap where `read` caps at `maxReadLen`. A **pure read**
>   (not subject to the run-control state rule). Routes exactly as `read`'s `bus` space: work-RAM
>   window or cartridge ROM, `-32004` past the end, refused never clipped. `fnv1a64` is
>   `state_hash`'s family (16-hex, `$defs/hash64`); `crc32` is IEEE/zlib CRC-32 (8-hex,
>   `$defs/hash32`), chosen so a cartridge-window hash equals CRC32 over the same slice of the ROM
>   file. Fills the gap `state_hash` leaves — its five fingerprints cover VDP state only; nothing
>   else on this bus hashes 68000 memory. Distinct from §9's deferred `frame_hash`: that is a
>   picture hash, already served by `state_hash`'s `includeFramebuffer`.
```

### The proposed `$defs/hash32` (verbatim, plan Task 3 Step 5)

```json
"hash32": {
  "type": "string",
  "pattern": "^0x[0-9A-Fa-f]{8}$",
  "description": "A CRC-32 (IEEE 802.3 / zlib polynomial) fingerprint, spelled as a hex string of exactly 8 digits (D9 category 1). Fixed width for hash64's reason: a fingerprint is compared, never computed on, and a 7-digit answer is a dropped leading zero."
},
```

### The proposed schema fragment (verbatim, plan Task 3 Step 6)

```json
"emulator/memory_hash": {
  "$comment": "protocol.md §6 (memory), added 2026-08-18 by §11.13 (CR-23). Fingerprints a byte range without moving it — the gap state_hash leaves (its five hashes cover VDP state only). A pure read: not run-state gated. Routes as read's bus space; -32004 past the end, refused never clipped. Distinct from §9's deferred frame_hash (a picture hash, served by state_hash includeFramebuffer).",
  "params": {
    "type": "object",
    "required": ["len"],
    "properties": {
      "addr": { "$ref": "#/$defs/hex", "description": "First byte to hash. D9 category 1." },
      "symbol": { "allOf": [{ "$ref": "#/$defs/symbolName" }], "description": "Resolved to an address (D7); alternative to addr." },
      "len": { "type": "integer", "minimum": 1, "maximum": 4194304, "description": "Bytes to hash. REQUIRED — a hash without a length hashes nothing. Bounded by limits.maxHashLen." }
    }
  },
  "result": {
    "allOf": [{ "$ref": "#/$defs/replyFields" }],
    "required": ["addr", "len", "region", "fnv1a64", "crc32"],
    "properties": {
      "addr": { "$ref": "#/$defs/hex" },
      "len": { "type": "integer", "minimum": 1, "maximum": 4194304 },
      "region": { "type": "string", "description": "Which region of the 68000 map the range landed in (work RAM / cartridge ROM)." },
      "fnv1a64": { "$ref": "#/$defs/hash64" },
      "crc32": { "$ref": "#/$defs/hash32", "description": "IEEE/zlib CRC-32, so a cartridge-window hash equals CRC32 over the same slice of the ROM file." },
      "caveat": { "type": "string", "description": "protocol.md §2.4. Conditional, never constant." }
    }
  }
},
```

### The demand evidence

- The Aeon gap list, Tier 1 item 3: *"region hashing for gates. The FNV code exists in `state_hash`."*
  The Aeon side's `ab_runner` gates compare memory regions between runs; a hash answers that without a
  payload where `read` would need `len/4096` round-trips.
- The legacy socket op + MCP tool shipped 2026-08-01 and the MCP validation pass filed it under
  *"backed by shipped core capability, exposable"* (quoted in CR-20's "What the collapse actually
  buys"). The capability exists; only the contract row is missing.
- The CRC-32 choice has an external consumer property: a cartridge-window hash equals `crc32` over the
  same slice of the ROM *file*, so an Aeon-side gate can precompute its expectation from the `.bin`
  without a running emulator.

### The `frame_hash` distinction, explicitly — because a CR that ignores a standing deferral invites the momentum-refund failure

§9 defers *"**Scenario ops** (`scenario_load/advance/seek`) and **`frame_hash`** — the replay &
regression primitives (later)"* (`protocol.md:1457-1458`), and §9.1 re-affirms it after lifting the
save-state deferral: *"What is **not** lifted: `scenario_load/advance/seek` and `frame_hash` stay
deferred"* (`protocol.md:1488`). **CR-23 does not touch that deferral, and is not a partial lift of
it.** `frame_hash` is a *picture* hash — a fingerprint of rendered output, whose live spelling is
already `state_hash includeFramebuffer` (`protocol.md:1152`) — and it stays deferred. `memory_hash` is
a *memory* hash, filling the opposite gap: `state_hash`'s five fingerprints (`vram`, `cram`, `vsram`,
`regs`, `combined`) cover VDP state only, and nothing on this bus hashes 68000 memory. The two methods
share a suffix and nothing else. This paragraph exists because the keep-dead register's caution is
*"do not let momentum re-fund these"* — an amendment that added a hash method without saying which
deferral it does *not* lift would be exactly the momentum-refund a later reader could cite.

### Open questions: none — pins 7-9, each with its reasoning

7. **`memory_hash` is a pure read** (not gated, answers free-running — like `read`, unlike
   `write_memory`): it advances nothing and mutates nothing, so §5's worked `-32005` examples give no
   ground for refusing it, and the checkpoint block's reasoning against uncontracted bounds applies.
   Params `addr`|`symbol` + **required** `len` (1..=`limits.maxHashLen` = 4194304 — the legacy op's
   cap, advertised rather than implied). Routes exactly like `debug_read` (work-RAM window or cart ROM,
   `-32004` past the end): one routing rule shared with `read`, not a second one to keep equal by hand.
8. **`memory_hash` ≠ the deferred `frame_hash`** (§9/§9.1): that is a picture hash and
   `state_hash includeFramebuffer` already serves it; `memory_hash` fills the gap `state_hash` leaves
   (its five fingerprints cover VDP state only — nothing today hashes 68000 memory). Spelled out in
   full above.
9. **CRC-32 lives in `oracle-aether`** (`src/crc32.rs`, const-table, dep-free), NOT in `oracle-core`'s
   `state_hash.rs` — that module is Oracle-byte-compat FNV territory with a do-not-touch warning, and a
   bus convenience with a different job does not belong inside a byte-frozen currency module.
   (Implementation placement, recorded here so the adjudicator sees the whole design; it changes no
   wire shape.)

---

## The close — fragment count, limits, and the adoption condition

**Fragment delta: 29 → 32.** Counted 2026-08-18, not transcribed: the vendored
`empyrean/contract/schema/bus-protocol.schema.json` `methods` object holds **29** method keys, every
one with a `result`. (The schema's own top-level `description` still says *"23 of §6's ~60"* — stale;
the amendment should correct it to the recount, per plan Task 3 Step 7.) These three CRs add three
fragments, none removes any: **32**.

**`initialize.limits` gains two keys** (pin 10 — without them the vendored-schema closure on
`initialize` would reject the handshake the moment a server advertises the new caps), verbatim from
plan Task 3 Step 7:

```json
"maxWriteLen": { "type": "integer", "description": "Byte ceiling for one emulator/write_memory payload." },
"maxHashLen": { "type": "integer", "description": "Byte ceiling for one emulator/memory_hash range." }
```

**Adoption condition**, in the §11.6/§11.8/§11.10/§11.11 lineage: *registered when a conformant reply
passes each fragment closed — happy path plus one refusal per catalogued bound per method.*
